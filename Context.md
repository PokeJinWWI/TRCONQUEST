TR: Conquest
# Overview:
"Terra Relicta: Conquest" is a web-based grand strategy game.

# Setting / Lore:
## FTL Travel
Two methods of faster-than-light travel exist:
- **Warp Drive** — uses "Exotic Matter," a form of matter found on certain planets with a higher mass-energy conversion rate than normal matter. Warp drives use exotic matter to warp and move space itself to travel.
- **Hyperdrive** — uses "Hyperium," extremely rare to find naturally but yields effectively infinite energy. Hyperium cannot be broken up/subdivided, so each ship equipped with a hyperdrive is extremely valuable. Hyperdrives use hyperium to access "hyperspace," a dimension outside the normal universe where time passes faster, allowing FTL travel. (Exact mechanics of hyperspace aren't important to nail down yet.)
- Both exotic matter and hyperium don't just power FTL drives — they also power the entire ship's systems and weapons.

## STL (sub-light) Travel
- STL warp drives tend to use **antimatter**, harvested via massive particle accelerators or extracted from locations with high "antimatter density."

## Timeline
- The game's campaign starts in the **year 2600**.

# Visual Style / UX Reference:
- The game should look like a tactical map, similar to **DEFCON** (see reference screenshots: minimalist neon vector map, arcs for missile/unit trajectories, glowing markers, DEFCON-style HUD/side panels).
- Scale/view structure (4 zoom levels, see View Levels below):
  - **Satellite view** (internal `level: 'satellite'`, was called "planet view" — renamed since it now also handles stars and, eventually, other close-up-inspectable things like asteroids/black holes): close-up of a single planet or star, with any of its moons.
  - **System view**: 2D-ish top-down (like DEFCON's flat world map).
  - **Interstellar view**: 3D, styled like an **Obsidian graph view** — nodes (nearby stars) connected by edges.
  - **Galactic view**: 3D, same graph style, zoomed out to galaxy scale (placeholder only so far).

# Tech Stack:
- **Vite + React + TypeScript** (scaffolded via `npm create vite@latest -- --template react-ts`).
- **three.js + @react-three/fiber + @react-three/drei** for 3D rendering — chosen because the interstellar/galaxy view needs true 3D navigation, and it also drives the in-system view. `OrbitControls` (via drei) handles camera drag/zoom/pan in every view.
- **zustand** for shared app state: `gameTimeStore` (simulation clock) and `viewStore` (navigation level).
- Dev server: `npm run dev` (Vite, port 5173). `.claude/launch.json` is configured so the Browser preview tool can launch it by name `trc-dev`.
- Build: `npm run build` (runs `tsc -b && vite build`) — verified passing. Type-check only: `npx tsc --noEmit`.

---

# Economy & Politics Track — Open Items
(The collaborator's own running notes on the economy/politics track specifically — see this file's own "Two-person team split" note further down, under Additional Context. Kept separate from this document's own Roadmap/Current Objective, which track the combat/tech side.)

## Objective (Economy/Politics track)
TRCONQUEST ("Terra Relicta: Conquest") — a browser-based sci-fi 4X/grand-strategy game (TypeScript + React + Three.js/react-three-fiber, Vite, Zustand stores). Current focus: building a **Victoria 3-flavored economy simulation** (pops, supply/demand, national government, politics) adapted to a space setting, at a bounded scale so it stays fast in the browser.

## Current State
- **Industry-depth + trade + balance pass (latest session) DONE** — build green (tsc + all economy tests + combat/scenarios/escape + lint):
  - **Machinery taxonomy (5 goods)**: `tools` → `machinery` → `heavyMachinery` (mechanical ladder) + `electricalMachinery` (electronics-based branch: motors/generators/drives) + `precisionMachinery` (advanced high-tolerance: semiconductors/aircraft). Producers: toolWorkshop, machineryFactory, heavyMachineryPlant, electricalMachineryPlant, precisionMachineryPlant. Broadly consumed (extraction/farms use tools; mills/plants use machinery; deep extraction/heavy industry use heavy; power/electronics/advanced use electrical; semiconductors/aircraft use precision).
  - **Engines & vehicles**: `engines`, `automobiles` (pop comfort), `locomotives`, `aircraft` (pop luxury) + engineFactory/automobilePlant/locomotiveWorks/aircraftFactory. Engines feed heavy machinery + all vehicles.
  - **Infrastructure = market access**: `infrastructure` service good produced by roadNetwork/railway/spaceport (category 'services', `LOGISTICS_OUTPUT` map); consumed by pops (everyday need); its output RAISES the country's freight capacity (`effectiveLogistics = base + Σ infrastructure`) — the Vic3-style market-access lever that M5 trade uses. Railways need locomotives, spaceports need aircraft, roads are the cheap universal.
  - **Balance pass → empire SoL ~0.80** (all worlds 0.69–0.92), all nations run DEFICITS again. Key levers now: `JOB_SCALE = 0.15` (was 0.3 — fixed labor-starvation from the deep building roster), `SUBSISTENCE_INCOME = 3.5` (informal-economy income floor so under-employed worlds aren't destitute — NEW), `WAGE_FLOOR = 1.0`, `INVENTORY_DECAY = 0.08`, `PUBLIC_SPENDING_PER_CAPITA = 4.5` (restores deficits on the now-richer economy), and boosted per-level outputs for power (solar 850 / coal 2500 / fusion 3600), steel (950/1250), food (grain 2000 / mixed 2700), clinic (560/950). Diagnosis method that worked: dump per-good supply/demand ratios to find the bottleneck (it was electricity → steel → everything; then food price crowding out everyday needs; then under-employment poverty on small nations).
  - **Bug fixed**: `infrastructure` was in the GoodId union + recipes but missing from `GOOD_IDS`, so its price was undefined → NaN cascade across the whole sim. (Lesson: every new good must be in the union, GOODS record, AND GOOD_IDS.)
  - **STILL PENDING (told user, not done)**: government **subsidies** (companies + individual buildings) and per-**building** nationalisation (setBuildingOwner exists; needs UI + confirm). Corp AI (build/expand/close/invest heuristics) — recommended as highest-leverage next. Financial-district dynamic spawn + dividends; trade price-blending/tariffs.

## Current State (Economy/Politics track)
- **Setting/rendering** already existed: 8 real nearby star systems with hand-authored planets/dwarf planets/asteroid belts, multi-star rendering (Alpha Centauri triple, Sirius/Luyten binaries), ownership/countries, a nation-select main menu, a left NavBar (Government/Economy/Military/etc. categories with sub-tabs, resizable draggable windows), a bottom ActionBar (Buildings/Politics/Diplomacy docked panels that also switch map modes), map modes (GDP/Political), and an Outliner with reserved sections.
- **Economy — Milestones 1, 2 AND 3 of the merged "v2" design COMPLETED and verified live**, plus corporations/characters/stock-exchange and a full goods+power+buildings overhaul (clean restructure). Three layers:
  - **Country** (national government): one treasury, taxRate, welfarePerCapita, debt, credit rating, **economicSystem law** — spans all its worlds.
  - **World** (local economy): pops, buildings, local market, labor — NO treasury.
  - Trade/logistics between worlds NOT built yet (each world's market is local/isolated for now).
- **Milestone 2 (production methods + throughput + qualified employment) DONE**:
  - **Production Methods**: each building type (`recipes.ts`) has ≥2 selectable methods (e.g. farm manual vs mechanized) with different inputs/outputs/worker mix. `Building.methodId` + `getMethod()`.
  - **Throughput**: `Building.throughput` (0..1) ramps toward what labor/inputs/demand allow (`rampToward`, RAMP_UP 0.05 / DOWN 0.15) — kills profit "teleporting"; new buildings start at 0.1, seeded/established at 1; switching a method retools (×0.85 dip).
  - **Qualification-gated employment**: `CLASS_QUALIFICATION` + pop `educationLevel` (seeded by class) → qualified labor; under-schooled classes leave skilled jobs understaffed → buildings throttle. Report exposes `qualified`/`qualifiedRate`; buildings report `employed`/`jobsPosted`.
  - **Owner autonomy + economic-system law** (`src/economy/laws.ts`, user-requested): private buildings (stateFraction < 0.5) autonomously pick their most-profitable method each tick (hysteresis `OWNER_SWITCH_MARGIN`); the state CAN override (pins the method) but under a market economy that interference cuts output (`interferenceMalus`: laissez-faire 0.7, interventionism 0.9, command 1.0). Command economy = no malus but no owner self-optimization. UI: method dropdown, **Private/State/State-set** tags (tooltips name the owning nation), release (↩) button. Store: `setProductionMethod` (pins+dip), `releaseProductionMethod`, `setEconomicSystem`.
  - **UI polish pass (user-requested)**: (1) **All money is USD now** — `formatMoney`/`formatPrice` in `format.ts` ($ + K/M/B, minus for negatives); applied across BuildingsPanel (GDP/treasury/profit), EconomyPanel (fiscal rows, Finance headline+graphs, market prices), PopsPanel (wealth). Currency is a placeholder — real setting money undecided. (2) **Economic system is a LAW**, not a building-tab toggle — moved to **Government → Laws** (`LawsPanel.tsx`, enact buttons + effects); BuildingsPanel shows it read-only ("set it in Government → Laws"). (3) **Pops are now visible** — new **Pops tab** in the planet inspector (`PopsPanel.tsx`): cohort table (class × religion, culture+species header) with population, USD wealth, and a Needs% (avg satisfaction of the species' real tiers — a stand-in until the M3 SoL number). (4) Ownership legibility: Overview tab already names the owning nation; building tags now read Private/State.
- **Milestone 3 + corporations/characters/stock-exchange + goods/power/buildings overhaul DONE** (big multi-system pass, all user-requested):
  - **Goods overhaul** (`goods.ts`): 23 goods incl. **electricity** (first-class universal input), raw (ironOre/coal/oil/rareMetals/timber/phosphate), crops (wheat/rice/livestock), intermediates (steel/lumber/fuel/chemicals/fertilizer/explosives/semiconductors/machinery/electronics), consumer (food/consumerGoods/luxuryGoods/medicine). `zeroGoods` is now generic over GOOD_IDS.
  - **Buildings overhaul** (`recipes.ts`): ~24 specific buildings across categories energy/extraction/agriculture/industry/healthcare, each with power + input chains (Solar/Coal/Fusion plants; Iron/Coal/Oil/RareMetals/Phosphate mines + Logging; Wheat/Rice farms + Livestock ranch; Steel mill, Sawmill, Oil refinery, Chemical/Fertilizer/Explosives/Semiconductor/Machinery/Electronics/Food/ConsumerGoods/Luxury factories; Clinic). Extraction/power have power-free MANUAL methods (bootstrap); mechanized methods consume power+machinery. `JOB_SCALE` (0.3) sets labor productivity so the roster fits the population.
  - **Ownership model** (`economyTypes.ts` `BuildingOwner`): every building owned by **state** (profit→treasury), a **corporation** (profit→corp cash), or **worker co-op** (profit→pops' wealth as dividends). Replaces the old `stateFraction`. Autonomy + interference-malus now key off `owner.kind`.
  - **Corporations** (`Corporation`): state or private companies owning buildings, with cash, a cap table (`shares`/`totalShares`), a leader character, sector. Seeded: **Martian Restoration Administration** (state agri-corp, owns Mars farms, led by Aurelia Vance) and **Redmines** (private miner, owns Mars mines, led by Doran Kessler). `corporationValue`/`sharePrice` in economyTick.
  - **Characters & families** (`Character`/`Family`): traits, skills (admin/finance/diplomacy), wealth, log, family membership. Seed families Vance & Kessler. Store action `characterAction` (grant-funds/demand-dividend/mentor/honor — real effects).
  - **Shareholding + stock exchange**: `tradeShares` (state buys/sells public float, costs/credits treasury), `nationaliseCorporation` (dissolve→state), `privatiseCorporation` (float a state corp), `createCorporation`, `setBuildingOwner`.
  - **Milestone 3 SoL loop** (`economyTick.ts`): `pop.standardOfLiving` from weighted needs satisfaction (smoothed); drives **population growth** (GROWTH_RATE 0.012, midpoint 0.35 — gentle) and **education drift** (toward SoL). Consumption is tiered/budget-constrained; richer pops reach comfort/luxury. **A demand-throttle→inventory-decay change** (`INVENTORY_DECAY` 0.12, removed demandScale from the throughput target) fixed a deep-chain deadlock. Steady state: SoL ~0.3–0.8 across worlds, populations grow and stabilize.
  - **Money display** rescaled: `formatMoney` multiplies internal units by `USD_PER_UNIT` (1e6) so aggregates read in **billions/trillions** ($100B treasury, $194B world GDP, $74B corp value). `formatPrice` stays small (per-unit).
  - **New UI**: **Corporations** category (State Owned/Private tabs — found/nationalise/privatise), **Stock Exchange** category (buy/sell shares), **Society→Demographics** (empire bars: SoL distribution, occupations, culture, religion, species), **Characters** category (detail + interactions + families). **Buildings tab Owner column** now names the owner (State / corp name / Co-op). **Pops tab** shows species, culture, SoL, and the species' needs basket by tier. All wired in `NavBar.tsx`; new components: `CorporationsPanel`, `StockExchangePanel`, `DemographicsPanel`, `CharactersPanel`, `LawsPanel`, plus rewritten `PopsPanel`/`BuildingsPanel`.
  - **Tests**: `tests/economy.test.ts` rewritten — 16 sections (~60 checks) incl. power flow, ownership profit-routing, SoL loop, corp value/shares; all pass. tsc + lint clean.
  - **Balance is tuned but not final**: some worlds lag (e.g. Venus/Lalande healthcare low — clinic input starved). Per-world building sets are hand-authored for local self-sufficiency (no inter-world trade yet — that's M5). Levers: `JOB_SCALE`, `INVENTORY_DECAY`, `GROWTH_*`, species needs amounts, seed building levels.
- **Coherence pass + services/bonds/bureaucracy (big user-requested pass) DONE**:
  - **Ticks are now MONTHLY** (`SIM_DAYS_PER_ECONOMY_TICK = 30`, `TICKS_PER_YEAR = 12`); growth/edu/interest rates rescaled to per-month.
  - **Pop growth fixed** (was ~30×/yr too fast): `GROWTH_RATE = 0.0016`, ~2-3%/yr peak. Populations now stable.
  - **Deficits are real** — no free surpluses. `PUBLIC_SPENDING_PER_CAPITA = 2.3` baseline + generous welfare defaults (2.0/1.7) + **public healthcare** funding push default budgets into deficit (Mars ~−$13B/mo). Welfare clamp raised to 10.
  - **Services + healthcare law**: goods now include the SERVICES `healthcare`/`education`/`retail` (replaced `medicine`); produced by `clinic`(→healthcare)/`school`/`retailShop`. **Healthcare law** (`laws.ts` public/mixed/private): public = state funds all pops' healthcare (`serviceSubsidy` → expenditure, a deficit driver); pops pay their share only. Set in Government→Laws.
  - **Bonds/debt** (`Country.bonds{pops,corporations,foreign}` + `bondRate` + `foreignBondPolicy` law + `requireForeignApproval` setting + `pendingForeign` offers). Store: `issueBonds`/`redeemBonds`/`setForeignBondPolicy`/`setForeignApproval`/`approve|rejectForeignOffer`. Foreign demand offers generated in `advance()` every 12 ticks. UI: **Economy→Debt** panel (`DebtPanel`).
  - **Bureaucracy** (`Country.bureaucracy` + `decrees`): a national resource PRODUCED by government buildings (`governmentOffice`/`ministry`, category `government`, `BUREAUCRACY_OUTPUT`), stored to a capacity, CONSUMED by directly-state-owned buildings (high rate) + state-CORP buildings (low rate — the "give it to a state enterprise to save bureaucracy" incentive) + decrees. Running dry applies `BUREAUCRACY_SHORTAGE_MALUS` to state buildings. Shown in the **top bar** (`FiscalIndicators`).
  - **Corporate HQ**: each corp auto-gets a `corporateHq` building (`syncCorporateHqs` in store), level scales with asset count; pure overhead (employs pros, no output → cost center reducing corp cash).
  - **Nationalize fixed**: converts a private corp to a STATE-OWNED enterprise (kept, shows in State Owned tab) — was deleting it. Costs 60% compensation from treasury + a bureaucracy hit.
  - **Money → billions/trillions** already (USD_PER_UNIT 1e6). **Top-bar indicators** (`FiscalIndicators`): Treasury, Balance/mo, Bureaucracy stock/cap/net.
  - **Buyers/sellers visible**: Market tab rows expand on click to list producing buildings (sellers) + consuming buildings + Households (buyers) — `marketParticipants` in EconomyPanel.
  - **Species = Human**: baseline-organic renamed "Human" (Martians/Venusians/Arcadians are human cultures; only Tidalians are alien). **Pops tab** drops volatile wealth column, leads with SoL.
  - **DEFERRED (told user)**: Espionage system (standalone, large) and **Milestone 5 Trade & logistics** (huge — lets worlds specialize instead of each being self-sufficient) — the clear next items. Also foundational-only: decrees have upkeep cost but no UI to enact yet; balance still uneven (Redmines/mining runs at a loss — raws glut to price floor).
- **UX + districts + construction pass (user-requested) DONE**:
  - **Building detail popup**: click a building row in the Buildings tab → expands full detail (`BuildingDetail` in BuildingsPanel): **inputs/tick, outputs/tick, employment by class**, owner, description, throughput, profit. Answers "WHERE ARE THE INPUTS".
  - **Confirmation dialogs**: `confirmStore` + `ConfirmDialog` (rendered in App). Impactful actions route through `requestConfirm({title, body, effects[], onConfirm})` — a modal listing what you gain/lose before you commit. Wired to nationalise, privatise, and law enactments (economic system + healthcare). Reusable for more.
  - **Districts**: worlds have finite `districtCapacity` per `DistrictType` (`core`/`urban`/`industrial`/`resource`, `recipes.ts`). Category→district: government/corporate→core, services→urban, industry/energy→industrial, extraction/agriculture→resource. `districtUsage`/`canBuild` in economyTick gate construction; build buttons disable when a district is full.
  - **Construction pools + private construction**: `ConstructionOrder.owner` decides who pays — GOVERNMENT pool (state orders, treasury-funded) or PRIVATE pool (corporation orders, funded from that corp's cash, `corpCash` threaded through tickWorld, deducted in tickEconomy). `queueConstruction(worldId, recipeId, owner?)`. Construction **consumes building materials** (steel/machinery/consumerGoods added to market demand → fuels the economy). New **Economy→Construction** panel (`ConstructionPanel`): district usage bars, pool balances, a **Funded-by** selector (Government or a corporation), build buttons (district-gated), and the queue with owner + progress.
  - Construction "methods"/construction-site buildings — not built (pools + material consumption cover the intent).
- **M5 trade + financial districts + share-fix pass (user-requested) DONE**:
  - **Corp kind/ownership fix**: a corporation's `kind` is now DERIVED from state shareholding (store `derivedKind`) — selling off all of a "state" corp on the exchange flips it to private (was incongruous: kind stayed 'state').
  - **Milestone 5 — Trade & logistics**: `Country.logisticsCapacity` (freight per tick) + `World.importStock`. After the world loop, tickEconomy ships each good's surplus (unsold building inventory) to the country's deficit worlds within capacity, minus `TRANSPORT_LOSS` (12%); delivered goods become import stock the world draws on next tick (added to supply). Worlds can now SPECIALISE (verified: a farmless Luna imports food from Mars). Fiscal reports `tradeVolume`/`logisticsCapacity`. New **Economy→Trade** panel (`TradePanel`): capacity, shipped/tick, per-world imports. NOTE: price-blending (MAPI) and money-side inter-world payments/tariffs are the remaining refinements; trade currently moves physical goods (transport cost = transit loss).
  - **Financial districts** (`kind: 'financial'`): auto-formed in seed on any world ≥100M pop (`makeFinancialDistricts`) — a co-op-like institutional entity that owns a **Financial Center** building (new recipe, core district, produces the retail/finance service) and holds a 15% stake in its country's private corporations (new `ShareHolder` kind `'financial'`). Shown in a new **Corporations → Financial Districts** tab. Grows via the same corp machinery. (Dynamic auto-spawn on population growth is NOT wired — seed covers all current worlds; add a store hook when worlds can grow past the threshold.)
  - **STILL DEFERRED**: Espionage (standalone, large). Corp AI (build/expand/close/invest heuristics) — the user flagged corps "just exist"; I recommended building this next as the highest-leverage work (turns the machinery into a living economy). Financial-district dynamic spawn + dividend flow to shareholders. Trade price-blending/tariffs.
  - **Stack decision (user asked)**: agreed to STAY TypeScript/web — the bottleneck is rendering not the sim (sub-ms ticks), there's a large working+tested codebase, and the sim is already pure/headless so a Rust→WASM port stays a contained future option. The real gap is agent behavior (corp/country AI), not the stack.
- **Content expansion + subsidies/nationalization + needs-SoL rework + building-menu UI pass (user-requested, large multi-batch session) DONE**:
  - **14 new goods + buildings** (`goods.ts`/`recipes.ts`/`economySeed.ts`): `meat`(meatPacking, livestock→meat), `sugar`/`coffee`/`tea` (sugarPlantation/coffeePlantation/teaPlantation on Arcadia, distinct from generic crops per user's explicit coffee≠tea request), `dyes` (dyeWorks), `glass` (glassworks), `sulfur` (sulfurMine, extraction), `hardwood` (hardwoodLogging, extraction, separate deposit pool from `timber`), `paper` (paperMill), `art` (artStudio, a services-category "culture" good) and `onlineServices` (dataCenter) both seeded on Venus, and three capital-ship goods — `oceanGoingShips` (shipyard), `spaceships` (spaceyard, expensive/low-throughput late-game), `rockets` (rocketFactory) — all seeded on Mars. `ironOre`/`coal` already existed (no change). Every good confirmed present in the union type, `GOODS` record, AND `GOOD_IDS` array (the place a past NaN-cascade bug came from omitting one).
  - **Resource depletion**: `World.resourceDeposits?: Partial<Record<GoodId, number>>` — a finite reserve for genuinely finite raw goods (`DEPLETABLE_GOODS` = ironOre/coal/oil/rareMetals/sulfur/hardwood/timber/phosphate, exported from `economyTick.ts`), NOT crops/manufactured goods. Seeded per world at ~800× a building's per-tick output (~66 years of monthly ticks — a long-game pressure, not a near-term crisis). Each tick, extraction output is capped at `min(intendedOutput, remainingDeposit)` and the deposit is drawn down, floored at 0 (a fully depleted mine idles, producing nothing). Answers the user's "are resource buildings unlimited? We don't have unlimited iron mines on earth" directly.
  - **Government subsidies**: `Country.subsidies: {corporations: Record<string,number>; buildings: Record<string,number>}` — a real per-tick treasury cost (folded into `expenditure`/deficit, not free money) crediting either a corporation's cash directly, or (for a building subsidy) whichever corp owns that building — a state/worker-owned building's subsidy still costs the treasury but has no separate account to land in. Store: `setSubsidyForCorporation`/`setSubsidyForBuilding`. UI: a "Subsidize/tick" ± stepper on each corp card (`CorporationsPanel`) and in each building's detail popup (`BuildingsPanel`). New `CountryFiscal.subsidiesSpent` line shown in the Budget tab.
  - **Per-building nationalization**: `nationaliseBuilding(worldId, buildingId)` — the store already had `setBuildingOwner` internally but no player-facing entry point for seizing ONE building regardless of its owning corp's overall status. Mirrors the existing full-corp-nationalization pattern but scoped to one asset: compensation = 60% of `level × BUILD_COST_PER_LEVEL` if corp-owned, 15% (flat disruption cost, no shareholders) if worker-co-op-owned, plus a smaller bureaucracy hit (`50 + level*30`). Routed through the same `requestConfirm`/`ConfirmDialog` flow as corp-level nationalize. Deliberately did NOT build the reverse direction (state building → a specific corp) — ambiguous which corp should receive it.
  - **Needs/SoL presentation rework**: the underlying mechanic was already goods-based (species `needs` basket per tier), but `PopsPanel` only showed one blended 0-100% number per tier, which the user said "doesn't make sense." Added `Pop.needsDetail?: Record<NeedTier, {good, wanted, consumed}[]>` (additive — `needsSatisfaction` kept, other code still reads it, now derived-consistent with the detail) populated every tick alongside the existing satisfaction calc. `PopsPanel` now shows a per-tier collapsible breakdown with real numbers ("Food 0.80/0.80 — 100%") — healthcare renders as an ordinary tier, not siloed.
  - **Stockpiling mechanic** (new, not previously designed): per-world `World.stockpiles`/`World.stockpileTargets: Partial<Record<GoodId, number>>`. Store: `setStockpileTarget(worldId, good, targetAmount)`. Each tick: a capped share (10%/tick, ≤50% of that tick's shortage) of the reserve auto-releases into supply BEFORE market clearing when there's a genuine shortage (so it actually cushions that tick, not just the next); a capped share of genuine leftover surplus (10%/tick, ≤50% of surplus) fills the reserve, bought from building owners at real cost (drawn from the same treasury pool construction uses, reported as `CountryFiscal.stockpileSpend`). New **Economy→Stockpiles** panel (`StockpilePanel.tsx`): per-world target-setting UI with progress bars.
  - **Building-menu UI clarity**: the "menu below" (bottom ActionBar's Buildings dock) and "the planetary menu" turned out to be the SAME underlying component (`BuildingsPanel.tsx`, docked two different ways) — fixing it once fixed both surfaces; the same treatment was also applied to `ConstructionPanel.tsx` for consistency. Added a purely additive/presentational grouping lookup in `recipes.ts` (`BuildingGroup` type, `RECIPE_GROUP` map, `buildingGroup()`) sorting all ~56 recipes into 10 groups (Power/Extraction/Agriculture/Heavy Industry/Chemicals & Materials/Consumer Manufacturing/Vehicles & Craft/Infrastructure/Public Services/Corporate & Government) — no change to any recipe's actual economic data. Tabs with >6 distinct building types now get sub-group divider header rows in both the existing-buildings table and the build-button grid. Extraction buildings on a `DEPLETABLE_GOODS` good now show their remaining reserve inline (e.g. "1.2M · ~66.7 yrs at this rate", or "depleted") in both the compact row and the expanded detail popup.
  - **Re-verified, no changes needed**: the corp share/privatise congruity concern the user re-raised was already fixed (`derivedKind()` computes `kind` from actual state shareholding on every `tradeShares` call, and the UI only ever shows Nationalise for a private corp / Privatise for a state corp — there's no path to click Privatise on an already-private corp). Live-checked corp profitability: both a state corp (Martian Restoration Administration, $5.45B/mo) and a private corp (Redmines, cash growing tick over tick, profit/tick fluctuating but staying positive) are currently healthy — no corp-AI work was needed this pass; that recommendation from the prior session stands as the next high-leverage item whenever the user wants it.
  - Executed as 4 sequential background-agent batches (goods/buildings/depletion → subsidies/nationalization → needs-SoL/stockpiling → building-menu UI), each independently verified (`tsc -b --noEmit` clean, `tests/economy.test.ts` all sections + new sections added per batch — now **24 sections**, `combat`/`scenarios`/`escape` tests, `npm run lint` 13 pre-existing warnings unchanged, live browser smoke test) before moving to the next, specifically to avoid repeating the earlier NaN-cascade/scope-pileup mistake. All 4 batches committed **locally only** — the user asked not to push/commit further this session ("DO NOT COMMIT TO GITHUB I WILL DO IT MYSELF"), so nothing has been pushed to the remote and no further commits should be made without being asked again.
- **Production-method input coherence pass (user-flagged) DONE** (uncommitted working tree, per the "never commit, I'll do it myself" standing instruction):
  - The user noticed "electric arc for steel doesn't use machinery but blast furnace does" and flagged it as a pattern. Audited every multi-method recipe for the anti-pattern *"an upgrade method consuming LESS capital than its base."* Fixed 6: steelMill electricArc (no machinery → `electricalMachinery 20`, the coherent mechanical→electrical swap), consumerGoodsFactory refined (`tools 30→45`) + automated (zero capital → `machinery 70`), foodProcessor mixed (dropped tools → `tools 35`), coalMine mechanized ("cutting machines", no capital → `machinery 15`), phosphateMine mechanized ("strip mining", no capital → `tools 25`). Deliberately LEFT ALONE: all **manual** methods (capital-free is the intended bootstrap tier) and the **clinic** (its "advanced" method *adds* inputs, and hospital equipment isn't well-modeled by industrial machinery goods). No output values inflated, no seed changes — kept surgical. Verified SoL still 0.74–0.85 across worlds; confirmed via before/after probe the corp losses are pre-existing, not caused by this (Redmines −2687→−2747 profit/t, ~2%).
  - Also made the brittle **section-17 test** robust: it now compares corp cash at the construction-COMPLETION tick (isolating the ~6000 build spend) instead of after a 40-tick window where operating profit could erase it. (A crashed background agent had earlier attempted a sprawling recipe rebalance with output inflation + seed edits and left it self-described broken; that WIP was set aside in `git stash` and NOT used — the surgical fix above replaced it.)
- **Country AI — first pass (Paradox/Stellaris-style non-player nation brains) DONE** (uncommitted working tree):
  - **Design frame agreed with user**: heuristic game-AI (NOT LLM — LLM is impractical as the per-tick decision engine for dozens of agents in a real-time browser sim; its future place is a *flavor/narration layer* — leader speeches, diplomatic wording, an advisor you can chat with — over decisions the heuristic already made). The country AI is ONE brain per nation composed of specialized MANAGER subsystems (like Paradox), not a monolith and not independent agents. Started with the governance/economy manager because that layer is deeply simulated today; MILITARY and DIPLOMACY managers plug into the same brain later when those systems are AI-wired.
  - **New file `src/economy/countryAI.ts`** (pure, headless, no React/DOM — same portability discipline as the rest of the sim, so a future native/Steam port moves the whole sim+AI as one contained unit; this is *why* it's TypeScript and not another language — a language boundary mid-tick would be slow and would fracture a future port, not help it). Three managers: **fiscal** (raise tax / trim over-generous welfare on a deep deficit or sub-A rating; spend a fat surplus down toward pops when AA+ and low-debt — keeps AI nations near break-even, never in a permanent green), **debt** (fund an unfunded overdraft with bonds unless debtToGdp ≥ 1.8; redeem when flush; auto-resolve its own foreign bond offers since the AI has no human to approve them), **governance** (one government-funded construction decision per review: priority 1 build a ministry/governmentOffice when bureaucracy runs dry or near ceiling; priority 2, for command/interventionist economies only, build the growth building whose output good is priciest = most under-supplied — respecting district capacity). Reviews are staggered per-nation (a stable id-hash phase) and spaced (fiscal/debt yearly, construction twice-yearly) so policy doesn't oscillate.
  - **Wiring**: `tickEconomy(countries, worlds, corps, ai?: CountryAIOptions)` — a new **optional** 4th arg `{ humanCountryIds?, tick?, enableAI? }`, defaulting to OFF so every existing headless caller and all economy tests keep byte-identical behavior. Runs at the END of tickEconomy (after production/fiscal/reports settle) for each country whose id is NOT in `humanCountryIds`, so decisions land next tick. **The game is MULTIPLAYER — `humanCountryIds` is a SET, not a single id**, so any number of nations can be human-run at once and all are excluded (in a networked game the authoritative sim passes every connected player's nation; only unclaimed nations get AI). `economyStore.advance()` turns it on, passing `[selectedCountryId]` (this local store knows only its own player) — so **the player nation(s) are whatever the humans picked, never hardcoded**.
  - **Verified**: new **test section 25** (AI is strictly opt-in — no options = no policy change; an AI nation in a forced deep deficit raises tax + trims welfare + funds its overdraft with bonds; the parameterized PLAYER nation is left byte-untouched; a bureaucracy-starved command nation queues a government building). tsc clean, **all 25 economy sections pass**, combat/scenarios/escape pass, lint unchanged (15, all pre-existing — my code adds zero). Headless 60-tick probe (player=Mars example): AI nations moved tax 0.09–0.12 → 0.18–0.22, welfare → 0.8, and financed deficits with bonds so treasuries sit near −$4k instead of drifting to −$600k unfunded; the player nation stayed byte-identical between AI-on and AI-off runs. Live browser smoke test: game runs with AI enabled, no console errors.
  - Country-AI future managers: military, diplomacy, laws (currently the country AI does not change laws/economic-system/healthcare — left static in v1).
- **Corporation AI — the "invisible hand" (first pass) DONE** (uncommitted working tree):
  - **New file `src/economy/corporationAI.ts`** (pure/headless). Runs for EVERY company (in the player's nation too — a market economy's firms run themselves; the player governs via law), except financial districts (institutional investors, not operators — skipped). Amoral profit-maximizer, twice-yearly staggered review. Two managers: **investment** (if cash > 2.5× build cost and not already building: EXPAND the most profitable owned building — leveling it up, a proven winner is the safest capital — else FOUND a new building in the highest-gross-margin `FOUNDABLE_SECTOR` on a home-country world with district room; the foundable list is deliberately the margin-rich manufacturing/luxury/capital lines and EXCLUDES essentials like food/healthcare/power/raw extraction — the "exploitation" is concrete: a firm expands an essential it already profits from but won't newly invest in one when a luxury earns more, leaving essentials to the state) and **divestment** (user's refinement: "they'll try to make losers profitable but at some point pull out" — the tick loop already runs each building at its best method every tick = the "trying"; after a building stays unprofitable for `DIVEST_STREAK = 12` consecutive ticks it is CLOSED, recovering `SALVAGE_FRACTION = 0.3` of book value as cash and freeing the district; workers return to the labor pool).
  - **Supporting change**: `Building.unprofitableStreak?: number` (economyTypes) — consecutive loss ticks, incremented/reset in `tickWorld`; the divestment trigger.
  - **Wiring**: same opt-in `enableAI` path in `tickEconomy` (runs after the country AI, over every corporation, threading salvage cash back into the corp and building removals/orders into the worlds). Off by default → tests/headless unchanged. `runCorporationAI(corp, worlds, tick) → { corp, worlds }`.
  - **Verified**: new **test section 26** (cash-rich company with a profitable building invests; a streak-≥12 loser is divested with salvage; a brief-streak dip is KEPT — tries before bailing; a financial district builds no ordinary industry). tsc clean, **26 economy sections pass**, combat/scenarios/escape pass, lint unchanged (15, all pre-existing). Headless 10-yr probe (player=Mars): with the AI, the two seeded corps stop their runaway bleed — MRA −228k cash/−8,602 per tick with 6 buildings → divested to 1 building, cash +75k near break-even; Redmines −367k/−7,345 with 7 mines → PULLED OUT of all 7 losing mines, bleeding stopped.
  - **The seeded-corps-own-losers finding** (from the corp-AI probe): with the AI the seeded corps initially STABILIZE by shedding losers rather than THRIVE, because the seed hands corporations the money-losing primary sector while profitable value-add is mostly state-owned. The user chose to fix this via **dividends + primary-sector rebalance** (below), not by seeding corps value-add.
- **Dividends + primary-sector rebalance (user-chosen fix for corp profitability) DONE** (uncommitted working tree):
  - **Dividends** (`distributeDividends` in `economyTick.ts`, exported + tested): each profitable company pays out `DIVIDEND_RATE = 0.35` of its OPERATING profit to shareholders — state shares → the nation's treasury, public shares → the company's home-nation pops' wealth (spread by population), financial shares → that financial district's cash. Character-held shares are RETAINED for now (character wealth settled elsewhere; characters aren't threaded into tickEconomy). Crucially paid on operating profit only: subsidies are tracked separately (`corpSubsidyTotal`) and EXCLUDED, so the state never funds shareholder payouts. Runs each tick right after the corp profit payout, before the AI. This is the income side of the for-profit economy — it makes shares, the stock exchange, and financial districts actually pay.
  - **Primary-sector rebalance = a RELATIVE price floor** (`goods.ts`): the floor was an absolute `PRICE_FLOOR = 0.1`, so over-supplied raws (mines/farms) crashed to ~3% of value, far below extraction cost → primary producers could never break even. Added `PRICE_FLOOR_FRACTION = 0.4` and `priceFloor(good) = max(PRICE_FLOOR, basePrice*0.4)` (symmetric with the existing relative ceiling); `economyTick` clamps to `priceFloor(g)`. A good now never sinks below 40% of base value. RESULT (10-yr full-AI probe): Redmines flipped from −7,345/tick bleeding to **+1,145/tick profitable** (kept its mines); raws hold above cost (iron 1.20, coal 0.80, wheat 0.80).
  - **Verified**: new **test section 27** (public dividend → pop wealth; state dividend → treasury; financial-district dividend from stakes; a loss-making company pays nothing). tsc clean, **27 economy sections pass**, combat/scenarios/escape pass, lint unchanged (15). Note `0.4` beat `0.3` on BOTH corp profit AND SoL in probing (profitable mines stay open → more supply/jobs, which helps SoL more than cheap raws do), so 0.4 was kept.
- **Per-building level up/down controls (user-requested) DONE** (uncommitted working tree): the player asked to up/downgrade buildings without scrolling to the build section. Each STATE-controlled building row's Level cell now has inline **− / level / +** buttons (`BuildingsPanel.tsx`, `.bld-level-*` CSS): **+** calls `queueConstruction(world.id, b.recipeId, {kind:'state'})` — a normal queued upgrade (levels up the existing same-owner building via the construction step; keeps cost/time/materials; disabled when the district is full, tooltip shows the cost); **−** calls the new store action `downgradeBuilding(worldId, buildingId)` — INSTANT (unlike the queued upgrade): drops one level, or demolishes at level 1, recovering `DOWNGRADE_SALVAGE = 0.3` of a level's build cost as salvage to the owner's pool (state→treasury, corporation→its cash; a demolished corp building re-syncs HQs). Controls show only for `owned && control==='state'` (the buildings the player runs directly); corp/worker rows still show just the number (market-run). Live-verified: − dropped a mill 5→4 and credited the $1.8B salvage to the treasury, + queued an upgrade row, no console errors. tsc/tests/lint all green.
- **Per-building + on ALL owned rows + building-tab reorganization (user-requested) DONE** (uncommitted working tree):
  - **+ on every owned building row** (extending the earlier state-only controls): the player asked for a + on private/SOE/co-op rows too. Now EVERY building on an owned world shows a **+** (an empty aligned spacer sits where the − would be on non-state rows); the − stays only on directly-state-run buildings. The + ALWAYS queues a STATE-owned building via `queueConstruction(world.id, b.recipeId, {kind:'state'})` — so clicking + on e.g. a private Redmines Coal Mine builds a *separate* state-owned Coal Mine (the user explicitly accepted this: "it would be separate"); on an existing state building of that type it levels it up. Live-verified: +/− on state; + only on Redmines/MRA/Co-op/Financial-District rows; clicking + on a private Coal Mine queued a state Coal Mine.
  - **Building-tab reorganization** (`BuildingsPanel.tsx` `TAB_GROUPS`, `ActionBar.tsx` subtabs): "Development" was a catch-all (`null` → showed EVERY building), and energy sat under "Resources" with the mines. Tabs now filter by the fine-grained BUILDING GROUP (`buildingGroup(recipeId)`), not the broad category, so each building has exactly ONE home. Five tabs: **Development** = power + infrastructure + civic (power plants — per the user — plus roads/rail/spaceport and government/HQ/financial), **Agriculture** = agriculture, **Resources** = extraction only (mines/logging, no more power), **Industry** = heavyIndustry + chemicals + consumerGoods + vehicles (all 26 factories, with the existing sub-group divider headers), **Services** = clinic/school/retail/art/dataCenter. Verified live per tab. tsc/tests/lint all green.
- **Vic3-style construction + planet switcher + per-level nationalize/privatize (user-requested) DONE** (uncommitted working tree):
  - **Parallel construction** (`economyTick.ts` construction step): previously only the FRONT queued order progressed each tick. Now EVERY queued order advances simultaneously — the world's `CONSTRUCTION_CAPACITY` (400) is split evenly across all orders, each capped by its remaining cost and its owner's funds (shared treasury pool for gov/worker orders, per-corp cash for corporation orders); several can complete in one tick. Verified (test §28): 3 queued orders each advanced 133/tick (=400/3). This is the Vic3 "build multiple at once" behavior.
  - **Planet switcher in the dock header** (`ActionBar.tsx`): the bottom Buildings/Politics/Diplomacy dock now shows the scoped world name as a clickable button ("· Mars ▾"); clicking it drops down every world the player's nation owns (plus a "↺ Follow selection" item to return to focus-following). Picking one pins the panel to that world without hunting for the body in the 3D scene. Verified live: switched Mars↔Luna, panel content changed accordingly. (Politics/Diplomacy docks share the header so they get it too once those panels exist — currently "Not yet available".)
  - **+ on a private/corp row builds a SEPARATE state building — now clearly indicated**: the + already queued a state-owned build (the user worried it might upgrade the company's building — it does not; a state order only stacks onto a state building of that type, else creates a new one). Made it obvious: construction-queue rows now show an **owner tag** (e.g. "Coal Mine · State"), so clicking + on a private Redmines Coal Mine visibly queues a *State* Coal Mine. Verified live.
  - **Per-COUNT nationalize/privatize** (store actions `nationalizeBuildingLevels(worldId, buildingId, levels)` / `privatizeBuildingLevels(...)`, helpers `makeLevelBuilding`/`addOwnerLevels`/`removeLevels`/`makePrivateCorp`): the building detail popup has a level-count picker — a number input `[ N ] / L` plus an "All" button — before the Nationalize/Privatize button, so the player chooses how many of a building's levels to transfer (the user's request: "how many out of how many (or all)"). Nationalize moves N levels from a corp/co-op building into a state building of the same type (compensation = N × per-level × 60%/15%, bureaucracy 20+20N); Privatize moves N levels from a state building to the country's largest private company (floating a new one if none exists; proceeds = N × per-level × 70% to the treasury). Taking ALL levels flips the whole building's owner in place (preserving its throughput/identity) rather than rebuilding it; a partial transfer splits the levels into a separate building. Both routed through the confirm dialog with the count in the text. Verified live (picker renders, input max = level, confirm→action fires) and by headless test §29 (nationalize 1 → source 3→2, state +1; privatize 2 → source 5→3, private +2, treasury +$8,400). NOTE: the old singular `nationalizeBuildingLevel`/`privatizeBuildingLevel` were REPLACED by these count-based actions; `nationalizeBuilding` (whole-building owner-flip) is kept and still used by test §22.
  - Verified: tsc clean, **28 economy sections pass** (added §28 parallel construction), combat/scenarios/escape pass, lint unchanged (15). Note: HMR sometimes doesn't hot-swap store/ActionBar structural changes — a full reload was needed to see them live (a dev-only quirk, not a runtime bug; a stale `TAB_CATEGORIES is not defined` error lingered in the console buffer from a mid-edit HMR step but no such reference exists in the code and tsc is clean).
- **Financial-district + SoL fixes, then CONSTRUCTION REWORK Stage 1 (user-requested, in progress) DONE-so-far** (uncommitted working tree):
  - **Financial districts (improved, not fully solved):** removed the unstaffable `investor` jobs from `financialCenter` (they left it permanently understaffed) + boosted its output, and added **investment income** — a per-tick return (`FINANCIAL_RETURN_RATE = 0.045` in economyTick) on the market value of the stakes an FD holds in other companies. FDs with real portfolios (e.g. Mars) are now profitable; FDs on under-developed worlds still lose money because they have no private companies to invest in and their one building competes in a glutted retail market — a structural thing to revisit (the proper fix is the financial-sector/pool work in later construction stages).
  - **SoL (improved, deeper issue remains):** the country-AI governance manager now builds **essentials (food/healthcare/power) even when the treasury is thin** (necessities borrow; only discretionary growth waits for a cash buffer — `ESSENTIAL_NEEDS_THRESHOLD = 0.8`), for ANY economic system, backstopping corps abandoning low-margin essentials. Helps modestly; the sag is multi-factor (corps closing also cut pop INCOME, not just supply), so full SoL balance is a later pass. Command economies hold up best (state provides); laissez-faire naturally lower (intended).
  - **CONSTRUCTION REWORK — Stage 1 (Vic3-inspired, agreed design) DONE:** the user hated the flat "$6B, paid by the government" model. Now: a building level costs a fixed amount of **construction WORK (points)**, tier-based (`constructionWork()` in recipes.ts: T1 primary 100 → T5 spaceyard/rocket 900, via `CONSTRUCTION_TIER`/`CATEGORY_TIER_DEFAULT`). The **money cost is emergent** — each point of progress consumes a basket of materials (`CONSTRUCTION_GOODS_PER_POINT` in economyTick: concrete 2.0, steel 0.7, lumber 0.25, tools 0.15, glass 0.08) bought at **market price**, so a building "costs" whatever its materials cost that tick (no dollar figure; varies with prices). New good **`concrete`** + **`cementWorks`** building (coal+electricity→concrete, seeded one per country: Mars/Venus/Arcadia/Lalande). Construction capacity is `CONSTRUCTION_CAPACITY = 70` points/tick per world, split across the queue (parallel). Funding is per-owner: **government/state builds paid from the treasury, corporation builds from that company's own cash** — the government no longer "funds everyone" (the confusion was that the +button always builds STATE buildings). `BUILD_COST_PER_LEVEL` ($6000) is KEPT but only as the **asset value** for nationalization compensation / downgrade salvage / corp value — no longer the construction cost. New `estimateConstructionCost(recipeId, prices)` powers UI tooltips ("about $X of materials, built over time"). AI construction orders (country + corp) use `constructionWork()`. Verified: tsc clean, **30 economy sections** (added §30: work scales by tier, emergent cost scales, build completes over ticks, treasury pays vs a no-build control), combat/scenarios/escape pass, lint unchanged (15). Live: build tooltips show per-building material estimates (Iron Mine ≈$1.5B, varies by tier), header updated, no console errors. Balance notes: concrete floods to floor when not building (cement works self-throttle via throughput); Mars steel is pricey (pre-existing Mars industry imbalance, not construction). **Construction Stage 2 DONE:** added the **Construction Sector** building (`constructionSector` recipe: consumes machinery/tools/electricity + labor, produces no market good; `CONSTRUCTION_OUTPUT = { constructionSector: 55 }` points/level, like BUREAUCRACY_OUTPUT/LOGISTICS_OUTPUT). A world's build capacity is now `worldConstructionCapacity()` = `CONSTRUCTION_BASE_CAPACITY (15)` + Σ sectors' output × level × throughput — replacing the old fixed 70/tick. So **building more construction sectors builds everything on that world faster** (Stage 2 goal; tech-improvable later via `CONSTRUCTION_OUTPUT`). Exported `constructionCapacityOf(world)` for the UI; the Construction panel shows the capacity + a hint to build sectors. Seeded one per world (Mars L2, Venus/Arcadia/Lalande L1; Luna/Proxima none → base only). Industrial district capacity loosened (`/85 → /72`, min 16) to fit the new industry buildings; verified seed still fits all districts. Sectors are state-owned infrastructure (their upkeep = a treasury cost, the "labor of construction"); the bulk MATERIALS are still consumed per-point by the projects (Stage 1). Capacity is per-WORLD and shared — any owner's construction on a world draws on it, which implicitly implements the "government uses whatever construction firms exist" contractor idea; the explicit gov-pays-a-private-contractor money flow is deferred to Stage 3's investment pool. Verified: tsc clean, **31 economy sections** (added §31: sectors give more capacity than a sector-less world; base capacity keeps a sector-less world building slowly; a high-capacity world builds the same clinic in 3 ticks vs 17 for a sector-less one), combat/scenarios/escape pass, lint unchanged (15). Live: "+ Construction Sector" is buildable in the Development menu, no console errors.
**Construction Stage 3 DONE — investment pool + financial-sector fix:** added `Country.investmentPool` (seeded 40000/nation). Money flows: each tick every PRIVATE company contributes `POOL_CONTRIBUTION_RATE = 0.3` of its retained operating profit into its country's pool (deducted from the company's cash via `corpPoolContribution`); **private construction is now financed from that shared pool, not per-company cash** (tickWorld's corp-order funding param changed from a `corpCash` map to a single `investmentPool` number, threaded per-country like the treasury; returns `poolConstructionSpend`). So a profitable-but-cash-poor company can be financed to expand — the user's original concern, solved. The corp AI now gates on the pool (`runCorporationAI(..., investmentPool)` — "capital market dry" instead of "corp too poor"). **Financial sector fixed properly:** replaced the old FD portfolio-return hack with a **pool MANAGEMENT FEE** (`POOL_MANAGEMENT_FEE = 0.02` of the pool, split among a country's financial companies by value) — their real business is stewarding the capital market, so a bigger/healthier private economy → bigger pool → more fee income. Also slimmed the `financialCenter` recipe to a lean overhead (jobs 60/25, was 150/70; output retail 400) since FDs earn from the fee, not from running a shop. RESULT (10-yr probe): FDs went from bleeding ~−77k to **mostly solvent with positive cash** and near break-even/profitable (was the biggest open economy wound). SoL stable (~0.54). New good `concrete` confirmed live-traded. UI: Construction panel shows the treasury pool + the private investment pool. Verified: tsc clean, **32 economy sections** (added §32: pool finances a cash-poor company's build, it's the pool that pays vs a control, a dry pool can't finance it, the financial sector earns more on a bigger pool), combat/scenarios/escape pass, lint unchanged (15), no console errors live. **STILL TO ENRICH (deferred, additive):** the user wanted **banks / asset managers** as distinct financial company types feeding+managing the pool — the pool + FD-as-manager is the working foundation; banks/asset-managers layer on as more building/company kinds later. Also government **contractor money-flow** (paying private sector owners) can now build on the pool.
**Construction Stage 4 DONE — construction/budget UI + welfare rethink:** answered the user's "why is welfare per capita" and "specific budget controls." KEPT the per-capita welfare MECHANIC (it's the sound model — a per-person entitlement like a UBI/pension, so total spend scales fairly with population; a fixed-total pot would instead dilute as pop grows) but fixed the cryptic PRESENTATION: the Budget-tab control now reads "Welfare — <tier>" (None/Minimal/Basic/Generous/Comprehensive via `welfareTier()`) with the value shown as "$X/person", a coarser ±0.5 step (was ±0.02), and a hint explaining it's per-person (total = per-person × population) and pointing to the OTHER budget levers' locations (healthcare funding → Government→Laws; subsidies → Corporations / building detail). The Budget breakdown already listed all expenditure lines (welfare/services/admin/subsidies/construction/interest); added the **Private investment pool** to it. Construction panel: the funding selector is relabeled "Owner / funding" and spells out government→treasury vs a company→investment pool; the pool + construction capacity are shown (from Stages 2–3). Verified live: welfare reads "Welfare — Basic · $2.0M/person", investment pool line present, no console errors. tsc clean, 32 economy sections + combat/scenarios/escape pass, lint unchanged (15). NOTE: additional *adjustable* budget levers (defense, education spending) were NOT added — each needs a real gameplay effect to be worth a slider; the current adjustable set is tax + welfare (Budget) + healthcare law + subsidies, which is coherent. That's a candidate for a future pass if the user wants deeper fiscal policy.
**The five-stage construction rework (points → sectors → investment pool → UI/budget) is COMPLETE.**
- **FOREIGN INVESTMENT — v1 DONE** (uncommitted working tree): the first cross-border-capital channel — **state equity investment in foreign companies, with repatriated dividends, gated by a foreign-investment law.** New **`ForeignInvestmentPolicy`** law (open/approval/closed, mirrors the foreign-bond law) on every `Country` (`foreignInvestmentPolicy`, seeded), governing whether foreign capital may own equity in that nation's corporations. Extended `ShareHolder`'s state kind to `{ kind: 'state'; countryId? }` — absent/own-country = the home state, a different countryId = a FOREIGN government's stake. Dividend routing now sends a state stake's dividends to `holder.countryId ?? corp.countryId` → **foreign stakes' profits repatriate to the investing nation's treasury.** `derivedKind` rewritten to count only HOME-state shares (a foreign stake never flips a company to domestic-state-owned). New store actions `investAbroad(investorCountryId, corporationId, shares)` (buy/sell a foreign stake from the public float, treasury-funded, blocked if the host is `closed`) and `setForeignInvestmentPolicy`; `tradeShares` updated to touch only home-state holdings (foreign/financial/character stakes preserved). UI: the **Stock Exchange** now lists foreign companies with a "foreign" tag + host name, routes their trades through `investAbroad`, disables buying when the host is closed, and shows YOUR government's stake; the **foreign-investment law** is enactable in Government → Laws (confirm-dialog). Verified: tsc clean, **33 economy sections** (added §33: a state takes a stake in a foreign company; it doesn't flip it to home-state-owned; the stake's dividends repatriate to the investor's treasury; a closed host blocks it), combat/scenarios/escape pass, lint unchanged (15), no console errors live (dev-server HMR reload-loop blocked a clean screenshot of the SE foreign tags this session, but the mechanic is fully headless-tested). - **FOREIGN INVESTMENT — v2 DONE** (uncommitted working tree): built out the deferred channels. **Company/SOE foreign investment:** extended `ShareHolder` with `{ kind: 'corporation'; id }` (any company can hold equity in any other), and dividend routing sends such a stake's dividend to the holding company's cash (`case 'corporation'`) — so an SOE or private firm, at home or abroad, earns from equity it holds, repatriating cross-border. New store action `corpInvest(holdingCorpId, targetCorpId, shares)` (buy/sell from the holder's own cash, blocked cross-border if the host is `closed`). **AI foreign investment** (`foreignInvestmentAI.ts`, `runForeignInvestmentAI`, wired into tickEconomy's AI block, `sharePrice` passed in to avoid an import cycle): on a slow staggered cadence, a non-player STATE with a flush treasury and a cash-rich private COMPANY each take a small equity stake (`STAKE_BLOCK = 80`) in the most profitable FOREIGN private company whose host is open — proven live: after 120 ticks three foreign governments hold stakes in Redmines. **UI:** the Corporations panel shows a "Foreign-held X%" line when foreign governments/companies own a slice (and the home-state stake calc now correctly excludes foreign stakes); the Stock Exchange foreign tags verified live (Venus/Arcadia/Proxima/Lalande FDs tagged foreign from Mars). Verified: tsc clean, **34 economy sections** (added §34: a company holding equity gets the dividend in its cash; the AI forms cross-border state stakes over time), combat/scenarios/escape pass, lint unchanged (15), no console errors live.
- **FOREIGN INVESTMENT — v3 DONE (cross-border construction + approval queue + auto-approve toggle)** (uncommitted working tree):
  - **Cross-border construction financing:** restructured the investment pool from per-country `runningPool` threading to a SHARED, mutable `poolByCountry` map for the whole tick, plus a `corpCountry` resolver passed to `tickWorld`. Now a corporation's construction on ANY world draws from the OWNING company's HOME-country pool — so a foreign company building on a foreign world spends its own nation's capital (true FDI), and the finished building is foreign-owned (profit repatriates). Final pools are written onto countries AFTER the whole world loop so cross-border draws from later-processed nations are reflected. The corp AI can now FOUND buildings abroad on open-host worlds (`FOREIGN_BUILD_BIAS = 0.85` home-bias discount; gated by `openHosts` set), though in the default single-private-corp seed it usually prefers expanding a home winner. Verified by test §35 (a Venus company builds a factory on Mars financed from Venus's pool while Mars's pool is empty).
  - **Approval queue + auto-approve toggle:** new `Country.pendingForeignInvestment: ForeignInvestmentOffer[]` + `foreignInvestmentAutoApprove: boolean` (seeded false), and `ForeignInvestmentOffer` type. When a foreign entity would invest into a HUMAN host under the 'approval' law with auto-approve OFF, the foreign-investment AI QUEUES an offer for the player instead of executing (AI hosts, open hosts, and auto-approve hosts let it straight through). Shared `executeForeignBuy` helper (state or corp investor) in economyStore. New store actions `setForeignInvestmentAutoApprove`, `approveForeignInvestment(hostId, offerId)` (executes the queued buy + removes it), `rejectForeignInvestment` (discards it). UI: the Government → Laws foreign-investment section shows an "Auto-approve foreign investments" checkbox and a pending-offers list with Approve/× per offer (only when the law is 'approval'). Verified by test §36 (approve executes the buy + clears the queue; reject discards) and a probe (ISM as human 'approval'/no-auto host accumulated 3 pending offers from foreign governments wanting Redmines).
  - Verified: tsc clean, **36 economy sections** (added §35 cross-border construction, §36 approval flow), combat/scenarios/escape pass, lint unchanged (15), no console errors live. Foreign investment (state + SOE + company equity, repatriation, cross-border construction financing, the foreign-capital law + approval screening) is now a complete system.
**STILL DEFERRED (minor):** the corp AI rarely FOUNDS abroad in the default seed because expanding a home winner is preferred (the mechanism works and is tested; it just needs richer seeds / a margin-comparable expand-vs-found choice to trigger often); the offer 'price at approval time' can differ from offer time (executes at approval-time price — fine).

- **CENTRAL BANKING SYSTEM — STAGE 1 of 5 DONE (institution + laws + governance shell)** (uncommitted working tree): a big user-requested feature (full 14-section spec) to model central banks as real institutions, NOT a generic interest-rate modifier. USER DECISIONS on scope (via AskUserQuestion): **full multi-currency** FX (every country its own currency + real exchange rates, cross-border trade/investment repriced — the heavy option, Stage 3), and **distinct commercial-bank entities** with their own AI (not an aggregate — Stage 2). Prereqs that DON'T yet exist and must be built under the CB: currency/FX (nothing — all USD placeholder), commercial banks (nothing — FDs only earn a pool fee), inflation-as-a-DRIVER (currently a passive price-level readout in economyTick ~L892), base/broad money. So this is the game's monetary core, built in 5 stages (tests green each checkpoint, Context.md updated, NEVER committed).
  - **Stage 1 (this entry):** the political/legal shell only — no economic wiring yet. New pure module `src/economy/centralBank.ts`: the `CentralBank` institution (status, structure, policyAuthority, appointment, mandate, debtFinancing, exchangeRegime, credibility, governmentPressure, governorName + term, policyRate, reserveRequirement) + **seven law families** each with full defs (`CentralBankStatus` no-bank/treasury-office/state-bank/independent/highly-independent; `BankStructure` single/regional-branches/federal-reserve/decentralized; `PolicyAuthority` government/finance-ministry/governor/board/mpc; `GovernorAppointment` government/head-of-state/legislature/joint/fixed-term/staggered; `CentralBankMandate` currency/price/employment/financial/exchange/multiple/development w/ MandateWeights read by Stage 4; `DebtFinancingRegime` prohibited→direct w/ monetaryFinancingCap; `ExchangeRateRegime` float→peg w/ defenseIntensity, wired Stage 3). Governance functions: `hasCentralBank` (plain boolean, NOT a type predicate — a no-bank record is still a CentralBank object, so `cb is CentralBank` would mislead the narrower), `centralBankModelLabel` (derives the archetype from status+structure), `effectiveIndependence` (status base + appointment mod, capped 0.95 so pressure always has headroom, − governmentPressure*0.4), **`governmentControlsPolicy`** (THE gate: true when policyAuthority is government/finance-ministry OR effectiveIndependence<0.25), `monetaryFinancingCap`, `defaultCentralBank`.
  - `Country.centralBank?: CentralBank` (economyTypes.ts, optional so nothing predating it breaks; seeded for all 4). economySeed: `seedCentralBank` helper + 4 distinct institutions — Mars=state-bank/regional-branches/multiple mandate/managed FX; Venus=highly-independent/federal-reserve/mpc/price/float (the showcase independent bank); Orion=independent/single/board/currency/prohibited-financing; Lalande=treasury-office/finance-ministry/development/**direct** monetary financing/fixed FX (the govt-directed development bank).
  - economyStore: 13 actions — `establishCentralBank`, `setCentralBankStatus/BankStructure/PolicyAuthority/GovernorAppointment/CentralBankMandate/DebtFinancingRegime/ExchangeRateRegime`, `setPolicyRate`/`setReserveRequirement` (GATED — no-op on an independent bank via `governmentControlsPolicy`; the whole point), `pressureCentralBank` (raises pressure, −credibility), `appointGovernor`. Helper `mapCentralBank`.
  - UI: new `CentralBankPanel.tsx` mounted at **Government → Institutions** (NavBar renderContent). Model label, independence/credibility/pressure meters, governor/mandate/authority/control facts, policy-rate + reserve-requirement sliders (disabled unless govt-controlled), a "Pressure the bank" button (shown only when independent, with confirm), and all 7 law families as enact-lists (reused `.laws-option` styles + new `.cb-*` styles in App.css). Abolishing (status→no-bank) confirms.
  - Verified: tsc clean, **37 economy sections** (added §37: seeded models differ, independent>treasury independence, the governance gate — govt can set Lalande's rate but NOT Venus's, pressure lowers credibility+independence, appointment-law change resets term, abolish/re-establish, CB survives 6 ticks finite), combat/scenarios/escape pass, lint unchanged (15, CB files add 0). Live-verified BOTH gate states: Venus (independent) = disabled sliders + pressure button + working confirm; Lalande (govt-directed) = enabled sliders, no pressure button, "set policy directly". No console errors.
  - **Stage 2 DONE (commercial banks + money supply):** distinct `Bank` ENTITIES with real balance sheets, per-country monetary aggregates, and the reserve-requirement credit gate — Stage 1's levers (reserveRequirement, policyRate) now BITE. New pure module `src/economy/banking.ts`: `Bank` = { reserves, loans, securities (assets); deposits, cbBorrowings (liabilities); riskAppetite, lastProfit, stressStreak }; capital DERIVED = reserves+loans+securities−deposits−cbBorrowings (`bankCapital`/`capitalRatio`). `tickBank` per-tick order: earn net interest margin (income = lendingRate·loans + 0.9·policy·securities; expense = depositRate·deposits + discountRate·cbBorrowings; retained as reserves), amortize loans 3% (money destruction: loans & deposits both fall), deploy EXCESS reserves into new loans (creates matching deposits = broad-money creation; gated by capital target 8%), then reconcile reserves vs. requirement via the discount window (short → borrow a fraction of shortfall from CB; flush → repay). `tickBanking` steps all banks under their country's CB policy, reconciles each CB's `loansToBanks` = Σ cbBorrowings, and emits `monetaryAggregatesFor` per country (baseMoney=currency+reserves, broadMoney=currency+deposits, loans, capital, reserveRatio, loanToDeposit, cbBorrowings). Rates: lendingRate=policy+3%, depositRate=0.5·policy, discountRate=policy+1%. `CentralBank` extended with balance-sheet fields fxReserves/govSecurities/currencyInCirculation/loansToBanks + `centralBankEquity(cb, bankReserves)`. economyTypes: `Bank`, `MonetaryAggregates`, `EconomyState.banks`, `TickReports.money`. Seed: `seedBanks()` — 6 banks (Mars×2, Venus×2, Orion×1, Lalande×1) each ~12.5% capital ratio; Lalande CB holds more FX reserves (defends its fixed rate). tickEconomy signature now `(countries, worlds, corporations, ai, banks)` → also returns `banks` + `reports.money`; banking runs LAST on settled countries (sees this tick's policy). economyStore: `banks` + `moneyReports` state, seeded + threaded through `advance`. UI: **Economy → Banking** = new `BanksPanel.tsx` (per-bank two-column asset/liability sheet, capital ratio, profit, stressed-highlight when <8%; system totals), and a **Balance sheet & money** section added to `CentralBankPanel.tsx` (M0/M2, currency, deposits, reserves, loans, reserve ratio vs. requirement, + the CB balance sheet). NavBar: 'Banking' subcategory. New `.bank-*` CSS. SELF-CONTAINED for Stage 2: the banking system ticks and responds to monetary policy but does NOT yet feed pop investment/inflation/employment (Stage 4). DEFERRED note: retained profit accumulates as reserves so bank capital/reserves grow unbounded over very long horizons (finite & fine for any real game length; Stage 4 adds demand/dividends). Verified: tsc clean, **38 economy sections** (added §38: banks solvent + ≥8% capital, aggregate identities M0/M2, **RR 10%→25% shrinks broad money 147k→108k**, a reserve-short bank draws the discount window + CB loansToBanks mirrors it, higher policy rate widens margins/profit, CB equity finite, full tickEconomy wires banking + stays finite 12 ticks), combat (isolated)/scenarios/escape pass, lint unchanged (15, banking files add 0). Live: Banking panel renders real balance sheets (Aphrodite Savings/Cytherean Trust, 12.5% cap), CB money section shows correct pre-tick empty state, no console errors (couldn't cross a 30-day economy-tick live due to the paused/HMR-flaky clock — post-tick numbers are covered by §38).
  - **Stage 3 DONE (full multi-currency + FX):** every country issues its own currency with a floating/pegged exchange rate vs. a common numeraire, the **Terra Standard Credit (TSC)**. KEY DESIGN: currencies are a THIN layer at cross-border boundaries — domestic economies are UNCHANGED (every treasury/price/wage/cash is just reinterpreted as denominated in the local currency), so no domestic test moved. Only value CROSSING a border converts. New pure module `src/economy/fx.ts`: `Currency` = { name, code, rate (TSC value of 1 unit — higher = stronger), target (peg level) }; `convert(amount, fromRate, toRate)` = amount·fromRate/toRate; `convertBetween(amount, fromCountryId, toCountryId, countries)` (same-country = 1:1 passthrough); `depreciationBias(country)` (deterministic fundamentals: low credibility + govt pressure + monetary-financing regime → depreciate; higher policy rate → appreciate — stands in for inflation-as-driver until Stage 4); `updateExchangeRates(countries)` — floats drift toward fundamentals; defended regimes (managed/band/fixed/peg) steer toward `target` spending `fxReserves` (cost = gap·DEFEND_COST·defenseIntensity, so a hard peg spends more than a managed float), and a peg whose reserves are EXHAUSTED breaks: partial defense + devaluation + a credibility hit (PEG_BREAK_CRED_HIT). `Country.currency?` added (economyTypes; optional, seeded for all 4). Seeds: Mars MSV 1.00 (managed float), Venus VNC 1.15 (free float, appreciates), Orion ORM 0.95 (float, appreciates), Lalande LLD 0.70 (FIXED peg, direct financing + low credibility → burns reserves then breaks). **Cross-border repricing wired at every boundary:** distributeDividends converts a FOREIGN state/corporation stake's dividend from the paying corp's currency into the owner's (repatriation FX); tickWorld's cross-border construction converts the host-priced per-point cost into the financing country's currency (new `fxRate` map param); store `executeForeignBuy`/`investAbroad`/`corpInvest` convert the host share price into the investor's currency; `foreignInvestmentAI` converts stake cost. tickEconomy runs `updateExchangeRates` LAST (after banking, sees this tick's credibility/reserves). UI: **Currency & exchange rate** section on `CentralBankPanel.tsx` (currency name/code, rate in TSC, regime, peg target + held/−% status, FX reserves). Verified: tsc clean, **39 economy sections** (added §39: convert arithmetic, depreciation bias signs, strong currency appreciates / weak depreciates over 30 ticks, **peg defense drains FX to 0 then breaks — cred 0.35→0.00, LLD devalues 0.70→0.39**, cross-border dividend repatriates at rate, foreign equity costs more in a weak currency, rates/reserves finite through full ticks), ALL existing cross-border tests §33-36 STILL PASS unchanged, combat/scenarios/escape pass, lint unchanged (15, fx files add 0). Live: Currency section renders (Lalande LLD 0.700 TSC, Fixed, peg held, $45B FX reserves), no console errors. Probe over 40 real ticks showed the full emergent arc: VNC 1.15→1.24 & ORM 0.95→1.06 appreciate, Mars managed float drains reserves then slides 1.0→0.74, Lalande defends the peg ~25 ticks then breaks (cred→0, LLD→0.53). NOTE: foreign BONDS are left in issuer-currency (the abstract foreign holder bears the FX risk; not a distinct country) — intentional, not converted. Display still uses formatMoney ($ + USD_PER_UNIT) for domestic amounts (interpreted as local currency); currency codes surface in the FX section. **Imported inflation** (depreciation → import prices → inflation) is COMPUTED-ready but fully wired in Stage 4.
  - **Stage 4 DONE (transmission with lags + inflation-as-a-driver + reaction + financing + OMO):** the levers finally reach the real economy, WITH LAGS. New pure module `src/economy/monetaryPolicy.ts`: `MonetaryState` on `Country.monetary?` = { inflation, expectation, outputGap, neutralRate, lastBroadMoney, lastRate }. `tickMonetary(countries, money, reports)` per tick: (1) demand impulse = (neutral − realRate)·DEMAND_SENS + moneyGrowth·MONEY_SENS → a LAGGED output gap (GAP_SPEED); (2) inflation pressures = demand-pull (gap) + imported (currency depreciation vs lastRate) + monetary-financing (deficit·cap/GDP), inflation LAGS toward expectation+pressures (INFL_SPEED); (3) expectations drift toward actual scaled by (1−credibility) — credibility ANCHORS; (4) credibility drifts toward a level set by distance from the 2% norm; (5) **bank reaction = a mandate-weighted Taylor rule** (`TAYLOR_INFL·w.inflation·inflGap − TAYLOR_GAP·w.employment·gap`) applied ONLY when `!governmentControlsPolicy(cb)` (an independent bank moves its own rate; a government-run bank keeps the rate the government set → runs hot); (6) credit-conditions feedback: output gap nudges `investmentPool`; (7) **monetary financing** (debtFinancing `supported`/`direct`, via `monetaryFinancingCap`) credits the treasury + expands `currencyInCirculation`, at an inflation cost. It PATCHES `reports.countries[id]` — `inflation` is now the MODELED value (no longer the raw price ratio) plus new readout fields policyRate/realRate/inflationExpectation/outputGap/monetaryFinanced (added to `CountryFiscal`, all optional). Runs LAST in tickEconomy (after banking+FX). **OMO** (open-market operations): new store action `openMarketOperation(countryId, amount)` — buy (+) injects bank reserves & grows CB `govSecurities` (creates base money, loosens), sell (−) drains (capped by securities held + bank reserves); gated by `debtFinancingRegimeDef(regime).secondaryMarket`. UI: CB panel Monetary-policy section now shows the live readout (inflation vs 2% target, expectations, real rate, output gap, money-financed deficit) + **Buy/Sell OMO buttons** (when govt-controlled & secondary-market allowed); hint text updated (transmission is live now). NOTE the coupling is one-directional into FX-safety: `updateExchangeRates` (Stage 3) is UNCHANGED so §39 is untouched; inflation→credibility→FX happens only in `tickMonetary` (full tickEconomy). Verified: tsc clean, **40 economy sections** (added §40: inflation lag on tick 1, loose>tight inflation, independent bank hikes / govt-bank holds, direct-financing credits treasury + adds inflation, depreciation imports inflation, OMO buy injects reserves + grows securities, OMO refused under a no-secondary-market regime), combat/scenarios/escape pass, lint unchanged (15, new files add 0). Live: panel renders monetary readout + OMO buttons, OMO click works with no console errors. Probe over 30 ticks showed the full arc: independent Mars bank chases rising inflation (rate 3.4%→9%, inflation peaks ~5.4% then falls — stabilizing with lags); forced-tight Mars dips toward deflation then recovers; **Lalande (govt-controlled + direct financing) pinned at 2% while inflation spirals 2%→23%** — the intended tradeoff. DEFERRED to Stage 5: LOLR/banking-crisis policy (the discount window auto-lends but there's no crisis/insolvency logic yet); employment feedback is a proxy via the output gap (labor sim untouched); no AI auto-OMO yet.
  - **CURRENCY RENAMES (user):** Mars = Imperial Standard Credit (ISC), Venus = Venusian National Credit (VNC), Orion = Orion Republic Dollar (ORD), Lalande = Lalande Royal Dinar (LRD). Rates/targets unchanged (ISC 1.0, VNC 1.15, ORD 0.95, LRD 0.70).
  - **Stage 5 DONE (AI monetary manager + banking crises/LOLR + events + regional readouts) — CENTRAL BANKING SYSTEM COMPLETE.** (a) **AI monetary manager** (`monetaryPolicy.ts` now takes `tick` + `humanCountryIds`): an INDEPENDENT bank moves its own rate for everyone (Taylor rule, already), but a GOVERNMENT-controlled bank in a NON-PLAYER nation is now steered by an AI rule (development-minded govts keep rates low − `0.02·w.development` but still lean against a runaway; price/currency-minded react harder), while the PLAYER's government bank stays where the human set it. (b) **Banking crises + LOLR** (`banking.ts`): emergent loan losses in a deep downturn (previous-tick `outputGap < −10%` writes off loans → capital falls → the crisis TRIGGER, no artificial shock needed); an INSOLVENT bank (capital<0) is recapitalized by the state up to 8% capital, funded from the home treasury (a real fiscal cost, partial if the treasury can't cover it) — the lender-of-last-resort backstop — logging a `bank-recapitalized` event. (c) **Political/crisis events** (`CentralBankEvent` + `TickReports.events`, rolling `centralBankEvents` log in the store, capped 60): governor term expiry → auto-appointment (from a name pool) + event; inflation-crisis (>15%, throttled on crossing); currency-crisis / peg-broken (>6% one-tick drop; peg-broken when a non-float regime is out of FX reserves). (d) **Federal/regional readouts**: for a federal-reserve/decentralized structure the CB panel lists each world (regional reserve bank) with population + employment — READOUT only, national policy stays unified (per the spec's "don't overcomplicate"). UI: CB panel gained a **recent-events strip** (red for crises), the **Regional reserve banks** section, and Stage 4's monetary readout/OMO. Store: `centralBankEvents` threaded through `advance`. Verified: tsc clean, **41 economy sections** (added §41: AI steers a non-player govt bank's rate up vs inflation; deep downturn inflicts loan losses; LOLR recapitalizes an insolvent bank + debits treasury + logs event; governor replaced on term expiry + event; events accumulate over a run), combat/scenarios/escape pass, lint unchanged (15). 120-tick stability probe: **0 non-finite values**, events fire (inflation crisis, governor rotations), independent economies healthy (~2.4% inflation, VNC/ORD appreciate, credibility ~0.88), and **Lalande spirals into bounded HYPERINFLATION (~127%, currency collapse, credibility 0)** — the intended cautionary tale of a government-run, money-printing, development-mandate bank; NO contagion to others. Live: panel renders regional readout + renamed currencies; a stale Vite HMR cache briefly 500'd on a mid-edit duplicate import (fixed; a dev-server restart cleared it; disk/tsc always clean). DEFERRED-BUT-MINOR: no explicit discount-facility SETTINGS law (normal/emergency-only) — the window auto-lends + LOLR recap covers the behavior; employment is a proxy via the output gap (labor sim untouched); no AI auto-OMO (the rate reaction suffices); events surface in the CB panel, not yet the Situations tab.
  - **CB STAGE ROADMAP:** ~~Stage 2 = distinct commercial-bank ENTITIES~~ (DONE) · ~~Stage 3 = multi-currency + FX~~ (DONE) · ~~Stage 4 = transmission + inflation + OMO~~ (DONE) · ~~Stage 5 = AI manager + crises + events + regional~~ (DONE). **THE FULL CENTRAL BANKING SYSTEM IS COMPLETE** (all 14 spec sections): institution + 7 law families, distinct commercial banks + money supply, full multi-currency FX with peg defense/breaks, transmission-with-lags + multi-source inflation + reaction functions + OMO + monetary financing, and AI managers + banking crises/LOLR + events + federal readouts. Files: `centralBank.ts`, `banking.ts`, `fx.ts`, `monetaryPolicy.ts` (all pure), UI `CentralBankPanel.tsx` + `BanksPanel.tsx`. Tests §37-41. (own balance sheets: capital/reserves/deposits/loans/securities/CB-borrowings) + base/broad money + reserve-requirement credit gating + CB balance sheet + bank AI. Stage 3 = **multi-currency + FX** (per-country currency, exchange rates, CB FX reserves, float/peg/band/fixed defense, REPRICE existing trade + foreign-investment — careful, keep those tests green). Stage 4 = policy transmission WITH LAGS (rate→credit→investment/consumption→demand→inflation & employment) + inflation-as-driver (multi-source: demand/supply-shock/money-growth/deficit) + OMO + discount window/LOLR + debt-financing regimes wired. Stage 5 = AI monetary manager (non-player CBs per mandate & independence) + bank-AI depth + crises + events (governor appointment, govt pressure, inflation/currency/banking crisis) + regional/federal readouts + final polish. The law fields (mandate weights, financing caps, exchange-regime defense) are already DATA in centralBank.ts, just not yet READ downstream.
Remaining agreed big milestones, not started: the rest of **foreign investment** (company/building channels above) (state/SOE/company cross-border capital + a foreign-capital law extending the foreign-bond law; build on the investment pool + ownership model), then **special economic zones**. Also deferred/additive: richer financial sector (banks/asset-managers as distinct company kinds), government contractor money-flow, character-share dividends, deeper SoL balance, the pre-existing Mars steel-scarcity imbalance.
**NEXT STAGES (agreed roadmap):** ~~Stage 3 = investment pool~~ (DONE) (private profits → shared pool, managed by a richer **financial sector**: banks/asset managers/FDs, framed as capital markets — "the pool finances, a company owns", fixing FDs properly). Stage 4 = construction/budget UI + **budget-controls / welfare-per-capita rethink**. Then major milestones: **foreign investment** (state/SOE/company cross-border capital + a foreign-capital law extending the foreign-bond law) and **special economic zones**. Also flagged for construction goods: the user wants more (concrete added; glass exists).
- **American-English spelling pass (user-requested) DONE** (uncommitted working tree): swept all `.ts`/`.tsx`/`.css` in `src/` + `tests/` from UK to US spelling — identifiers, UI strings, and comments together so everything stays consistent. Verb/-isation forms (nationalise→nationalize, privatise→privatize, subsidise→subsidize, utilisation→utilization, specialise→specialize) including the three renamed STORE ACTIONS + all their call sites: `nationalizeCorporation`, `nationalizeBuilding`, `privatizeCorporation` (interface + impl in economyStore + UI in BuildingsPanel/CorporationsPanel); plus behaviour→behavior, colour→color, centre→center (and centred/centres/recentre), cancelled→canceled, travelling/travelled→traveling/traveled, labelled→labeled, neighbour→neighbor, manoeuvre→maneuver, defenceless→defenseless, metres→meters, grey→gray. Left already-American words untouched (fulfill, specialist, organism). Verified: tsc clean, all 27 economy sections + combat/scenarios/escape pass, lint unchanged (15). Convention going forward: **all game text and identifiers are US English.**
  - **HONEST TRADEOFFS / still-open (flagged to user, their balance-planning domain)**: (1) **empire SoL falls under full AI** (~0.58 vs ~0.80 no-AI): partly the higher raw prices, partly the exploitative corps CLOSING low-margin essential buildings (food/healthcare) — an intended consequence, and note command economies (Lalande ~0.77, state AI rebuilds essentials) hold up far better than laissez-faire (Venus/Arcadia sag), which is the correct emergent dynamic; the player nation isn't AI-managed so it needs the human to respond. How low SoL should be allowed to go / how hard the state should counter is balance-tuning. (2) **Financial districts are still deeply negative** (−100k to −200k over 10 yrs): the dividend flow helps but is insufficient because their `financialCenter` building runs at a loss and their 15% stakes yield little — FDs need dedicated work (make financialCenter profitable, or give FDs larger institutional-investment income, or stop them operating a costly building). (3) Character-share dividends unbuilt.
- 4 countries: Imperial State of Mars, Republic of Venus, Orion Republic, **Kingdom of Lalande** (alien). 6 inhabited worlds: Mars+Luna (ISM), Venus (RoV), Arcadia+Proxima b (Orion), Lalande 21185 d (Lalande). **Earth is an uninhabited relict** (no economy).
- **4-axis pops**: species + culture + religion + class. Species: baseline Humans + **Tidalians** (alien needs template). Populations stored in **millions**, displayed as B/M (e.g. "4.00B").
- Working live: national fiscal (revenue/expenditure/debt/credit rating), CPI/inflation, finance graphs (inline SVG), Budget tax/welfare controls, **construction** (queue buildings, funded from national treasury, ramps to completion), **politics readout** (interest groups from pops), per-planet tabbed inspector (Overview/Buildings/Economy/Politics), context-aware panels (follow the focused world).
- **All tests pass** (`tests/economy.test.ts` + combat/scenarios/escape via `npx tsx`), `tsc -b --noEmit` clean, lint clean.
- **Design doc**: `docs/economy-design-v2.md` (the merged plan — read it; sections 10 = milestone build order, 11 = setting content decided).

## Decisions (Economy/Politics track)
- Keep pops + supply/demand (micro), with a NATIONAL government layer on top (macro) — hybrid, Vic3-style, not pure macro/TNO. Optimize later.
- Trade model chosen: **connected markets with real physical transport** (freighters/logistics/range) — designed, not yet built.
- Sim scope: simulate all countries + all inhabited worlds at full detail (LOD designed but unneeded at this scale).
- Execution: clean restructure (done for M1).
- Religion mixes per world set by judgment (Mars: Imperial Church majority; Venus: Storm Cult plurality/Axiomatic/Silicon Dream; Arcadia: Arcadian Idyll/Old-Earth Theravada; Lalande: Tidal Communion).
- **Language**: stay TypeScript for now (performance is not the bottleneck at this scale). Real insurance = keep the sim as pure, isolated, headless-tested functions (already the case) so a Rust→WASM port later is a contained swap. Consider Rust→WASM only on a profiled wall or a native "real game" pivot. LOD is the biggest language-agnostic perf lever.

## Constraints (Economy/Politics track)
- Runs in the browser; game clock is rAF-driven and throttles when the browser pane is hidden (affects live testing — front the pane to advance time).
- Session state (playerStore, economyStore) is NOT persisted — reloading returns to the nation-select menu. Vite HMR sometimes triggers a reload mid-edit, resetting the session.
- `read_console_messages` in the browser tool retains a STALE buffer (old timestamps/exports like seedEconomy/estimateGdp/planetEconomy) after edits — visual/functional evidence is the reliable signal, not that buffer.
- Economy tick cadence: 1 tick per 7 sim-days (weekly). Population unit = 1 million people; recipes scaled ×100 to match.

## Important Details (Economy/Politics track)
- Economy code: `src/economy/` (economyTypes [+Building.owner/Corporation/Character/Family/Pop.standardOfLiving], economyTick [tickWorld + tickEconomy(countries,worlds,corporations) + corporationValue/sharePrice + JOB_SCALE/INVENTORY_DECAY/SoL consts], economySeed [seedWorlds/seedCountries/seedCorporations/seedCharacters/seedFamilies], goods [23 goods incl electricity], recipes [~24 buildings, ProductionMethods, CLASS_QUALIFICATION], species [needs baskets incl electricity/comfort/luxury], demographics, politics, laws, format [formatMoney×USD_PER_UNIT]). Store: `src/state/economyStore.ts` (adds corporations/characters/families + createCorporation/nationalise/privatise/tradeShares/characterAction/setBuildingOwner). UI: `BuildingsPanel` (Owner column), `EconomyPanel`, `PoliticsPanel`, `LineGraph`, `InspectPanel` (Pops tab), `PopsPanel`, `LawsPanel`, `CorporationsPanel`, `StockExchangePanel`, `DemographicsPanel`, `CharactersPanel`, `NavBar`.
- Ownership lives on `planetData.ts` (ownerId) AND on the economy World; countries in `src/data/countryData.ts`.
- Sim is pure functions (no React/DOM/Three deps) — the portability discipline to preserve.
- **Git**: earlier work (multi-star/map-mode/UI, economy M1) IS committed (see `git log`: "Part 1 of Economies and Society", "Basics of Internal Mechanisms", etc.). **Milestone 2 + the autonomy/economic-system feature are uncommitted on `main`** (working tree otherwise clean). User has not yet asked to commit — offer before large context loss.

# Project Structure:
- `src/App.tsx` — app shell: mounts the global clock (`useGameClock`), renders the HUD bars (breadcrumb nav top, time controls bottom), and switches the active scene component based on `viewStore.level`.
- `src/App.css` — all HUD styling (bars, breadcrumb, time controls, markers, info panels, draggable window, galactic placeholder).
- `src/index.css` — minimal full-viewport dark reset.
- `src/state/gameTimeStore.ts` — zustand store for the simulation clock: `simDays` (elapsed since Jan 1, `START_YEAR` = 2600), `paused`, `speedIndex`, and `mode: 'normal'|'tactical'` — two *rates* on one clock, not two clocks (see Design Decisions), at `NORMAL_DAYS_PER_SECOND` (6) vs `TACTICAL_DAYS_PER_SECOND` (one sim-second per real second). Speed tiers differ per mode (`speedMultipliersFor`), and `setMode` clamps `speedIndex` into the new mode's range without touching `paused`. Helpers `simDaysToDate`/`simDaysToYears` convert for display and orbit math; `simSecondsToDays`/`simDaysToSeconds` are the single conversion point for combat, which is authored in seconds but stored as absolute `simDays` deadlines like everything else; `formatClockTime` renders the wall clock shown in tactical mode.
- `src/hooks/useGameClock.ts` — drives `gameTimeStore.tick()` off a `requestAnimationFrame` loop mounted once in `App.tsx`, independent of any `<Canvas>` — so the clock survives view-level Canvas remounts.
- `src/data/combatData.ts` — the combat vocabulary: `DamageType` (energy/kinetic/missile/torpedo) and the `DAMAGE_PROFILES` multiplier matrix against each defense layer; `ComponentKind` (weapons/utility/core) and `OVERALL_WEIGHTS` for the blended bar; `HullSizeClass` (small/medium/large/x — a hull's rough physical size, reused verbatim as the ship builder's slot-size vocabulary, see below); `WeaponMount`/`DefenseProfile`/`CombatProfile` shapes (`CombatProfile` now carries `sizeClass`; `DefenseProfile` now carries `flakRating` and `evasion`, both `0` on every hand-authored hull); the `WEAPON_TYPES` archetype catalog (`missileBattery`/`torpedoTube` now carry `optimalRangeUnits`); per-hull profiles (`CIVILIAN_`/`CORVETTE_`/`FRIGATE_`/`DESTROYER_`/`CRUISER_`/`BATTLESHIP_PROFILE`); FTL charge durations; the `weaponsEffectiveness`/`utilityEffectiveness` degradation curves; and (Phase 15) `rangeEffectiveness`/`missileDamageMultiplier`/`torpedoAccuracy`, the shared linear-taper math behind missile damage falloff and torpedo accuracy. `hullMotion()` is now exported (was module-private) so `hullChassis.ts` can build custom chassis on the exact same speed-of-light-relative math every preset hull uses. Kept separate from `shipData.ts` (hulls and drives) so "what can this ship shoot with" has one home. Every number is a picked balance value, not derived from anything physical — grouped as named exports so a future tech tree has one place to scale from.
- `src/data/shipModules.ts` (Phase 15) — the ship builder's equippable module catalog: `SlotCategory` (weapon/armor/shield/defense/upgrade) and `SlotSize` (= `HullSizeClass`); `WeaponModule`/`ArmorModule`/`ShieldModule`/`DefenseModule`/`UpgradeModule` shapes and their five catalog constants (`WEAPON_MODULES` wraps the existing `WEAPON_TYPES` archetypes plus one new X-slot `Titan Beam`; `ARMOR_MODULES`/`SHIELD_MODULES`/`DEFENSE_MODULES`/`UPGRADE_MODULES` are new content, one module per slot size); `moduleFitsSlot` (a module fits any slot at least as big as it is — down-fit only, never up-fit) and `modulesForSlot` (what the builder UI's per-slot picker offers, generic over category so it returns the concrete module type rather than the shared base shape). Deliberately separate from `combatData.ts` for the same reason `WEAPON_TYPES` already is — "what can be fitted" is its own vocabulary.
- `src/data/hullChassis.ts` (Phase 15) — the ship builder's hull chassis catalog: `HullChassis` (size class, fixed component/speed baseline, FTL drives, and a `slots: Record<SlotCategory, SlotSize[]>` budget — metadata only, never a `CombatProfile`) and `HULL_CHASSES`, six chassis mirroring the five warship presets' identities (plus Civilian) by name/size, each a **separate array from `SHIP_CLASSES`** with its own slot budget. Also `ShipDesign` (a saved loadout: chassis id + `equipped` module ids per slot, positionally aligned to the chassis's own slot list) and `emptyLoadout`; `buildCombatProfile(chassis, design)` — the load-bearing pure function that sums a design's equipped modules into a standard `CombatProfile`, capped stacking (point defense/flak/evasion each have a hard ceiling so filling every slot with the same module can't buy a sure thing); and `designToShipClass` wrapping a built profile into the same `ShipClass` shape every preset already is (id namespaced `design:<id>`), which is what lets every existing combat/UI function treat a custom design identically with zero changes to any of them.
- `src/scene/combatArena.ts` — the arena's coordinate system and its pathfinding lattice, kept deliberately separate (see Phase 4 in Design Decisions for why the two used to be conflated, and the bug that caused). `ArenaPoint` (`{x,y,z}` in real, continuous arena units) is the type actual game state is stored in — ship/obstacle/window-centre positions — and is density-independent by construction. `GridNode` (a lattice index) exists only as an ephemeral planning aid, converted to/from `ArenaPoint` via `nodeToArenaPosition`/`arenaPositionToNode` at the moment a route needs computing. Also holds `GridDensity` (subdivision count only — physical `ARENA_SPAN_UNITS` is constant, so weapon ranges mean the same thing at every setting), `isInsideWindow` (density-free — the window's *physical* size never changes), the `CombatObstacle` shape (`position: ArenaPoint`) plus `isPointBlocked`/`segmentIntersectsSphere`/`hasLineOfFire`/`segmentClearsObstacles`, 26-way `neighborsOf`, and `latticePath` — greedy in open space, A* bounded by `boundsFor(from, to, margin)` when a body actually clips the straight route. Pure/stateless throughout.
- `src/scene/combatResolution.ts` — the combat simulation, pure functions over `(engagements, ships, simDays, rng)` with no store access, mirroring `shipPhysics.planMove`'s "physics computes, caller applies" split. Holds `applyShot` (the three-layer damage walk with raw-carryover overflow), `pickComponent`, `overallHealthFraction`, `stepEngagements` (one fixed timestep: approach → movement → shield regen → firing → escapes), `syncEngagements` (roster reconciliation against where ships actually are), `approachNode`/`orderParticipantTo`, and `planFtlCharge`/`ftlChargeSeconds`. The RNG is injected so tests can seed it — this is the primary verification path for combat.
- `src/state/combatStore.ts` — live engagements: `Engagement` (location key, density, participants, `resolvedThroughSimDays`) and `CombatParticipant` (lattice node, in-flight hop, queued path, per-mount weapon timers, chosen target + target component). Also `areHostile`/`sideFor`/`combatLocationKey`, and `autoTacticalOnEngage` (whether a fight yanks the clock into tactical). Deliberately holds only what's transient to a fight — persistent damage lives on `ShipInstance.combat`.
- `src/state/shipDesignStore.ts` (Phase 15) — zustand store for player-built ship designs, same dependency-free pattern as `fleetStore.ts`: `designs: ShipDesign[]` plus `createDesign`/`renameDesign`/`deleteDesign`/`equipModule` (a `moduleId` of `null` clears a slot). Ids follow `shipStore.ts`'s own fleet-id pattern (`design-${Date.now()}-${random}`). In-memory only — no persistence, a page reload loses custom designs (same as every other store in this project; nothing here has storage yet).
- `src/state/shipClassResolver.ts` (Phase 15) — `resolveShipClass(classId)`, the one place a `classId` becomes an actual `ShipClass`: checks `SHIP_CLASSES` first (the common case, same cost as the old direct lookup), then falls back to resolving a `design:<id>` classId against `shipDesignStore` + `hullChassis.designToShipClass`. Every call site that used to do `SHIP_CLASSES.find((c) => c.id === classId)` directly (`combatResolution.ts`, `shipPhysics.ts`, `ShipPanel.tsx`, `DebugConsole.tsx`, `FleetManagement.tsx`, `useEscapeBehavior.ts`) now goes through this instead — the single change that makes a custom design visible everywhere a preset already was, without any of those call sites needing to know the design store exists.
- `src/data/techData.ts` (Technology Phase 1) — the tech tree's data model: `TechCategory` (physics/society/engineering, three independent research pools) and `TechNode` (id/name/category/description/`cost`/`prerequisites: string[][]` — an "OR of ANDs," so a plain single-parent node is `[['parentId']]`, a convergent node reachable from either of two branches is `[['a'], ['b']]`, and a node needing both together is one set with two entries — plus an optional `locked` flag, Anomalous only). `PHYSICS_TECHS` is the real, fully-populated tree (30 nodes: 8 open root branches — Classical Mechanics, Thermodynamics, Electromagnetism, Biology, Relativity, Quantum, Atomic, Extradimensional — plus locked Anomalous; Biology sits deliberately before the more advanced/theoretical branches, per the user's own framing of it as a foundational science); `SOCIETY_TECHS`/`ENGINEERING_TECHS` are structurally real but content-empty (Society is the collaborator's track, Engineering nobody's yet — every helper below already works for them the moment either gets real nodes). Pure helpers: `prerequisitesMet`, `anomalousUnlocked` (a simple aggregate threshold — `ANOMALOUS_UNLOCK_THRESHOLD` non-Anomalous physics techs researched — since the node itself isn't expanded yet), `visibleNodeIds` (every root + every researched node's children in full + grandchildren as a locked preview — the literal "two nodes into the future" rule), `canResearch`.
- `src/state/techStore.ts` (Technology Phase 1) — per-country tech state, `Record<countryId, TechState>` (mirrors the REAL per-nation economy state shape, `economyStore.ts`'s countryReports, not the inert global `resourceStore.ts`), each `TechState` a `researchPoints` record (one number per category) plus a `researched: Set<string>`. A fresh/untouched country's state seeds `researched` with `['warp-theory', 'hyperspace-theory']` already in it — NOT empty — so a brand-new country starts exactly as capable as one is today (see the Technology system section below for why). `stateFor(countryId)` returns a single stable shared reference for any country not yet touched (never constructs a fresh object per call — this is read inside `useCombatResolver`'s per-tick hot path and inside React selectors, so a fresh reference every read would defeat memoization entirely). `grantResearch` (the dev-console hook — no real income system exists yet) and `researchNode` (validates prerequisites/lock/cost, deducts, adds).
- `src/hooks/usePlayerTech.ts` (Technology Phase 1) — mirrors `usePlayerEconomy.ts`'s exact join pattern (`playerStore`'s `selectedCountryId` → that country's own state) against `techStore` instead of `economyStore`.
- `src/components/TechPanel.tsx` (Technology Phase 1) — `NationTechPanel({ subcategory })`, the Technology nav category's real content (Physics/Society/Engineering sub-tabs), mirroring `NationEconomyPanel`'s own join (`usePlayerEconomy()` for the country id) plus `usePlayerTech()`. Physics renders every root as its own column (`branchNodes` — a small BFS over the flat node list, purely for display grouping; a convergent node like Exotic Matter Theory legitimately appears under both branches it's reachable from), each node a researched / real-and-affordable (button) / locked-preview (name + 🔒/— only, no cost shown) row per `visibleNodeIds`/`canResearch`/`prerequisitesMet`. Society/Engineering render the same honest "no research trees yet" placeholder every other not-yet-real system in this project uses. A "Tree View" button opens `TechTreeGraph`.
- `src/components/TechTreeGraph.tsx` (Technology Phase 1, later addition) — a real node-link diagram of a tech category, requested specifically because the flat list can't show a node genuinely converging from two branches (Exotic Matter Theory: Quantum OR Atomic; Hyperium Synthesis: Extradimensional AND Quantum/Atomic together — the list view can only place either once). Rendered via `createPortal` straight to `document.body` — triggered from inside a small `DraggableWindow`, whose own root has a CSS `transform` that would otherwise make `position: fixed` relative to that window instead of the viewport. Layout: `computeDepths` (longest-path-from-a-root, purely for column placement — a convergent node's depth follows whichever prerequisite alternative is ready soonest), nodes grouped into lanes by root-branch and stacked within a `(lane, depth)` cell when several share a tier, edges drawn as bezier curves between them (a distinct "convergent" style — thicker, amber — when a node has more than one incoming edge or an AND-set with 2+ members). Still respects `visibleNodeIds`' fog-of-war exactly like the list view — showing the FULL unresearched tree was deliberately not the ask, only making the ALREADY-visible convergence structure legible. Nodes are directly clickable to research, same `canResearch`/`researchNode` as the list.
- `src/components/TabBar.tsx` (Workspace Tabs phase) — `HudCenterSwap`, mounted in the header's centre slot in place of a bare `<ResourceBar />`: shows `ResourceBar` OR the tab strip (`TabStrip`), never both — a small toggle button flips `workspaceStore.showTabBar`. Each `Tab` renders `tab.name ?? autoTabLabel(tab)` (level + selected body name, e.g. "System · Mars" — cheap to derive from a stored snapshot, not a full `LocationLabel` port), a close `×` (hidden when it's the only tab), and double-click swaps the label for an inline rename `<input>` (Enter/blur commits via `workspaceStore.renameTab`, which normalizes an empty/whitespace name back to `null`). A trailing `+` calls `createTab`.
- `src/hooks/useHudBarLayout.ts` — **bugfix, found and fixed this session while verifying the tab bar didn't break sidebar docking**: the hook's effect had a `[topRef, bottomRef]` dependency array, but a `useRef` object's identity never changes across renders — App.tsx calls this hook *before* its own `if (!selectedCountryId) return <MainMenu />`, so on the very first render (still showing `MainMenu`, header/footer not yet mounted) the refs are null, the effect no-ops, and — because the dependency array never actually changes — it then **never runs again**, even once a nation is chosen and the real header/footer mount. `--hud-top-height`/`--hud-bottom-height` had therefore never actually been set since the onboarding gate was added (per this file's own earlier note on that gate being "encountered via a merge"); every docked panel (NavBar, Outliner, DebugConsole) had silently been running on the hardcoded `52px`/`58px` CSS fallbacks the whole time. Harmless when the header happened to be close to that height, but a real, measurable overlap (~24px, confirmed live) whenever it wrapped to two lines. Fixed by dropping the dependency array entirely — the effect now re-runs after every render (cheap: disconnect + reobserve, and this is a rarely-re-rendering top-level component), so it correctly picks up the refs the first time they're actually non-null.
- `src/hooks/useCombatResolver.ts` — drives combat off the clock the same way `useShipOrderSettler` does (subscribing to `simDays`, not its own rAF loop), so battles resolve regardless of which view's `<Canvas>` is mounted, or whether anyone is watching. Reconciles the roster, steps in strict whole timesteps (carrying sub-step remainder — see Design Decisions), applies damage/destructions, turns completed FTL charges into real move orders, and keeps the clock's mode in sync with whether any fight is live.
- `src/components/FleetManagement.tsx` — the Fleet Management window's three subtabs. **Fleet Manager**: every ship in service with live integrity and current action. **Ship Designer**: now itself split into two sub-tabs (Phase 15) — **Presets** is the original read-only catalog of the five hand-authored hulls (components, defenses, a per-mount armament table, raw DPS, damage-type matchups), completely unchanged; **Custom Designs** is the new ship builder — pick a `HullChassis`, name a design, fill its per-category slot grid via size-filtered `<select>` pickers (`SlotEditor`, one row per physical slot — the dropdown itself can only ever offer modules that fit, so there's no illegal-pick case to guard against), and the computed stats below reuse `DesignDetail` completely unchanged by wrapping the live design through `designToShipClass`. A custom design is spawned the same way any hull is — through the Debug Console's class picker, which now lists custom designs alongside the presets. **Strategizer**: reserved, deliberately empty.
- `src/scene/CombatViewScene.tsx` — the combat "detailed view": an engagement rendered as a navigable 3D arena. Structurally a sibling of `SatelliteViewScene` (own Canvas, OrbitControls, exit-by-zooming-out watcher, panels outside the Canvas) but its subject is a fight. Owns click-to-move (latching `holdPosition`) and right-click-to-target, and exits itself when the engagement ends.
- `src/scene/CombatGrid.tsx` — the lattice visual: interior grid, node points, and outer cage as three single-draw-call `BufferGeometry` layers, with interior opacity scaling *down* as density rises. Also exports `nearestNodeToRay`, which scores every node by perpendicular distance to the click ray (a ray passes through many nodes, so a hit test can't answer "what did they click").
- `src/scene/CombatShipMarker.tsx` — a combatant in the arena. Reads its position from the store every frame via `useFrame` + `getState()` rather than props, since the resolver rewrites participant state constantly. Carries an integrity bar and a pulsing FTL-charge badge directly on the marker.
- `src/scene/CombatPathLine.tsx` — a ship's committed lattice route. Drawn for every participant, not just the selected ship: it's both the feedback for your own order and the read on where the enemy is going, and it's what makes the grid constraint visible (routes staircase rather than glide).
- `src/components/CombatPanel.tsx` — the per-engagement command surface: full roster with live integrity per side, grid-density control, and (for a selected player ship) firing target, focus-fire component, and manual/auto movement state. Deliberately distinct from `ShipPanel` (a per-ship inspector available everywhere), and the one `DraggableWindow` with no close button.
- `src/state/viewStore.ts` — zustand store for navigation: `level: 'galactic'|'interstellar'|'system'|'satellite'`, `selectedStarId`, `selectedBodyName` (the planet/star currently open in satellite view — generic name since it's not just planets), `inViewSelection` (whatever's selected *within* the active scene, in that scene's own id space — star id for interstellar, body/moon name for system/satellite; see Design Decisions), `lockOnEnabled` (whether selecting something eases the camera onto it — see Design Decisions), with `enterGalactic`/`enterInterstellar`/`enterSystem`/`enterSatellite`/`exitSatelliteToSystem`/`selectInView`/`toggleLockOn` actions. `exitSatelliteToSystem` deliberately leaves `selectedBodyName` set (unlike `enterSystem`, which clears it) so `SolarSystemScene` can detect "we just zoomed out of a body" and start near it — see Design Decisions. `pendingSelectBodyName` (a separate field) was retired this session, folded into `inViewSelection`. Also `exitSystemToInterstellar` (a later session's zoom-out-focus fix). Since the Workspace Tabs phase (see Design Decisions): `activeNavCategory`/`activeNavSubcategory`/`setNavCategory` — NavBar's own "which category window is open" state, moved here from local `useState` specifically so it's covered by the same generic per-tab snapshot `workspaceStore.ts` already does for the rest of this store, rather than needing its own special case.
- `src/state/workspaceStore.ts` (Workspace Tabs phase) — browser-style tabs, single-active-tab (never more than one live `<Canvas>` at a time — see Design Decisions for why a "gallery" of several rendered at once was scoped out). `TabSnapshot` (`id`, `name: string | null` — a player override, `null` falls back to an auto-derived label, `view` — every `viewStore` data field except `lockOnEnabled`, deliberately left a true global — plus `selectedShipId`). `tabs`/`activeTabId`, `createTab`/`closeTab`/`switchToTab`/`renameTab`, `showTabBar`/`toggleTabBar` (whether the header is currently showing the tab strip instead of `ResourceBar` — see `TabBar.tsx`). The load-bearing trick: `switchToTab` snapshots the OUTGOING tab's live `viewStore`/`shipStore.selectedShipId` state into its array slot, then restores the INCOMING tab's stored slot back into those same live stores via `setState`/`selectShip` — every other component in the app keeps reading the plain global `viewStore`/`shipStore` completely unchanged, unaware tabs exist at all. `useCombatResolver` already resolves every engagement independent of which view is mounted, so a fight in a backgrounded tab keeps resolving for free.
- `src/scene/orbitMath.ts` — shared helpers: `getOrbitPosition` (circular orbit position with inclination + ascending-node tilt applied), `angleForYear`, `getPlanetPosition(data, simYears)` (a planet's exact live position from data alone), and `getMoonPosition(moon, simYears)` (same idea for moons, using `MOON_TIME_DILATION` — see Design Decisions).
- `src/scene/planetData.ts` — real astronomical data (AU distances, km radii — now exposed as `radiusKm` on `PlanetData` too, for info-panel display — orbital periods, inclination + longitude of ascending node, fixed `phaseDeg`) for the Sun + 8 planets, on a single unified scale (`UNITS_PER_AU`). Also exports `SUN_RADIUS_KM` and `AU_IN_KM` (the latter now reused by `shipPhysics.ts` for real-scale travel-time math, not just the rendering scale).
- `src/scene/moonData.ts` — real radius/distance/period data for each planet's major moons (not an exhaustive list — Jupiter alone has ~95 known moons; only the well-known ones are rendered, e.g. the 4 Galilean moons). `getMoonsForPlanet(name)` returns `{ totalCount, moons }` — `totalCount` is the real known total (for the info panel), `moons` is the derived, render-ready list (visual radius/orbit radius computed from real ratios, log-compressed and now also hard-clamped (`MAX_ORBIT_RADIUS = 12`) for orbit distance, clamped for size, since real moon proportions are far too extreme to render true-to-scale within a fixed `VISUAL_RADIUS`).
- `src/scene/bodyStats.ts` — `estimateHabitability(name, orbitAU)` and `estimateSize(radiusKm)`: deliberately simple, transparent Stellaris-flavor heuristics (habitable-zone distance from Sol, radius → size class → district count) for the satellite-view info panel's "game info" section — not a real economy/habitability simulation, which this project doesn't have yet.
- `src/scene/inspectableBody.ts` — the `InspectableBody` shape (`name`, `kind: 'star'|'planet'|'moon'`, `color`, `radiusKm`, optional orbit/moon-count fields) used to feed the info window regardless of what's selected.
- `src/scene/Sun.tsx`, `src/scene/Planet.tsx`, `src/scene/Moon.tsx`, `src/scene/OrbitRing.tsx` — system-view and satellite-view marker/orbit components. `Planet.tsx`/`Moon.tsx` read global sim time directly from `gameTimeStore.getState()` in `useFrame`. `Sun.tsx`/`Planet.tsx` (system view) stay marker-only, no visible sphere. `Moon.tsx` (satellite view) now renders an actual `HologramBody` sized to `moon.visualRadius` alongside its marker dot — reversed from an earlier decision to keep it marker-only; see Design Decisions ("Moon hologram restored").
- `src/scene/FocusableMarker.tsx` — reusable: the clickable name marker for whatever's the sole/primary subject of a satellite-view scene (the planet/star in `SatelliteViewScene`, the moon in `MoonDetailScene`). Always renders as a prominent label anchored *above* the hologram (`.focused-label`) rather than sitting on top of it — it's always shown for as long as that body is the view's subject, not gated on the inspect window being open (see Design Decisions).
- `src/scene/MoonDetailScene.tsx` — a moon's own close-up: same structural pattern as `SatelliteViewScene` (HologramBody + FocusableMarker + Stars + exit watcher + InspectPanel) but simpler — no orbiting children, and exiting calls an `onExit` prop instead of a `viewStore` action, since it's not a distinct `ViewLevel` (see Design Decisions).
- `src/scene/CameraFocusRig.tsx` — reusable: each frame, snaps `OrbitControls.target` exactly to a live target position (no lag) and eases the camera's *distance* toward it exponentially, firing `onArrive` once within `arriveDistance` **or** after `maxFlightMs` (default 2500ms) regardless of distance — the timeout is what guarantees "Detailed View" always completes even for a fast-orbiting planet at high game speed (see Design Decisions). `OrbitControls` is disabled for the duration. Used for the "Detailed View"/"Enter System" fly-ins (system view, interstellar view) and the fly-to-a-moon inside satellite view.
- `src/scene/SelectionTracker.tsx` — reusable: every frame, *eases* `OrbitControls.target` toward a live object position (exponential smoothing, not an instant snap) while leaving user input **enabled** — the passive "camera lock" (object stays centered no matter how the player free-zooms/orbits from there). Runs at r3f priority `-2` so the target is fresh before drei's own OrbitControls update (priority `-1`) consumes it.
- `src/scene/DistanceThresholdWatcher.tsx` — reusable: fires once when camera distance crosses a min or max threshold. Distance is measured from the world origin by default, or from the current `OrbitControls.target` if a `controlsRef` is passed (needed so "zoom out to leave this view" still works correctly while camera-locked onto something off-origin, like Saturn). Drives *scroll-past-the-edge-to-navigate* between whole view levels, both directions, for every adjacent pair. Has a `graceMs` (default 500) after mount to absorb scroll-momentum carryover from the previous view.
- `src/utils/forwardWheel.ts` — `forwardWheelToCanvas`: HTML marker overlays sit in the canvas's shared event container but, being DOM siblings of the canvas rather than descendants, swallow wheel events before they reach it. Every marker's `onWheel` re-dispatches onto the actual `<canvas>` element.
- `src/scene/HologramBody.tsx` (was `HologramPlanet.tsx`, generalized) — the DEFCON/HUD-style visual used in satellite view: a core sphere, sparse lat/long wireframe grid, glowing Fibonacci-distributed dot cloud, and fresnel rim glow — all **procedural**, no texture assets. Takes `color`/`radius`/`variant` (`'planet'` = dark reflective-looking core, `'star'` = bright self-luminous core + a point light + a bigger corona-style rim) instead of a `PlanetData` object, so it works for the Sun too. Optional `onSelect` adds an invisible, slightly oversized click-catcher sphere so clicking anywhere on the hologram itself (not just its separate `FocusableMarker`) selects it.
- `src/scene/SatelliteViewScene.tsx` (was `PlanetViewScene.tsx`) — satellite-view `<Canvas>`, takes a `bodyName` (planet name or `"Sol"`). Renders `HologramBody` for the primary body (clickable via its own `onSelect` + a `FocusableMarker`), that body's moons (now each its own `HologramBody` too, not marker-only — via `Moon.tsx` + real orbit rings), any ships resting in orbit here (`SatelliteShipMarker`), a `DistanceThresholdWatcher` (max) that calls `exitSatelliteToSystem` on zoom-out past the edge, and an `InspectPanel`/`ShipPanel` (draggable window, ship panel taking priority) for whichever body/moon/ship is currently selected. Selecting a moon also engages a `SelectionTracker` lock and, via the InspectPanel's optional `action` button, "Detailed View" — `CameraFocusRig` flies to it and, on arrival, swaps the whole scene over to `MoonDetailScene` (see Design Decisions). Selection is `viewStore.inViewSelection`-driven (not local state) so the `Outliner` can select entries here too; a `useEffect` pops back out of `MoonDetailScene` if that selection points elsewhere.
- `src/data/starData.ts` — real nearest-neighbor stars/systems to Sol (within ~10 ly). Every one of them now has `hasSystemData: true` (drillable into system view) and a real planet list in `planetData.ts`'s `PLANETS_BY_STAR` (Sol, Alpha Centauri, Barnard's Star, Wolf 359, Lalande 21185, Sirius, Luyten 726-8, Ross 154) — this expanded past Sol-only at some point in the collaborator's economy/politics track (nation capitals now sit at non-Sol systems, e.g. Orion Republic at Arcadia/Alpha Centauri), noted here since an earlier version of this file claimed only Sol was drillable and that's since gone stale. Exports `starScenePosition` (was a local `toScenePos` inside `InterstellarScene.tsx`, promoted here so `shipPhysics.ts` can compute the same on-screen star position for travel math without a scene-importing-scene dependency).
- `src/data/shipData.ts` — ship drive types (`WarpDrive`, `HyperDrive`; every ship also implicitly has a `reactionDrive: true` for sublight, since warp/hyperdrives don't handle real-space movement) and `ShipClass` (name/`reactionDrive`/`ftlDrives[]` — an array, not a single optional drive, so a future ship combining warp *and* hyperdrive doesn't need a shape change; no longer carries a `color` — see `FleetAllegiance` below). `WARP_SPEED_TIERS_C` (`[0.5, 1, 10, 100, 314]`) and `HYPERDRIVE_BASE_COOLDOWN_DAYS` (27) are real data, not wired to a tech tree yet (none exists). `SHIP_CLASSES` currently holds two example classes: Swift Courier (10c warp) and Star Jumper (27-day hyperdrive). Also exports `FleetAllegiance` (`'player'|'friendly'|'neutral'|'hostile'`) and `ALLEGIANCE_COLORS`/`ALLEGIANCE_LABELS` — see Design Decisions.
- `src/state/shipStore.ts` — zustand store: `ships: ShipInstance[]` (`id`/`classId`/`name`/`allegiance`/`location`/`order`/`hyperdriveReadySimDays`) + `selectedShipId` (global — not per-scene local state, so selection survives view changes, per the explicit requirement) + `spawnShip`/`removeShip`/`selectShip`/`setShipOrder`/`setShipLocation`. Deliberately dependency-free of the scene/physics layer (matches `viewStore`/`gameTimeStore`'s existing convention) — callers resolve the actual travel math via `shipPhysics.planMove` and hand the store an already-computed result. `ShipLocation` (where a ship rests: `orbiting` a body with a small cosmetic `offset`, a static `system-point`, `star`, or `interstellar-point`) and `MoveOrder` (`destination`/`departSimDays`/`arrivalSimDays`/`space`/`startPosition`/`endPosition`) are the two halves of "where a ship is" — see Design Decisions for why travel is a pure function of simDays rather than accumulated per-frame movement.
- `src/scene/shipPhysics.ts` — all the real-scale travel math in one place: real constants (`SPEED_OF_LIGHT_KM_S`, `LY_IN_KM` derived from it, `REACTION_DRIVE_SPEED_KM_S`), `KM_PER_SYSTEM_UNIT`/`KM_PER_INTERSTELLAR_UNIT` (the same `AU_IN_KM`/`UNITS_PER_AU` and `LY_IN_KM`/`UNITS_PER_LY` ratios the rendering scale already uses, so ship speed is consistent with what's on screen), `getShipRenderPosition` (pure function of simDays — lerps between an order's start/end once travelling, or resolves a resting `ShipLocation` otherwise), `getShipStatusText` (the "In Sol System, orbiting Earth" / "En route to X" / "In Deep Space" display text), and `planMove` (the single entry point that turns "ship X, go to Y" into a `MoveOrder`, an instant hyperdrive jump, a cooldown rejection, or nothing — including picking which drive to use). Also `shipSystemId` (which system, if any, a ship currently belongs to — shared by `SolarSystemScene`'s membership filter and `InterstellarScene`'s presence-badge map) and `satelliteOrbitLocalPosition` (the stable per-ship-id hash placement `SatelliteShipMarker` uses).
- `src/scene/ShipMarker.tsx` — a spawned ship's marker: a small triangle (not the circular planet/star dot) so ships read as visually distinct from celestial bodies, colored by `ALLEGIANCE_COLORS[ship.allegiance]` (not the ship's class — see Design Decisions). Position comes from `getShipRenderPosition` each frame (same pattern as `Planet.tsx`); also the place that notices an order's `arrivalSimDays` has passed and calls `setShipLocation` to settle it — harmless if this fires late (nothing else is watching) since the render position already clamps at the destination regardless of when the bookkeeping catches up. Supports `selected`/`hovered` like other markers.
- `src/scene/SatelliteShipMarker.tsx` — the satellite-view equivalent of `ShipMarker`, for ships resting in orbit around the body the current satellite view is showing. Same triangle visual, but positioned via `shipPhysics.satelliteOrbitLocalPosition` (a stable per-ship-id hash placement near the hologram) instead of a physically resolved position, since a ship's real `orbiting` offset is in system-AU units that have no meaningful translation into this view's unrelated hologram-visual-radius scale — see Design Decisions.
- `src/scene/ShipPanel.tsx` — the selected ship's info window (`DraggableWindow`-based): class, drives, and a live location/status line (subscribes to `simDays` directly, same pattern `TimeControls` already uses, so it updates continuously while a ship travels, not just at the moment the panel opened).
- `src/scene/DeepSpaceClickPlane.tsx` — an invisible full-coverage ground-plane mesh added to both `SolarSystemScene` and `InterstellarScene`, the canonical "empty space" target for left-click (deselect) and right-click (issue a move order to a deep-space point). Needed because right-clicking true empty space has nothing to raycast against otherwise. Its own click handlers carry the same `.planet-marker`/`.ship-marker` closest-check guard as every other `onPointerMissed` use in this codebase (see Design Decisions — adding a full-coverage raycast target changed what "a miss" means at the plane's own event level, not just at the Canvas level).
- `src/components/DebugConsole.tsx` — dev-only ship-spawning tool (pick a class + a body to spawn "orbiting," at a small fixed cosmetic offset from a small ring of presets, an allegiance, spawn/remove; scenario loading; and, since Technology Phase 1, a "Grant Research" row — category + amount — the only way to gain research points beyond the default seed until the real economy produces them). Toggled with the backtick key. Only ever rendered behind `import.meta.env.DEV` in `App.tsx` — see Design Decisions for why that's the real access-control boundary, not the keybind.
- `src/scene/InterstellarScene.tsx` — interstellar-view `<Canvas>` + wrapper: star nodes (Obsidian-graph style) as marker labels only, no connecting lines yet (those represent player-plotted routes, added later). **Select-first, same model as system view** (see Design Decisions): clicking a star only locks the camera onto it (`SelectionTracker`); flying all the way in (`CameraFocusRig`) starts only via the info panel's "Enter System" button (shown when `hasSystemData`) or by manually zooming in close enough on the locked star. `enterSystem` fires on `CameraFocusRig` arrival. Has its own `onPointerMissed` deselect handler (same marker-click race guard as system view). `DistanceThresholdWatcher`s handle the unconditional zoom-in-on-Sol → system shortcut and zoom-out → galactic. `StarNode` also renders a no-text `fleetAllegiances` presence badge (see Design Decisions) for any fleet nested inside that star's system.
- `src/scene/GalacticViewScene.tsx` — galactic-view placeholder: starfield + "GALACTIC VIEW — Not yet charted" text, intentionally not built out further yet.
- `src/components/Breadcrumb.tsx` — top HUD nav, generic level labels only: GALAXY › INTERSTELLAR › SYSTEM › SATELLITE, clickable to jump up a level.
- `src/components/LockOnToggle.tsx` — top HUD button (next to `Breadcrumb`, inside a shared `.hud-top-right` flex group) toggling `viewStore.lockOnEnabled` — see Design Decisions.
- `src/components/LocationLabel.tsx` — the current *specific* location, e.g. "Sol System — Mars". Large and centered at the bottom of the screen (`.location-label`, absolutely positioned within `.hud-bottom`) — was originally a small subtitle under the app title; moved and enlarged since the location is core information the player should read at a glance (see Design Decisions).
- `src/components/TimeControls.tsx` — in-game date, pause/play, speed up/down, 5-pip speed indicator. Lives in the bottom-left HUD cluster alongside `ChatPlaceholder`, not centered (that spot is now `LocationLabel`'s).
- `src/components/ChatPlaceholder.tsx` — non-functional placeholder reserving a spot for a future fleet-comms/chat panel, bottom-left next to `TimeControls`.
- `src/components/Outliner.tsx` — collapsible right-side panel, Stellaris-style: a search box + kind-filter pills (Stars/Planets/Moons/Black Holes — the last one has no matching entries yet, reserved for when `GalacticViewScene` grows real content) above an "In View" section listing whatever the current view level shows (all stars in interstellar, Sol + all planets in system, the primary body + its moons in satellite — derived purely from `viewStore` + the same static data files every scene reads, colored dot/ring icons matching each body's real color) plus a real "Fleets" section (colored by `ALLEGIANCE_COLORS`, triangle icon matching the viewport) and placeholder "Colonies"/"Starbases" sections (empty — no gameplay systems back these yet, same non-invented-data spirit as `ChatPlaceholder`). Every section (including "In View") is independently collapsible via its own caret, on top of the whole panel's own collapse-to-edge-tab. Search/filter apply globally. **Entries are clickable** — "In View" entries call `viewStore.selectInView`, fleet entries call `shipStore.selectShip`, both highlighted (`.selected`) when active — see Design Decisions.
- `src/components/NavBar.tsx` — collapsible left-side nation nav, structurally the mirror image of `Outliner` (same collapse-to-edge-tab pattern, flipped): the player's real chosen nation name (`playerStore`/`countryData.ts` — this note was stale, a real nation-select `MainMenu` exists now, added by the collaborator's track) and Stellaris-style category buttons, most still `DraggableWindow` placeholders ("Not yet available" — same non-invented-data spirit as everywhere else in this HUD) except Settings/Map Modes/Economy/Military▸Navy (real) and, since Technology Phase 1, Technology▸{Physics,Society,Engineering} (`TechPanel.tsx` — Physics real, Society/Engineering honest placeholders). Technology (like Military▸Navy) renders `wide`, since its multi-column branch grid needs the room. Since the Workspace Tabs phase: which category/subcategory is open now lives in `viewStore.activeNavCategory`/`activeNavSubcategory` (via `setNavCategory`) instead of local `useState` — the `collapsed` sidebar-itself toggle stays local, since that's UI chrome rather than "what's open," and correctly stays the same across every tab.
- `src/components/DraggableWindow.tsx` — generic movable HUD window (pointer-based title-bar drag; real edge/corner resize; a real collapse toggle) — used by `InspectPanel`, `FleetManagement`, `CombatPanel`, `ShipPanel`, and NavBar's category windows. This note was stale — updated: resize now anchors the top-left corner (was centre-hung, since the box is positioned via a `calc(-50% + Xpx)` transform — growing width/height without correction expanded the box symmetrically in both directions; the resize handler now shifts `pos` by half of whatever the size just grew by on the axis that re-centres, cancelling the drift on the edge that should stay fixed — anchored windows skip the X-axis correction since their X position is already pinned via CSS, not the transform). Clicking anywhere in a window (capture-phase, so it works even through a child button) now also brings it to front, via a shared module-level `zIndex` counter each instance draws from on mount and on click — previously every window shared one fixed CSS `z-index: 20`, so which one visually "won" was pure DOM mount order with no way to re-focus an older one.
- `src/components/InspectPanel.tsx` — the shared "inspect a body" window used by every view (system, satellite, and — via a plain `DraggableWindow` with the same row markup — interstellar too): wraps `DraggableWindow`, renders an `InspectableBody`'s basic info (type/radius/orbit/moon count) plus, for planets/moons, the `bodyStats.ts`-derived game info (size class, districts, habitability). Stars skip the game-info section (doesn't apply to them yet). Optional `action` prop (`{label, pendingLabel, pending, onClick}`) renders a "Detailed View"/"Enter System"-style button (or, while `pending`, a status line).

# Design Decisions Made So Far:
- **True-to-scale solar system**: orbital distances and body radii both use the *same* real-world scale factor (1 AU = `UNITS_PER_AU` scene units, currently 20), so relative proportions are physically accurate.
  - Tradeoff: at true scale, planet spheres are geometrically sub-pixel at any usable zoom. Fixed DEFCON-style: each body has a small constant-screen-size marker (circle + label, via drei `Html`) at its exact true 3D position.
- **Orbital tilts are real, not decorative**: real J2000 inclination + longitude of ascending node values, via `orbitMath.getOrbitPosition`.
- **Global time engine (Stellaris-style)**: single `gameTimeStore` holds `simDays` since 2600-01-01, advanced by a `requestAnimationFrame` loop independent of any Canvas. 5 discrete speed tiers + pause. All orbital motion is a **pure function of `simDays`**, never per-frame delta accumulation.
- **View levels as separate `<Canvas>` per level**: each mounts its own Canvas rather than sharing one camera rig.
  - **Gotcha**: each remount creates a new WebGL context; heavy back-and-forth navigation in one tab can hit the browser's context limit (`THREE.WebGLRenderer: Context Lost`, frozen scene) — fix is a hard tab reload, not a code bug.
- **In system view: select = lock on immediately, not click-to-jump.** Clicking a planet or the Sun both selects it (info panel + "Detailed View" button — now available for the Sun too, see below) *and* immediately engages the camera lock (`SelectionTracker`), without requiring zoom-in first. Zooming out far enough still exits to interstellar exactly as if nothing were locked (`controlsRef`-aware `DistanceThresholdWatcher`). Clicking "Detailed View" triggers `CameraFocusRig`'s fly-in and transitions into satellite view on arrival. Clicking empty space clears the selection/lock.
- **Manually zooming in on a locked-on body also enters satellite view** (not just the "Detailed View" button) — a `DistanceThresholdWatcher` (mode `min`, measured from the tracked body via `controlsRef`, threshold `3`) fires `enterSatellite` once the player zooms in close enough on their own. Works for the Sun too, not just planets.
- **Scroll-past-the-edge navigation between whole levels**, wired both directions for every adjacent pair, each with a CSS fade/scale-in (`.view-transition` in `App.tsx`) regardless of what triggered the swap.
- **Interstellar view uses real star data**: nearest real stars/systems to Sol within ~10 ly, real RA/Dec/distance → Cartesian. Originally only Sol was drillable into a system view — since expanded (collaborator's economy/politics track) so every nearby star has real planet data and is drillable; see the `starData.ts`/`planetData.ts` entries above. Hub-and-spoke lines were removed — those represent player-plotted routes, not something drawn automatically.
- **Galactic view is intentionally a stub** per explicit instruction.
- **Celestial body visuals are procedural, not textured**: the DEFCON/HUD "hologram" look (`HologramBody.tsx`) is Three.js geometry + a small custom shader, no texture assets — deliberately chosen to match the vector-art DEFCON aesthetic. An `assets/` folder exists at the project root for future binary assets if ever wanted (user's convention: subfolder per asset type).
- **Marker/orbit line colors are bright cyan** (`#2fd8ff`) — an earlier dim navy was too hard to see.
- **Markers have `user-select: none`**: without it, real-mouse cursor jitter between mousedown/mouseup gets hijacked into a native text-selection drag instead of registering as a click.
- **Exit thresholds across all levels are tuned so the current view shrinks to "basically a dot" before handing off** — not a modest zoom-out. System→interstellar: `26000` (default start ~10500, see below). Interstellar→galactic: `3500`. Satellite→system: `130`/`160` (exit/max), against a visual radius of `3`.
- **`onPointerMissed` race with marker clicks**: r3f's `<Canvas onPointerMissed>` fires for any click inside the canvas's shared event container that doesn't raycast-hit a 3D object — including clicks on HTML marker overlays (portaled into that same container so drei's `Html` can render above the canvas). Clicking a marker fires the marker's own `onClick` *and* `onPointerMissed` almost simultaneously; without a guard, the miss handler undoes the selection in the same tick. Fixed everywhere `onPointerMissed` is used (system, interstellar, satellite) by checking `event.target.closest('.planet-marker')` and ignoring the miss when the click landed on a marker. **Any future `onPointerMissed` usage needs the same guard.**
- **`CameraFocusRig` snaps its target exactly each frame (never lerps it), and has a `maxFlightMs` timeout**: an earlier version lerped the target, which left it perpetually lagging a fast-moving planet (steady-state lag ≈ orbital speed / rig speed) — since arrival is judged against the *true* live position, that lag could stay ≥ `arriveDistance` forever, breaking "Detailed View" for fast planets (Mercury, especially at high game speed) while time was running. Snapping the target fixed most of it, but a *second*, subtler issue remained: even with an exact snap, each frame's offset partially reflects how far the target itself moved since last frame (not just how far the camera closed in), so a fast-enough orbit can still prevent the instantaneous distance from ever dipping under `arriveDistance`. Rather than chase a perfect closed-form fix for every possible orbital-speed/game-speed combination, added a hard timeout (`maxFlightMs`, default 2500ms) that force-arrives regardless of distance — this is what actually, robustly fixes "only works when the planet happens to be moving toward the camera." Verified at max game speed on Mercury (the fastest, worst-case planet).
- **Marker dot is centered independently of the label**: `.planet-marker` centers the dot specifically (its own `translate(-50%,-50%)`), with the label absolutely positioned beside it — not a single transform on the whole flex row (which centered the row's bounding box, visibly offsetting the dot from the true point/off the orbit ring).
- **`DistanceThresholdWatcher` has a `graceMs` (default 500) after mount** to absorb scroll-momentum carryover into a freshly-mounted Canvas, preventing an unintended cascade through multiple level transitions from one continuous zoom gesture.
- **`SelectionTracker` eases continuously (never snaps)**, unlike `CameraFocusRig`'s exact-snap-plus-eased-distance: it has no "arrival" check to break, so continuous eased lag is purely cosmetic (and desirable — smooth pan on lock-on) here, whereas the same kind of lag was a functional bug in `CameraFocusRig`.
- **System view starts far away on a fresh arrival, but "somewhat close" when returning from satellite view via zoom-out.** A fresh arrival (breadcrumb, interstellar fly-in) starts at `FAR_START` (`[0, 6300, 8400]`, distance ~10500) so the whole system reads as a small distant cluster, symmetric with the "shrink to a dot" exit feel. But if `viewStore.selectedBodyName` is still set at mount (meaning we just zoomed out of that body's satellite view — `exitSatelliteToSystem` deliberately doesn't clear it), `SolarSystemScene` instead starts the camera at `NEAR_START_DISTANCE` (`18` units) from that body's live position *and* pre-selects/locks onto it immediately — so leaving a planet's close-up reads as smoothly stepping down a level of detail while staying near it, not jumping to an unrelated distant view. Both the initial camera position and the initial `selectedName` are computed once via a ref captured at mount (`continuityBodyRef`), deliberately not reactive to later state changes.
- **Moon orbits are tied to the game clock but time-dilated by 60x (`MOON_TIME_DILATION`)**: real moon orbital periods (hours to weeks) are far too fast to animate at the same accelerated day-scale everything else uses — a real-speed Phobos orbit would complete in a fraction of a real second and look like a flicker. Rather than decouple moons from the game clock entirely (which would mean they keep moving while the game is paused, inconsistent with everything else), their effective period is multiplied by 60 before being run through the same `simDays`-driven math as planets — so pausing/speeding up the game still affects moons consistently, but they're visually legible. Relative speed differences between moons (closer/faster vs. farther/slower) are preserved from the real ratios.
- **Moon sizes/orbit distances are hand-scaled from real ratios, not literally true-to-scale**: real moon-to-planet size and distance ratios vary too wildly to render directly (Jupiter dwarfs its moons far more than Earth dwarfs the Moon) within a fixed `VISUAL_RADIUS` primary body. `moonData.ts` computes visual radius from the real `moon/planet` radius ratio (clamped to a visible min/max) and orbit radius from the real distance ratio (log-compressed) — the real *ordering* (bigger/farther reads as bigger/farther) is preserved even though absolute proportions aren't literal.
- **Only major/well-known moons are rendered per planet**, not the real full count (Jupiter has ~95 known moons) — rendering all of them would be unreadable clutter. The info panel's "Moons" field shows the real total count regardless of how many are actually rendered, so the number is honest even though the visual is curated.
- **The Sun is now inspectable via satellite view too**, not just planets — this is *why* `HologramBody` was generalized from `HologramPlanet` (added a `variant: 'star'` for a bright self-luminous look vs. planets' dark reflective one) and why `viewStore`'s `selectedPlanetName` was renamed to `selectedBodyName` / `enterPlanet` to `enterSatellite` throughout. Sets up for other future "satellite view" subjects (asteroids, black holes) per the user's explicit framing.
- **"Game info" (habitability/districts) is a transparent placeholder heuristic** (`bodyStats.ts`), not a real simulation — this project doesn't have gameplay/economy systems yet. Worth replacing with real mechanics once those exist rather than building further on the heuristic.
- **The inspect window is a real drag-anywhere HUD window** (`DraggableWindow.tsx`), not a fixed corner panel — pointer-capture-based drag on the title bar, no resize. Re-centers on whichever body is newly selected; stays wherever dragged until a different body is selected.
- **Interstellar view now uses the same select-first model as system view** (this was the last open roadmap item on navigation consistency): clicking a star locks the camera onto it via `SelectionTracker` (smooth eased pan, `OrbitControls` still enabled) instead of immediately flying in with `CameraFocusRig`. The actual fly-in only starts from an explicit "Enter System" button (shown in the info panel when the star `hasSystemData`) or by manually zooming in close on the locked star (`DistanceThresholdWatcher` min, measured via `controlsRef` like system view's `ENTER_SATELLITE_DISTANCE`) — mirrors system view's select-vs-"Detailed View" split exactly. The unconditional "scroll straight through toward Sol" shortcut (measured from world origin, not the lock target) is preserved unchanged for players who zoom straight in without clicking anything first.
- **Moons get their own "Detailed View," structured as a mode-switch inside `SatelliteViewScene`, not a new `ViewLevel`.** Selecting a moon (click) opens its `InspectPanel` and engages `SelectionTracker` on it; the panel's "Detailed View" action flies `CameraFocusRig` to the moon's live position, and `onArrive` swaps the scene's content to `MoonDetailScene` (moon rendered as its own `HologramBody`, real habitability via the parent planet's `orbitAU`). Zooming out of `MoonDetailScene` calls a plain `onExit` callback that flips back to the primary body's view — no `viewStore.level` change at all, so the breadcrumb/exit-to-system behavior at the outer satellite level is completely unaffected. The mode-switch is wrapped in its own `.view-transition`-keyed div (`'primary'` vs `` `moon:${name}` ``) purely so the swap gets the same fade the rest of the app uses for level changes — this is a **second, independent instance** of the "key change forces remount + CSS fade replay" trick already used by `App.tsx`'s `ActiveScene`, just scoped one level deeper. **Gotcha found while wiring this up**: `CameraFocusRig`'s `onArrive` must also clear the local "flying to X" state (not just set "focused on X"), otherwise it stays truthy after zooming back out and the flight (and thus the mode-switch into detail view) re-triggers itself immediately — this doesn't happen at the top `viewStore` level because a level change there fully unmounts the component, discarding state for free; a same-component mode-switch has no such reset and needs it done explicitly.
- **Moon habitability reuses `bodyStats.estimateHabitability` as-is, fed the *parent planet's* `orbitAU`** rather than adding separate per-moon habitability data — a moon shares its planet's distance from Sol, so the same habitable-zone heuristic applies without inventing a second one. `InspectPanel` computes habitability for `kind !== 'star'` now (previously `kind === 'planet'` only). **Gotcha**: the existing "Orbit" AU/period-years row is keyed off the same `orbitAU` field, so naively enabling habitability for moons made that row appear too, displaying the *parent's* solar orbit under a moon's own name with a blank year figure (no `orbitPeriodYears` set for moons) — looked exactly like a bug. Fixed by gating that specific row to `body.kind === 'planet'`, while the moon's own real orbit still shows correctly via the separate `orbitPeriodDays` row.
- **`HologramBody`'s click-catcher is a separate invisible mesh (radius ×1.15, `opacity: 0`, `depthWrite: false`), not an `onClick` on the visible meshes.** Opacity doesn't affect raycasting in three.js (only `object.visible` does), so a fully transparent mesh still catches clicks reliably — simpler and more robust than adding handlers to multiple existing meshes (core/wireframe/rim) with different geometries and blend modes.
- **`FocusableMarker` always renders the prominent above-hologram label — it's not a toggle keyed off whether the inspect window is open.** Originally it had a `focused` prop that fell back to a small dot+label until clicked; changed because the "focus" of a satellite-view scene is really "being the subject of this view at all" (the planet you zoomed into, the moon you're now looking at in `MoonDetailScene`), not "having been clicked since arriving" — a first-time visitor shouldn't see a plain marker on the one object the whole scene is about. Simplified the component to drop the now-dead small-marker branch and the `focused`/`color` props entirely rather than leave unused code paths around.
- **Manually zooming in on a selected moon also enters its `MoonDetailScene`**, not just the "Detailed View" button — a `DistanceThresholdWatcher` (`min`, `controlsRef`-measured against the locked moon) alongside the existing `SelectionTracker`, mirroring system view's manual-zoom-to-enter-satellite shortcut. Threshold (`ENTER_MOON_DISTANCE = 4.5`) is tuned just above `OrbitControls.minDistance` (4) so it's actually reachable by scrolling in rather than being clamped away before it fires.
- **`viewStore.enterSystem` takes an optional second `preselectBody` argument, stored as its own `pendingSelectBodyName` field** — when arriving at system view *by zooming into a star* (both the interstellar `CameraFocusRig` arrival and the unconditional scroll-through-Sol shortcut now pass `'Sol'`), the resulting system view pre-selects and camera-locks that star immediately. Plain jumps (the SYSTEM breadcrumb) omit the second argument and keep the old no-preselect behavior. **Correction**: this was first implemented by reusing `selectedBodyName` (the same field `exitSatelliteToSystem` uses for "returning from satellite view" continuity), since both cases want a pre-selected body — but `selectedBodyName` being set is *also* what tells `SolarSystemScene` to start the camera near that body instead of at the far default (`continuityBodyRef`/`NEAR_START_DISTANCE`), and arriving fresh from interstellar should still show the whole system at a distance (matching the "shrink to a dot" arrival feel everywhere else), just with the star already selected — not jump the camera in close too. Split into a separate `pendingSelectBodyName` field (consumed by a second ref, `pendingSelectRef`, in `SolarSystemScene`) that affects only the initial `selectedName`, never `initialCameraPosition` — so the two kinds of continuity (near-start-because-returning vs. far-start-but-preselected) can't get coupled together again.
- **Bottom HUD bar reorganized: location is now a prominent bottom-right element, time controls + a chat placeholder moved to the bottom-left.** Previously the location was a small, easy-to-miss subtitle under the title (top-left) and `TimeControls` alone occupied the centered bottom bar. Since where-you-are is core information the player needs constantly, it's now large/bold/glowing (20px, up from 11px/0.75-opacity) — a normal flex child of `.hud-bottom` (`justify-content: space-between` against `.hud-bottom-left`), not absolutely positioned. `TimeControls` moved to a new `.hud-bottom-left` flex group alongside `ChatPlaceholder` (non-functional, just reserves the spot for a future comms/chat feature). **Note**: it was originally placed bottom-*center* via `position: absolute; left: 50%`, which measured as pixel-exact under Sol's marker dot in every state tested (fresh load, star selected) — but the user still perceived it as visually misaligned relative to the cluttered, overlapping multi-planet marker cluster at the far starting zoom, and asked for bottom-right instead rather than continuing to chase the perception gap. Worth revisiting if center-alignment is wanted again later — the likely real culprit is the marker cluster's own visual asymmetry (labels only ever extend right of their dot), not the location label's positioning math.
- **Every marker/label `<Html>` (`Planet.tsx`, `Moon.tsx`, `Sun.tsx`, `InterstellarScene`'s `StarNode`, `FocusableMarker`) passes `zIndexRange={[0, 0]}`.** drei's `Html` defaults to `zIndexRange={[16777271, 0]}`, which sets an inline `z-index` scaled by distance-to-camera — meaning a marker's stacking order relative to real HUD elements (`.draggable-window` at `z-index: 20`, `.star-info-panel` at `10`) silently depended on how close the camera happened to be, occasionally winning against panels it should always render behind. Flattening every marker to a constant `z-index: 0` makes panels always win, unconditionally.
- **Earth's moon is named `'Luna'` in `moonData.ts`**, not `'Moon'` — matches the proper-name convention already used for every other moon (Io, Europa, Titan, etc.); "Moon" is still the generic *kind* label shown in the inspect panel's "Type" row, which is a different, unrelated string.
- **Outliner entries are now clickable — the selection-state-lifting refactor flagged as a future step (see below, superseded) was done.** `viewStore` gained `inViewSelection`/`selectInView`, and `SolarSystemScene`/`InterstellarScene`/`SatelliteViewScene` all now source their "what's selected/camera-locked" state from it instead of local `useState` — each scene still interprets the string in its own id space (star id for interstellar, body/moon name for system/satellite), which is safe since only one scene is ever mounted at a time and every navigation transition resets `inViewSelection` to `null` (or seeds it deliberately — see below). Clicking an Outliner entry calls the exact same `selectInView`/`selectShip` a marker click would, so it gets the same `SelectionTracker` camera lock for free, not a bypassed/cheaper version. `pendingSelectBodyName` (used only to seed `SolarSystemScene`'s initial selection on arrival from interstellar) was folded into this same field rather than kept as a separate parallel mechanism; `exitSatelliteToSystem` now seeds `inViewSelection` from `selectedBodyName` directly (replacing `SolarSystemScene`'s old `continuityBodyRef`-for-selection trick — that ref still exists, now used only for the unrelated initial-camera-position calculation). Being inside `MoonDetailScene` (a mode-switch inside `SatelliteViewScene`, not its own level) when the Outliner selects something else needed one extra piece: a `useEffect` in `SatelliteViewScene` that pops back out of the focused moon whenever `inViewSelection` no longer matches it.
- **Colonies/Fleets/Starbases sections are permanently empty placeholders, not mocked-up fake entries** — same reasoning as `bodyStats.ts`'s heuristics and `ChatPlaceholder`: this project has no economy/fleet/starbase game systems yet, so inventing sample data would misrepresent actual game state. Replace with real data once those systems exist.
- **System and interstellar view's selection info panel moved off the fixed bottom-right `.star-info-panel` onto the same moveable `DraggableWindow` satellite view already used (`InspectPanel` for system view; a plain `DraggableWindow` with matching `.inspect-row` markup for interstellar, since stars don't carry `radiusKm` and don't fit `InspectableBody`'s shape).** The old fixed corner collided with the new `Outliner` (also bottom-right-ish); reusing the existing draggable window fixed that for free and, for system view, upgraded the panel's content for free too (radius/moon-count/size-class/habitability, previously only shown in satellite view) since all the data was already available (`PLANETS`, `getMoonsForPlanet`, `SUN_RADIUS_KM`). The old `.star-info-*` CSS classes were deleted (nothing referenced them anymore) rather than left dead.
- **`DraggableWindow`'s default anchor moved from dead-center (`left: 50%`) to lower-right-of-center (`left: 68%; top: 60%`)** — center blocked the very body you just clicked (you couldn't re-click a planet you'd just selected, or watch it while reading its panel), and collided with the `Outliner`'s right edge at some viewport widths. This is a single shared CSS rule, so it affects every `DraggableWindow` use (system/satellite/interstellar/moon-detail info panels) uniformly.
- **Deselecting (closing the panel, clicking empty space) now eases the camera back to the current view's "main body"**, instead of leaving it wherever it last pointed — Sol in system view, the primary body (always at local-origin) in satellite view. Implemented by making `SelectionTracker` run *unconditionally* (previously gated on `selectedName`/`selectedMoon` being truthy) with `getPosition` falling back to `ORIGIN` when nothing's selected — no new mechanism, just widening the existing lock-on's target to have a sensible default instead of "nothing." This is also what makes clicking a planet/the primary body again *after* focusing something else re-center correctly (e.g., clicking Earth's own marker right after Luna was focused): selecting the primary clears the moon/planet-specific selection, which falls through to the same `ORIGIN`/default-body target. The min-distance "zoom in close enough to enter" watchers stayed gated on an actual selection being present, since `enterSatellite(null)` or flying to a non-existent target would be wrong.
- **`NavBar` and `Outliner` are structural mirror images of each other** (`nav-sidebar`/`outliner` CSS is the same shape flipped left↔right: `left:0` vs `right:0`, DOM child order reversed so each panel's collapse-toggle tab always sits at the *inner* edge, closest to screen center, and each panel's content always touches the true screen edge). Intentionally duplicated CSS/structure rather than a shared abstracted component — the two are conceptually different enough (nation nav vs. celestial+asset outliner, different content shapes) that a shared component would need almost as many props/slots as just writing both out, and the codebase's existing convention is small purpose-built files over premature reuse.
- **`DraggableWindow`'s default anchor moved again, from `left:68%;top:60%` to `left:32%;top:55%`**, now that a left-side `NavBar` exists too — needs to clear *both* sidebars (`Outliner` on the right, `NavBar` on the left, each ~220px) without sitting dead-center over whatever body/category was just clicked. One shared position works for both use cases (celestial inspect panels and nav category placeholders) since both are "pops up somewhere in the middle, left-of-center" by the same reasoning. Values were tuned empirically against a live render, not derived exactly — revisit if either sidebar's width changes meaningfully.
- **The debug console's real access boundary is `import.meta.env.DEV` in `App.tsx` (`{import.meta.env.DEV && <DebugConsole />}`), not the backtick keybind.** This is a pure client-side app with no backend/auth, so "no player should have access to it" can only mean "not present in what ships to players" — a hidden-but-present feature is always eventually discoverable (view-source, devtools) in that setting. Vite replaces `import.meta.env.DEV` with the literal `false` at production-build time, and dead-code elimination then strips the whole `DebugConsole` branch — and everything it alone imports — out of the bundle, not just out of the rendered page. **Verified, not assumed**: after `npm run build`, `dist/assets/*.js` contains none of `DEBUG CONSOLE`/`Spawn Ship`/`DEV BUILD ONLY`/`No ships spawned`, while real player-facing strings (`Imperial State of Mars`, ship class names) are present as expected. **Known gap**: `.debug-console`'s CSS rules *do* still ship in `dist/assets/*.css` (Vite bundles the whole stylesheet regardless of which JS references a class name — CSS isn't tree-shaken by JSX className usage) — so a player could find the class names/colors in the shipped CSS, though without the JS there's no way to actually construct or use the tool from them. Not worth a PurgeCSS-style build step at this project's current size; revisit if that changes.
- **Ship data model gives every `ShipClass` an `ftlDrives: FtlDrive[]` array, not a single optional drive** — directly anticipates the stated near-term extension ("ships may be equipped with multiple drives, like a HyperWarp-Drive") without needing a shape change later: a future combined-drive hull just pushes both a `WarpDrive` and a `HyperDrive` into the same array. `reactionDrive: true` is a fixed literal (always present, every ship) rather than optional, since the brief was explicit that reaction drives are the sublight baseline every FTL-equipped ship also carries, not a distinct alternative some ships lack.
- **`WARP_SPEED_TIERS_C = [0.5, 1, 10, 100, 314]` is an interpretation, not a literal transcription** — the brief gave "0.5c, then 1c, then 10c, and so on until 314c" without spelling out every intermediate step. Read as order-of-magnitude jumps (×2, then ×10 each step) capped at a deliberately non-round final tier (314, a nod to π) rather than a clean 1000c. Not wired to an actual tech tree (none exists yet) — exists as real data for future research/progression systems to consume.
- **Spawned ships are static (`position` set once, at spawn time) and render unconditionally in `SolarSystemScene`** — no movement/travel simulation exists yet (that's a separate, larger unstarted roadmap item), and only Sol's system has any data to spawn "near" anyway, so there was nothing meaningful to scope ships *to* yet. Revisit both once other systems and ship movement exist.
- **The Outliner's "Fleets" section now shows real spawned ships, not a permanent placeholder** — unlike Colonies/Starbases (still empty; no such systems exist at all), ships are a real, populated concept the moment any are spawned, so showing them isn't inventing fake data the way a mocked-up fleet entry would be.
- **Ship travel is real-scale, computed in km using the exact same AU/ly-to-scene-unit ratios the rendering already uses**, not an arbitrary "feels right" speed. `REACTION_DRIVE_SPEED_KM_S = 3,000` (~0.01c, "normal thrusters") is the one genuinely invented number — chosen so a full system crossing takes weeks (believably slow) without pretending real propulsion-engineering precision this project has no tech-tree to justify; everything downstream of it (travel time = distance_km / speed_km_s) is real arithmetic, not tuned per-case. **Concrete numbers this produces** (useful for sanity-checking future tuning): Swift Courier (10c warp) crosses 1 AU in ~50 seconds and reaches Alpha Centauri (4.37 ly) in ~160 days; a pure reaction-drive ship takes ~17 days to cross 1 AU and would take centuries between stars (allowed, not blocked — just impractical, which is itself realistic).
- **`WARP_SPEED_TIERS_C`'s progression (`[0.5, 1, 10, 100, 314]`) is what actually determines travel time when a ship's tier changes** — a ship's `WarpDrive.speedC` is just one of those tier values; there's no separate "upgrade" mechanic yet since there's no tech tree to drive it, but the tiers exist as real, ready-to-consume data for when one does.
- **A single `MoveOrder` always plays out in one space only (system-AU or interstellar-ly), never both** — modeled by treating a system-to-interstellar (or reverse) crossing as instantaneous at the departure/arrival system's star (`shipPhysics.effectiveStart`). Real ships would spend time on that leg too; this project doesn't model it separately because the AU-scale distance is negligible next to light-year distances (Neptune's orbit, the widest thing in Sol's system, is ~30 AU ≈ 0.00047 ly — a rounding error against even the nearest star at 4.37 ly). Practical effect: the moment an interstellar order is issued, a ship "conceptually" leaves its system instantly and the whole visible travel happens in interstellar space (and symmetrically on arrival into a system). Revisit if a future feature (interception, blockades at a system's edge) needs that leg to be real.
- **A ship's destination is snapshotted at order-issue time, not continuously re-aimed at a moving target** (a 'body' order travels to where that body *was* when ordered, not an intercept-with-lead calculation) — deliberate, to preserve this codebase's established "everything is a pure function of simDays, never accumulated per-frame state" principle (see `getPlanetPosition`/`getMoonPosition`'s own doc comments). A true pursuit/intercept would need per-frame numerical integration since the target keeps moving, which every other piece of motion in this project specifically avoids. For fast FTL trips this is imperceptible; for a slow reaction-drive voyage to a fast-orbiting inner planet it's a known, acceptable simplification — once **arrived**, though, the ship's resting location becomes `orbiting` that body, which *does* live-track it going forward (same as the body itself), so it doesn't stay stuck at a stale point after arrival.
- **Ship selection lives in `shipStore` (global), not local scene state** — the explicit requirement was that switching views must not deselect a ship, and every scene's existing celestial-body selection is intentionally local `useState` (by design, per the original navigation work). Ship selection needed the opposite property, so it got its own global home rather than trying to retrofit the existing local pattern. Selecting a ship also clears the current scene's local body selection (one-directional — clicking a ship doesn't need the reverse plumbed through, see below); the two panels (`InspectPanel`/interstellar's `DraggableWindow` vs. `ShipPanel`) are mutually exclusive at render time, ship panel taking priority, so they never stack.
- **Scene membership (which of `SolarSystemScene`/`InterstellarScene` renders a given ship's marker) is computed from `ship.order.space` or `ship.location.kind` directly in the parent's render, not from a per-frame position check.** This only changes at two discrete moments — an order being issued or completing — both of which are `ships` array identity changes the parent already re-renders on via its zustand subscription; only the marker's on-screen *position within* its current scene needs continuous per-frame updates (handled inside `ShipMarker`'s own `useFrame`, same as `Planet.tsx`). Avoids subscribing whole scene components to `simDays` just to decide a list membership question that doesn't actually need frame-rate resolution.
- **`DeepSpaceClickPlane` needed the same `.planet-marker`/`.ship-marker` guard `onPointerMissed` already uses, at its own event level** — adding a full-coverage invisible mesh changed what counts as "a miss": clicks that used to raycast to nothing (triggering `onPointerMissed`, already guarded) now hit the plane instead, so the plane's *own* `onClick`/`onContextMenu` needed the identical target-check guard, or clicking a marker would immediately deselect/misfire an order via the plane racing the marker's own click. Found and fixed during implementation, not anticipated up front — worth remembering as a general rule: any new full-coverage raycast target added to a scene needs this same guard.
- **Right-clicking a body/star only makes sense with a ship already selected — a no-op otherwise (`if (!selectedShipId) return`), not an error or a menu.** No confirmation step, no path-preview, no cancel-order affordance yet — matches this feature's scope (make ships selectable/movable with real-scale math) rather than a full fleet-command UI, which would be a reasonable separate follow-up.
- **Known testing-environment caveat (not a code issue), root cause confirmed this round**: the sandboxed Browser-pane tab used for verification reports `document.visibilityState === 'hidden'` / `document.hasFocus() === false` during tool-driven interaction, which per spec suspends `requestAnimationFrame` — freezing the game clock and any in-flight `CameraFocusRig`/`SelectionTracker` animation mid-transition (confirmed via direct `document.visibilityState` inspection, not just inferred). Separately/additionally it can also lose its WebGL context (`THREE.WebGLRenderer: Context Lost`) on routine view-level remounts, reproduced even on unmodified pre-existing navigation paths. Both are artifacts of this specific automation environment, not the app — a real, focused browser tab keeps rAF running normally. Structural/DOM verification (`get_page_text`, direct DOM queries for rendered markers/panel fields/button gating) was used as a fallback wherever full animated-flight completion couldn't be observed live.
- **Context-loss caveat above got measurably worse this round**: in this session's sandbox instance, every 2nd+ `<Canvas>` mount within a tab lost its WebGL context deterministically — reproduced identically across two independent fresh tabs, and it silently prevents *all* of that Canvas's `Html`-portaled DOM (markers, badges) from ever mounting, not just blocking paint (confirmed: `.planet-marker` — pre-existing, untouched by this session's changes — also failed to mount under the same condition). Only the very first Canvas per tab reliably works. Live pixel/DOM verification of anything beyond the first view level (satellite-view ship markers, interstellar presence badges) had to fall back to code review + cross-validation of shared logic (e.g. `shipSystemId`) against what *did* verify live in the first-mounted view. Not a code regression — worth a fresh, un-navigated tab per feature being verified if this recurs.
- **Fleet allegiance (`player`/`friendly`/`neutral`/`hostile`) replaces per-`ShipClass` marker color everywhere.** A ship's class (hull/drives) doesn't imply who's flying it — the same Swift Courier could be player-owned or hostile — so allegiance lives on `ShipInstance`, not `ShipClass` (whose now-unused `color` field was removed rather than left dead). `ALLEGIANCE_COLORS` (`shipData.ts`) is the single source of truth every fleet-colored element reads from: `ShipMarker`, `SatelliteShipMarker`, the Outliner's fleet icons, and the interstellar presence badges. `DebugConsole` — currently the only ship-spawning path — got an allegiance selector (defaulting to `player`) so all four colors are actually reachable for testing, since nothing else assigns allegiance yet (no AI/multiplayer opponents exist).
- **Interstellar view gets no-text "presence badges" for fleets nested inside a system it can't otherwise show.** A ship orbiting Earth has no position at interstellar scale (only stars render there) and previously didn't render at all once it left interstellar space — `InterstellarScene` now computes a `Map<systemId, FleetAllegiance[]>` from `ships` (via the new shared `shipSystemId` helper) and each `StarNode` renders one small triangle badge per distinct allegiance color present in its system, with deliberately no name/count text (matches the explicit ask: "put a fleet icon next to Sol, but do not put additional text"). This is the complementary set to the existing `interstellarShips` filter — a ship is either genuinely in interstellar space (rendered directly, as before) or nested in exactly one system (rendered only as a badge), never both, so there's no double-counting.
- **A ship ordered to a body now actually renders once you enter that body's own satellite view — previously a real gap, not a placeholder.** `ship.location.kind === 'orbiting'` positions were only ever resolved in system-view AU-scale units (`bodyLivePosition + offset`); `SatelliteViewScene` didn't render ships at all, so a fleet "sent to Earth" was invisible the moment you actually looked at Earth up close — the one place "orbiting" should be most visible. Fixed by rendering matching resting ships (`!ship.order`, `location.bodyName === bodyName`) via the new `SatelliteShipMarker`, positioned with `shipPhysics.satelliteOrbitLocalPosition` — a stable hash-of-ship-id placement near the hologram, *not* a reinterpretation of the real AU-scale offset (which has no meaningful translation into this view's unrelated visual-radius scale) — same "compressed, not literal" spirit as `moonData.ts`'s orbit radii. A ship still mid-`order` (even one destined here) isn't resting yet, so it correctly doesn't show until it actually arrives; system view's own existing full ship rendering (already correct, already at the body's exact position) was left untouched.
- **Moon hologram restored — reverses the earlier "marker-only" decision.** `Moon.tsx` previously only rendered a flat marker dot (see the now-superseded note in Project Structure), on the reasoning that a moon's true shape should only appear in its own `MoonDetailScene`. Revisited because a moon like Luna — a quarter of Earth's own size — reading as a bare dot in Earth's satellite view understates it; `Moon.tsx` now renders an actual `HologramBody` sized to `moon.visualRadius` (already computed correctly from the real radius ratio, just never drawn) alongside the marker. Paired with a new `MAX_ORBIT_RADIUS = 12` clamp in `moonData.ts` — the unclamped log-compressed formula still put several major moons (Luna included, real ratio ~60 planet-radii out) beyond the satellite view's default camera framing even after being correctly sized, which would have undercut the same fix; the clamp trades exact relative ordering among the farthest few moons for all of them staying visible without hunting for them.
- **Ownership gates *commanding* a ship, not *selecting* one — and the two are enforced at different, deliberately separate chokepoints.** First pass this session wrongly conflated them (blocked selection itself in `shipStore.selectShip`); corrected once the actual requirement was clarified: players should be able to select/inspect any fleet, including hostile ones, and just not be able to move them. `selectShip` is back to unconditionally allowing any id — selecting is "look at its info," never privileged. The actual privileged action, planning a move, is gated once at `shipPhysics.planMove` (`if (ship.allegiance !== 'player') return { kind: 'not-owned' }`, checked first, before even the hyperdrive branch) — the single place every order-issuing call site (every scene's right-click handler, `DebugConsole`) already funnels through, so nothing needs its own ownership check. `ShipPanel` reads `ship.allegiance` directly to show "Not under your command." in place of the redirect hint for a non-owned selected ship (and now always shows an Allegiance row, since inspecting a foreign fleet's allegiance is itself useful info) — the marker/Outliner-level `.unowned`/`.unselectable` presentational treatment from the wrong first pass was removed entirely, since selecting is no longer restricted.
- **Fixed a real bug in cross-system move-order distance: a ship resting in interstellar space (e.g. at Alpha Centauri) ordered to a body inside a *different* system used to have its travel distance computed as if it already started at that system's own star — silently discarding the entire interstellar leg it actually needs to make first.** The old `effectiveStart` handled "landing in a system" by unconditionally returning `(0,0,0)`, which only happened to look correct because Sol is the only system with data today (so the target system's star always *is* the origin) — it never actually measured the distance from wherever the ship currently was. Replaced with a symmetric `interstellarAnchor(info)` (itself if already in interstellar space, else its own system's star) applied to *both* the current position and the destination whenever they're not already in the same space/system: the order becomes a single interstellar-space leg between the ship's real current star and the destination system's real star (the negligible in-system AU hop at each end stays free, same "system↔interstellar crossing is instantaneous at the star" simplification this project already uses — just now anchored to the *correct* pair of stars instead of assuming the ship is already there). Verified directly against `planMove` (bypassing the sandbox's WebGL/rAF limitations entirely, since it's a pure function): a player ship resting at Alpha Centauri ordered to Earth now correctly resolves to `space: 'interstellar'`, a ~4.37 ly leg, and `arrivalSimDays` ≈ 159.6 days at Warp 10c (matching `4.37 ly / 10c` exactly) — previously this would have been near-instant, measuring only Earth's ~1 AU distance from Sol's origin. Same-system orders (verified Mars→Earth) and the ownership gate (verified via `{kind:'not-owned'}`) both confirmed unaffected by the rewrite.
- **Ship camera-focusing now works the same way a planet's does, gated behind a new top-HUD "lock-on" toggle (`viewStore.lockOnEnabled`, default on).** Previously selecting a ship opened `ShipPanel` but never engaged `SelectionTracker` — planets/stars/moons always had "select = camera eases onto it," ships had nothing. Extended every scene's existing `SelectionTracker.getPosition` with a ship branch (only tracking a ship that's actually a member of that scene's own rendered-ships list — `systemShips`/`interstellarShips`/`orbitingShips` — same "only lock onto what's actually here" reasoning `trackedShip` uses everywhere), and made the "chase the current selection" part of every `getPosition` conditional on `lockOnEnabled`, while leaving each scene's no-selection default (Sol/primary body, `ORIGIN`) always active regardless — that default is navigation plumbing (what the exit-via-zoom-out threshold measures against), not "following a selection," so toggling lock-on off shouldn't break leaving the view. **Gotcha caught while wiring this up**: every scene's *implicit* "zoom in close enough to enter the next view down" `DistanceThresholdWatcher` (system→satellite, interstellar→system, satellite→moon-detail) measures distance from the same tracked target — with lock-on off, that target stays parked at the scene's default (not the actual selection), so an ungated watcher would trigger off proximity to the *wrong* thing (e.g. zooming into Sol would wrongly enter a selected-but-not-tracked planet's satellite view). Fixed by gating all three of those watchers on `lockOnEnabled` too — the explicit buttons ("Detailed View," "Enter System") are completely unaffected either way, this only concerns the zoom-in shortcut.
- **Found and fixed a real visual bug in `NavBar`/`Outliner`'s collapse-toggle animation: their long text content visibly wrapped down to single clipped characters while briefly squeezed into the panel's narrow in-between width during the 0.2s CSS width transition.** Root-caused by deliberately reproducing it (injecting a temporarily slowed `transition-duration` via devtools and screenshotting mid-animation) rather than guessing from the user's screenshot alone — confirmed it's a transition-timing artifact (this specific symptom didn't appear at rest, at any viewport width tried from 400px to 1280px — see below for a second, separate bug that *does* show at rest). `NavBar`'s longer labels (`"Imperial State of Mars"`, `"Fleet Management"`) made it far more visually obvious than `Outliner`'s shorter ones, but both share the identical underlying risk (same `flex: 1; width: 191px` content pattern). Fixed with an intentionally asymmetric opacity transition on both `.nav-sidebar-content`/`.outliner-content`: fading out has no delay (content hides before the panel has narrowed far enough to clip), fading in is delayed until the width transition is mostly done (content only reappears once the panel is already wide) — collapsing now shows nothing instead of garbled text, and expanding reveals clean, already-wide content instead of a similar glitch in reverse. Verified by replaying the exact repro after the fix and confirming the garbled frame no longer appears.
- **Follow-up report ("no arrow, too thin") surfaced a second, deeper bug in the same two sidebars — this one persistent in the fully-collapsed *resting* state, not just mid-transition.** `.nav-sidebar-content`/`.outliner-content`'s `padding: 12px 10px` (20px horizontal total) never actually shrinks away even at `flex-basis: 0%`, because padding isn't absorbed by flex-shrink under default `content-box` sizing — the content box has a hard 20px floor regardless of how much the flex layout wants to shrink it. In the fully-collapsed 28px container, that 20px plus the toggle's fixed 28px (`flex: 0 0 28px`, can't shrink) sums to 48px, overflowing the 28px container by 20px. Both sidebars have this identical underlying bug (confirmed by measuring `getBoundingClientRect` on both content divs and toggle buttons directly, not by eyeballing screenshots) — but it only *looked* broken on `NavBar`: since content is first in DOM/flex order and both sidebars overflow rightward, the overflow pushes `NavBar`'s toggle (second in flex order, left-anchored container) out past its own visible right edge, clipping the arrow glyph almost entirely away, while `Outliner`'s toggle (also second in flex order, but right-anchored) stays within its visible region by coincidence — the overflow instead pushes the now-invisible (opacity 0) *content* remnant out of view on that side. Fixed by dropping `padding: 0` alongside the existing `opacity: 0` in both `.nav-sidebar.collapsed .nav-sidebar-content` and `.outliner.collapsed .outliner-content` — content now genuinely shrinks to 0 width when collapsed instead of just becoming invisible while still occupying space, so the toggle sits exactly where the 28px container expects it on both sides. Verified via `getBoundingClientRect`: both toggles now land flush within their sidebar's visible bounds.
- **Ship status text now includes an ETA while travelling** (`"En route to Alpha Centauri — ETA 3 MAY 2601"`), using the exact same date format `TimeControls` already shows for the current date — `formatDate`/`MONTHS` moved out of `TimeControls.tsx` and into `gameTimeStore.ts` (alongside the pre-existing `simDaysToDate`) specifically so `shipPhysics.getShipStatusText` could reuse the same formatting without a UI-component file depending on another UI-component file for a pure string-formatting helper. Verified live: paused the clock, ordered a ship to Alpha Centauri, confirmed the exact ETA string renders (and that a same-system order, e.g. to Mars, resolves near-instantly at warp 10c — real enough that watching it "en route" requires either pausing or a genuinely interstellar destination).
- **A ship resting in orbit no longer shows a name label — Stellaris-style, just the triangle** (`ShipMarker`'s new `isOrbiting = !ship.order && ship.location.kind === 'orbiting'` gate; `SatelliteShipMarker` drops the label unconditionally, since every ship it renders is orbiting by construction). A ship still travelling, or resting at a bare star/point, keeps its label — there's nothing else nearby there to make it obvious which ship it is, unlike a body's orbit where the label was mostly just clutter (and, per the report that prompted this, prone to a garbled/overlapping look at some zoom levels). No information is actually lost: `ShipPanel` and the Outliner's Fleets list both still show the name on selection.
- **Interstellar presence badges (see above) are now clickable to select the fleet they represent, not just decorative.** Required changing `fleetPresenceByStar` from `Map<string, FleetAllegiance[]>` (just the colors) to `Map<string, ShipInstance[]>` (one representative ship per distinct allegiance color) so a click has an actual ship id to select — same dedupe-by-color simplification as before, so if multiple ships share a color in one system, the badge selects whichever was found first. Each badge's `onClick` calls `e.stopPropagation()` before selecting the ship, since it's nested inside the star marker's own clickable div and would otherwise also select the star. Verified live: clicking the badge selected the resting ship (confirmed via `ShipPanel`'s status text, distinct from a second, actually-travelling ship) and did not open the star's own inspect window.
- **Fixed a real bug: a ship arriving at a body or a star rendered exactly on top of that body/star's own marker, indistinguishable from it.** `resolveArrivalLocation`'s 'body' case hardcoded `offset: [0, 0, 0]` (only the *spawn* path, DebugConsole's own manual ring of offsets, ever gave a resting ship any separation) and the 'star' `ShipLocation` variant had no offset field at all. Fixed by adding a shared `restingOffset(shipId)` (0.6-unit hash-of-id nudge, same technique as `satelliteOrbitLocalPosition`, factored out into `hashAngleRad`) applied at every place a 'star'/'orbiting' `ShipLocation` gets constructed on arrival: `resolveArrivalLocation` (now takes `shipId`) and `planMove`'s hyperdrive-instant branch. `ShipLocation`'s 'star' variant gained an `offset` field to match 'orbiting', and `resolveLocation`'s 'star' case now actually applies it. Verified via direct `resolveArrivalLocation` calls (bypassing the sandbox's rendering limitations, since it's a pure function): both cases now return a non-zero ~0.6-unit offset instead of sitting dead-center.
- **Fixed a real bug: right-clicking in satellite view never issued a move order — no handler existed at all.** Every other view (system, interstellar) has right-click-to-order wired on its body/star markers; `SatelliteViewScene` never got the equivalent for its own primary body. Added `handleOrderToPrimary` (identical `planMove`+`setShipOrder` pattern to `SolarSystemScene`'s `handleOrderToBody`, targeting this scene's own `bodyName` prop) and wired it via a new optional `onOrderTo` prop on both `FocusableMarker` (the label) and `HologramBody` (the sphere itself, via `onContextMenu` on its existing invisible click-catcher mesh — r3f meshes support `onContextMenu` directly, same as `DeepSpaceClickPlane`) — either one right-clicked issues the order, matching the redundancy other markers already have between their Html label and clickable geometry. Moons still have no move-order target (unchanged, a moon isn't a valid `MoveDestination` yet). Verified live (had to route around this session's `requestAnimationFrame`/React-scheduler throttling under `document.hidden` by writing `viewStore` state directly instead of waiting on the animated camera flight — the delay was purely React's re-render being deferred, not a broken write, confirmed by re-reading the store immediately after and seeing it already correct while the DOM hadn't caught up yet): selected a ship, right-clicked Earth's label from inside Earth's own satellite view, watched its status go from "orbiting Sol" to "Arriving…".
- **Warp drives now have a 5-day cooldown after completing a jump (`WarpDrive.cooldownDays`, mirroring `HyperDrive`'s existing shape rather than a bare global constant) — but unlike hyperdrive's cooldown, it doesn't block the ship, it just falls back to reaction-drive speed for that trip.** Hyperdrive-on-cooldown refuses the order outright because a hyperdrive-only ship has no other reasonable way to cross interstellar distances; a warp-capable ship always has reaction drive as a (slower) fallback, so blocking it entirely would just be a confusing dead click — especially since `'on-cooldown'` results are already silently un-surfaced everywhere (a pre-existing gap for hyperdrive, not worth compounding for the much more common warp case). `MoveOrder` gained a `usedWarp: boolean`, set by `planMove` (`!!warpDrive && simDays >= ship.warpReadySimDays`) and read once at arrival by the new `warpCooldownAfterArrival` helper — recorded on the order rather than re-derived from the ship's class at arrival time, since "has a warp drive" no longer implies "used it for this specific order" once cooldown can force a reaction-drive fallback. `ShipInstance` gained a parallel `warpReadySimDays` field, and `shipStore.setShipLocation`'s trailing cooldown param became a `{ hyperdriveReadySimDays?, warpReadySimDays? }` object (was a bare `hyperdriveReadySimDays?: number`) now that two independent cooldowns can update at arrival. `describeFtlDrive` now prints `"Warp Drive (10c, 5-day cooldown)"`, surfacing the mechanic in `ShipPanel`/`DebugConsole` the same way hyperdrive's cooldown already was. Verified via direct `planMove` calls: a ship on warp cooldown still gets a valid order, ~1000x slower (matching the 10c/0.01c speed ratio) and `usedWarp: false`; `warpCooldownAfterArrival` on a just-completed warp order returns exactly `arrivalSimDays + 5`.
- **The `restingOffset` fix above wasn't actually enough — a fixed *world-space* nudge shrinks to nothing on screen at typical camera distances, so ships still visually sat on top of their body/star's marker.** 0.6 world-units is barely a couple of screen pixels once the camera is far enough back to see a whole system (the default view), which is exactly when the overlap is most visible and most confusing. Fixed with a `.ship-marker { transform: translate(4px, -18px) }` — a screen-space CSS offset, so separation from the anchor marker is now guaranteed at *any* zoom level, not just close-up ones. Kept `restingOffset` too (harmless, still gives real separation between multiple ships at closer zoom) rather than replacing it — the two work at different scales, not in conflict. Verified via `getBoundingClientRect`: before this fix, a ship at Sol measured ~1px from Sol's own marker at the default camera distance; after, ~18px.
- **Fixed a real gap the satellite-view right-click fix immediately made much easier to hit: an order completing while the player stayed in a view that doesn't mount that ship's `ShipMarker` never settled** (order never cleared, location never updated) — previously "a known gap, not fixed" per this doc's own prior note, tolerable back when there was no way to issue an order from inside satellite view at all. Settling used to happen only inside `ShipMarker`'s own per-instance `useFrame`, which only runs for ships whose marker is actually mounted in the currently-active `<Canvas>` — exactly the case that's now common (order Earth's satellite view; nothing in that scene renders a still-travelling ship). Fixed by moving settling out of `ShipMarker` entirely into a new `useShipOrderSettler` hook, mounted once in `App.tsx` outside any Canvas (same reasoning as the pre-existing `useGameClock`) — it subscribes directly to `useGameTimeStore` rather than running its own `requestAnimationFrame` loop, since `useFrame` isn't available outside a Canvas and it only needs to react to simDays actually advancing, not render anything. Verified live: issued an order, watched `ShipPanel`'s status go from "orbiting Sol" to "orbiting Mars" with the old per-marker mechanism *removed* from `ShipMarker.tsx` entirely — the settle could only have come from the new global hook.
- **Reduced the initial/fresh-arrival camera distance in system view (`FAR_START`) ~4.4x closer** — the old distance (~10,500 units, tuned for how *leaving* the system should feel, a "shrink to a dot" effect) put every planet within a tiny, illegible cluster near the center of frame on first load, which is also every fresh arrival's framing (interstellar fly-in, breadcrumb). Same viewing angle, just closer, landing at roughly 2.4x Neptune's own orbit radius (~600 units) — enough to still read as "the whole system," planets now individually legible instead of overlapping. Verified via marker `getBoundingClientRect`: Sol-to-Neptune screen separation went from a handful of pixels to ~196px on a 1280px-wide viewport.
- **Added a "Go To" button (`ShipPanel`) and real orbital motion for resting ships, both requested together.** "Go To" reuses `CameraFocusRig` exactly as planets/stars already do for their own fly-ins, but with `onArrive` doing nothing view-level (no `enterX` call) — a ship has no deeper view to transition into, this is purely "move the camera here, once," independent of `lockOnEnabled` (which only governs *continuous* follow via `SelectionTracker`). Each scene (`SolarSystemScene`/`InterstellarScene`/`SatelliteViewScene`) gets its own `flyingToShip` boolean, separate from that scene's existing fly-to-body state (`flyingToName`/`focusedId`/`flyingToMoon`) since arriving at a ship must *not* trigger a level transition the way arriving at a body does — and gates `OrbitControls`/`SelectionTracker`/the enter-next-level `DistanceThresholdWatcher`s on it exactly the same way those existing fly states already are. The button itself only renders when the selected ship is actually trackable in the current scene (reusing each scene's existing `trackedShip`, the same check that already gates continuous lock-on) — no button when there's nowhere to fly to.
- **Ships resting in orbit now have real orbital motion — a continuous circular orbit, not a static parking spot — in both system view and satellite view, using the *same* underlying angular motion in each.** Realized real planet radii are near sub-pixel at true AU scale (Earth ≈ 0.00085 scene units) — an "orbit radius" for a ship was never going to be a literal distance the way a moon's is, so, like `MOON_TIME_DILATION`, this is a deliberately picked default, not a derived one: `DEFAULT_SHIP_ORBIT_PERIOD_DAYS = 4`, and a radius chosen *per view* rather than stored once — system view's `SYSTEM_SHIP_ORBIT_RADIUS = 0.15` (scene units, sized to read as a visible "close orbit" against any planet's near-invisible true radius) and satellite view's pre-existing `primaryVisualRadius + 1.2` (hologram-local scale) are unrelated numbers for the same reason `restingOffset` earlier in this doc needed two different treatments for two different scales. What *is* shared and stored on `ShipLocation`'s 'orbiting' variant is the time-basis (`periodDays`, `phaseDeg`) — genuine ship state, not a rendering constant — so a ship's satellite-view orbit is visibly "the same orbit," just at a different zoom, not an independently-hashed placement like the old static offset was. Reused `orbitMath.ts`'s existing `getOrbitPosition`/`angleForYear` (the same functions driving every planet/moon) rather than inventing new circular-motion math. `SatelliteShipMarker` needed restructuring to match — it used to receive a static `position` prop computed once by its parent; now it drives its own `useFrame`-updated `groupRef`, the same pattern `ShipMarker`/`Moon`/`Planet` already use for continuous motion. Verified via direct calls to both position functions: satellite view's position rotates exactly 90° after 1 simDay (1/4 of the 4-day period) — the same angular rate system view's position is *also* driven by, confirmed live via a "Go To" flight landing the camera right next to the ship's marker, then watching that marker's screen position visibly shift over a few seconds.
- **Found and fixed a real bug the "Go To" feature exposed: a ship-selection "Go To" flight could leave the camera close enough to a *stale* body/star/moon selection that a completely unrelated auto-navigation watcher fired right after arrival — jumping the player into that stale selection's satellite/system/moon view with no zoom gesture on their part at all.** Root cause: selecting a ship (`shipStore.selectShip`) deliberately never clears a scene's own body/star/moon selection (`viewStore.inViewSelection` and friends) — a long-standing, correct design choice (switching what's selected for one purpose shouldn't erase the other) — but every scene's "zoom in close enough → auto-enter the next view down" `DistanceThresholdWatcher` only ever checked *that* stale selection, never whether a ship was *also* currently selected (and thus the actual reason the camera had just moved). A ship resting near that stale selection (very plausible — Sol is both the most commonly pre-selected body and a common ship destination) made this easy to trigger via "Go To," but the same class of watcher could misfire any time the camera happened to end up close to a stale target for an unrelated reason. Fixed by adding `!selectedShipId` to all three watchers (`SolarSystemScene`'s `ENTER_SATELLITE_DISTANCE`, `InterstellarScene`'s `ENTER_SYSTEM_DISTANCE`, `SatelliteViewScene`'s `ENTER_MOON_DISTANCE`) — while a ship is selected, none of them should be interpreting proximity as "the player is zooming in on a body" at all. This also turned out to explain a second, separately-reported symptom ("ships stuck in arriving when ordered to a star system from interstellar view") — ordering a ship *to* Sol specifically (the one star with system data) while `selectedStar` was stale-set to Sol meant the old `ENTER_SYSTEM_DISTANCE` watcher was live and armed for the ship's *entire* interstellar transit, since `SelectionTracker` tracks a selected, in-scene-traveling ship continuously; it could fire well before actual arrival (4.5 scene-units ≈ 0.56 ly is still a large fraction of most trips), bouncing the player into System view where the still-travelling ship isn't rendered by anything — reading as "stuck," though the order itself was never actually broken. Verified live: reproduced the exact setup (stale `selectedStar = 'sol'`, ship ordered to Sol) and confirmed the view now stays on Interstellar for the entire transit, with the ship correctly settling to "In Sol System" on arrival — confirmed independently that the core settle mechanism (`useShipOrderSettler`) itself was never at fault by watching a from-scratch order resolve correctly on its own.
- **Added "jump when ready" for hyperdrive: ordering a jump while the drive is still on cooldown now queues it to fire automatically once ready, instead of the order being silently dropped** (the pre-existing, documented behavior for `'on-cooldown'` results). `ShipInstance` gained `pendingHyperdriveJump: string | null` (the queued target starId) and `shipStore` a `setPendingHyperdriveJump` action; `InterstellarScene`'s `handleOrderToStar` calls it on the `'on-cooldown'` result instead of the previous silent no-op. Firing happens in `useShipOrderSettler` (already the single place reacting to simDays advancing regardless of which view is mounted — see above) — once `simDays >= ship.hyperdriveReadySimDays`, it re-runs `planMove` for the queued star and applies the (now-instant) result, or drops the queue if something changed and the ship can no longer make the jump. Issuing any new order/location — including a fresh jump attempt — clears any pending queue, since a ship should only ever have one destination in mind. `getShipStatusText` surfaces the queued state directly ("Hyperdrive charging — jump to Sirius queued (27.0d)") rather than looking idle while queued.
- **Added a cooldown display to `ShipPanel`** — a "Cooldowns" row showing `Hyperdrive Xd`/`Warp Xd` (or `Ready`) for whichever drives the ship's class actually has, via new `hyperdriveCooldownRemainingDays`/`warpCooldownRemainingDays` helpers in `shipPhysics.ts`. Both this and "jump when ready" were verified together, live, through a full controlled cycle: paused the clock to get exact timing control (real time otherwise keeps advancing during tool-call latency, not just explicit waits — confirmed by watching the in-game date jump six months between two "instant" checks), confirmed an immediate second jump attempt correctly queues (`"queued (27.0d)"`) rather than double-firing, then sped up and watched the queued jump fire on its own with no further input, landing the ship and resetting the cooldown display to `Ready`.
- **Warp drives got four real behaviors this session — direction-change stop-and-start, opt-in "warp when ready," a player-toggleable "use warp at all" switch, and gravity-well gating — all four unified into one two-phase order model rather than four separate mechanisms.** Explicitly out of scope: hyperdrives, untouched entirely.
  - **Mechanism**: `MoveOrder` gained three optional fields — `warpEngageSimDays` (when a two-phase order actually switches from reaction to warp speed), `warpEngageFraction` (the *distance* fraction, not time fraction, already covered at that switch — stored directly since the two phases move at very different rates, cheaper than re-deriving it in the render path), and `gravityWellClearSimDays` (present purely for status-line text, distinct from a cooldown wait even though both resolve through the same `warpEngageSimDays` gate). `getShipRenderPosition` now interpolates each phase on its own local timeline when these are set, falling back to the original single-span lerp when they're not (a plain reaction-only or plain full-warp order never sets them).
  - **Direction-change stop-and-start**: a new `isShipCurrentlyWarping(ship, simDays)` helper (true once `simDays >= order.warpEngageSimDays`, or immediately for a single-phase warp order) is checked first in `planMove`, before anything else — redirecting a ship actually mid-warp-leg forces `warpReadySimDays = simDays + cooldownDays` *right now*, returned via a new `warpReadyOverride` field on the `'order'` `MoveResult` (parallel to `'instant'`'s existing `hyperdriveReadySimDays`) and applied atomically by `shipStore.setShipOrder`'s new optional third param — no frame where the old order is gone but the penalty isn't in effect. A redirect during the reaction-drive or leaving-gravity-well portion of an order (order exists but isn't warping yet) never engaged the drive, so correctly incurs no penalty.
  - **"Use Warp Drive" / "Warp When Ready"**: two new persistent `ShipInstance` booleans (`warpEnabled`, default `true` — preserves old behavior unless turned off; `warpWhenReady`, default `false` — an opt-in), toggled via two checkboxes in `ShipPanel` (gated on `hasWarp && owned`, same ownership gate as commanding at all) and new `shipStore.setWarpEnabled`/`setWarpWhenReady` actions. `warpEnabled` false always short-circuits to a pure reaction-drive order, full stop — no gravity-well/cooldown logic even runs. `warpWhenReady` only gates whether a *cooldown* wait mid-flight is worth taking (if false, cooldown-blocked trips just ride reaction the whole way, same fallback this project already had before warp had a cooldown) — it never gates the gravity-well wait, which is mandatory physics, not a timing preference.
  - **Gravity-well gating**: a ship at rest in orbit (`!ship.order && ship.location.kind === 'orbiting'`) is inside that body's gravity well and can't warp from there — `planMove` forces a mandatory `GRAVITY_WELL_ESCAPE_DAYS = 1` (new constant in `shipPhysics.ts`, same "pick a legible default, not a literal one" heuristic as `SYSTEM_SHIP_ORBIT_RADIUS`/`MOON_TIME_DILATION` — there's no real orbital mechanics behind "how long escape takes") of reaction-only travel before warp can engage, regardless of `warpWhenReady`. A destination close enough that reaction drive alone would arrive before that escape window elapses just never triggers the gravity-well phase at all (correctly falls through to a plain short reaction-only trip). A ship already mid-order (any phase) is by definition already out of any gravity well, so a redirect never re-triggers this — deliberately not modeling "still escaping, redirected before clearing" as a compounding case. **Superseded next session** — the flat `GRAVITY_WELL_ESCAPE_DAYS` constant described here was replaced with a per-body, real-gravity-derived value; see the entry below.
  - **Status text**: `getShipStatusText` now branches on the order's phase — `"Leaving gravity well — reaction drive (warp in Xd)"` while `simDays < gravityWellClearSimDays`, `"En route to X — reaction drive (warp in Xd)"` while past that but still before `warpEngageSimDays` (the pure-cooldown-wait case), falling through to the original `"En route to X — ETA <date>"` once warp has actually engaged (or for any order that never had a warp phase at all).
  - **Verified live**, end-to-end, all four behaviors, using the debug console to spawn ships with warp ready-from-spawn (`warpReadySimDays: 0`) for controlled testing and the game clock's pause/speed controls for precise timing: (1) a freshly-orbiting ship ordered into deep space showed `"Leaving gravity well — reaction drive (warp in 1.0d)"`, then engaged warp and settled with the 5-day cooldown correctly already counting down; (2) a ship warping toward Ross 154, redirected mid-leg toward Alpha Centauri, immediately showed `Warp 5.0d` in the Cooldowns row (not `Ready`) and — with "Warp When Ready" left off — proceeded at reaction-drive speed for the *entire* remaining trip (a centuries-long ETA, matching `REACTION_DRIVE_SPEED_KM_S` over light-year distances, same magnitude this doc already noted for reaction-only interstellar travel); (3) toggling "Warp When Ready" and reissuing the same order showed `"reaction drive (warp in 5.0d)"`, and advancing the clock past that point showed the ETA collapse from centuries to months and the status line drop the "reaction drive" qualifier — confirmed the mid-flight engage actually fires, not just gets computed; (4) unchecking "Use Warp Drive" on a warp-*ready* ship and ordering it across an interstellar distance produced a millennium-scale ETA, confirming the drive is skipped entirely regardless of readiness.
- **Five requests landed together this session, all touching the fleet/ship layer: ships now orbit a star's own system on arrival, gravity-well escape time is derived from real per-body gravity instead of a flat number, a ship can be ordered to follow another ship, hyperdrive jumps carry real risk and chart hyperlanes, and two new scout ship classes exist.** Each is detailed below; none touch hyperdrive's core mechanics beyond the new risk roll (explicitly out of scope: "change nothing about hyperdrives" beyond that).
  - **Ships ordered to a star now arrive orbiting that star's own system, not resting beside it at interstellar scale.** Previously `resolveArrivalLocation`'s `'star'` case always produced a `{kind:'star', offset}` resting spot, regardless of whether that star actually had a system — meaning a ship ordered to Sol from interstellar view (the most common case) never actually settled *into* Sol's system, just floated next to its interstellar-view marker forever. Now it checks `star.hasSystemData`: if true, the ship settles into `{kind:'orbiting', systemId: star.id, bodyName: star.name, ...}` — literally orbiting the star as a system-view body, exactly like orbiting any planet (a new shared `orbitingLocation(systemId, bodyName, shipId)` helper factors out what used to be only the `'body'` case's logic, now reused by both). A star with no system data (every star but Sol today) keeps the old resting-beside-it behavior — there's nothing to orbit *into* yet. Applies uniformly to both arrival paths that can target a star: warp/reaction continuous orders (settled by `useShipOrderSettler`) and instant hyperdrive jumps (previously hyperdrive's branch in `planMove` constructed its own `{kind:'star',...}` literal directly — now calls `resolveArrivalLocation` too, so both paths share one source of truth). A ship now nested this way stops rendering as a full `ShipMarker` in `InterstellarScene` and instead shows only as a presence badge next to that star, same as any ship orbiting a planet — this is what actually satisfies "the orbit shouldn't be visible/moving around in the wrong view": `InterstellarScene`'s existing `isShipInInterstellarSpace`/`fleetPresenceByStar` split (a ship is either genuinely in interstellar space, rendered directly, or nested in exactly one system, rendered only as a static, non-animated badge — see the allegiance-badges entry above) already enforced this separation before this feature existed; extending "orbiting-a-star" to reuse the exact same `'orbiting'` location kind as "orbiting-a-planet" means it falls under that same pre-existing badge/marker split automatically, with no new isolation logic needed. Verified live: ordered a Turing Scout from Ross 154 back to Sol, watched its status go from `"At Ross 154"` through `"Hyperdrive charging — jump to Sol queued"` (cooldown) to `"In Sol System, orbiting Sol"`; confirmed only a small badge (not an animated marker) shows next to Sol's own marker in interstellar view the whole time; entered Sol's system view in a separate browser tab (see WebGL caveat below) and confirmed a resting-orbit marker renders and animates there, same as any planet-orbiting ship.
  - **Gravity-well escape time (see the warp-drive entry above) is now derived per-body from real mass/radius data, not a flat 1-day constant.** `planetData.ts`'s `PlanetData`/`RawPlanet` and `starData.ts`'s `StarData` both gained a `massKg` field (real published values — Sol's own `radiusKm`/`massKg` were added too, since a ship "orbiting" the Sun itself, or resting beside any star, is now also gravity-well-gated, extending the check beyond just planets: `gravityWellBody(location)` returns real mass/radius for both `'orbiting'` and `'star'` location kinds). The actual physical quantity used is **escape velocity** (`v = sqrt(2GM/r)`, computed at the body's real surface) rather than surface gravity — the more physically apt "how hard is this well to climb out of," and a fixed, real property of the body alone, independent of the fictional radius a ship's orbit is actually drawn at (`SYSTEM_SHIP_ORBIT_RADIUS`). `gravityWellEscapeDays(massKg, radiusKm) = EARTH_GRAVITY_WELL_ESCAPE_DAYS * (v_body / v_earth)` — only the *ratio* to Earth's own escape velocity is physically grounded; the `EARTH_GRAVITY_WELL_ESCAPE_DAYS = 1` anchor itself is still a picked, not derived, baseline (same "real constants, heuristic time mapping" pattern as `REACTION_DRIVE_SPEED_KM_S`/`LY_IN_KM`). Verified via direct `planMove` calls: Mercury (small, low gravity) → 0.38d, Earth → 1.00d (by construction), Jupiter (a real gas giant, ~5.3x Earth's escape velocity) → 5.38d — matching the expected ratio closely. Sol's own value (surface escape velocity ~617 km/s, ~55x Earth's) is large (~55 days) — a deliberate consequence of using a star's true surface, not adjusted down, since the reasoning ("real data in, heuristic time out") applies uniformly; worth revisiting if that specific number reads as too punishing once players actually hit it in practice.
  - **A selected ship can now be ordered to follow another ship** (`ShipInstance.followingShipId`) — right-clicking ship A while ship B is selected sets up B following A, via a new `onOrderFollow` prop wired through `ShipMarker`/`SatelliteShipMarker`'s `onContextMenu` (previously ships had no right-click handler at all) and a `canFollow(ship, targetId)` guard (same ownership rule as commanding at all, plus "can't follow itself") in `shipPhysics.ts`. Deliberately **not** a continuous real-time position-chase — there's no clean way to express "intercept a moving point" as a fixed-arrival `MoveOrder` the way every other order in this codebase works, and constantly recomputing every real-time frame would fight the warp stop-and-start/gravity-well logic added earlier this session (every tiny position delta would read as "changed direction"). Instead, a follower tracks its leader's **intended destination** — re-targeting only when that destination actually changes (a new order issued to the leader, or the leader arriving somewhere new), via a new `restingDestinationOf(location)` (the exact inverse of `resolveArrivalLocation` — turns a resting `ShipLocation` back into the `MoveDestination` that would land a ship there) and `destinationsEqual(a, b)` (a cheap string-key comparison, `destinationKey`, kept private). This check and the actual re-planning live in `useShipOrderSettler` (already the one place reacting to every simDays tick regardless of which view is mounted — see its own earlier entry) rather than anywhere view-specific, so following works identically no matter which scene the player is looking at. A **manual** order or instant hyperdrive jump issued to a following ship cancels the directive (same as it already cancels a pending hyperdrive jump) — `shipStore.setShipOrder`/`setShipLocation` both gained a `keepFollowing` param, defaulting to `false`; only `useShipOrderSettler`'s own calls (order-arrival settle, a queued hyperdrive jump firing, and the follow-recompute itself) pass `true`, since none of those are the player overriding the directive. `ShipPanel` shows a "Following: `<name>` [Stop]" row when set. Verified live across two ships (Mars→Earth chase, then redirecting the leader again to Sol): the follower correctly settled into the *same* resting spot as its leader each time — including, notably, ending up **orbiting Sol** once the leader hyperdrive-jumped there, since a follower's re-planned destination is a completely ordinary `MoveDestination` that goes through the exact same arrival logic as any player-issued order (including this session's own star-orbiting feature above) — nothing follow-specific had to be added to make that work, it fell out of reusing the same machinery. Also caught, mid-testing, a real one-off mistake worth noting: a resting, orbiting ship's marker (see the earlier "no name label" entry) shrinks to just its triangle glyph with no label — right-clicking near but not exactly on that small glyph silently misses it (no error, just nothing happens); confirmed by checking `ShipPanel`'s new "Following" row didn't appear after a miss, then landing precisely on the glyph and seeing it appear immediately.
  - **Hyperdrive jumps now carry real risk of losing the ship, and successful jumps chart a hyperlane.** `planMove`'s hyperdrive-instant branch, once past the existing cooldown check, now rolls against `hyperdriveLossChance(hyperDrive, laneEstablished)`: `HYPERDRIVE_BASE_LOSS_CHANCE = 0.5` normally, dropping to `HYPERDRIVE_ESTABLISHED_LANE_LOSS_CHANCE = 0.1` once a hyperlane already connects the two specific systems — both plain exported named constants in `shipData.ts` (not inlined anywhere), specifically so a future tech system has one obvious place to adjust them, per the explicit ask for "a framework for increasing/decreasing this later." A losing roll returns a new `'lost-in-hyperspace'` `MoveResult` — every caller (both scene handlers and `useShipOrderSettler`'s queued-jump-firing branch) responds by calling `removeShip`, same as any other ship removal; there's no toast/log system in this project yet to announce it beyond the ship simply vanishing from the Outliner/markers, a known, documented gap rather than a silent one. A new `useHyperlaneStore` (plain array of sorted `"a::b"` keys, `hasHyperlane`/`addHyperlane`) tracks established lanes; `planMove` reads it via `getState()` (its first store dependency — previously pure/store-independent, a deliberate small architectural shift, justified since lane state is genuinely global game state the physics layer needs to read, and `Math.random()` already made this one branch non-deterministic regardless) but never writes to it — writing is the caller's job, via a new optional `hyperlaneEstablished: [string, string]` field on the `'instant'` result, same "physics computes, caller applies" split every other result kind already follows. The **departure** star for a lane is derived by a new `hyperlaneOriginStarId(ship, simDays)` — the ship's current system (if inside one; a system's own id doubles as its star's id, e.g. `SOL_SYSTEM_ID === 'sol'`) or the star it's resting beside (if out in interstellar space); a ship resting at a bare point, or currently mid-order, has no clean anchor, so the jump still happens (roll still applies) but no lane gets recorded — a documented gap, not a bug. Charted lanes render in `InterstellarScene` as a new `HyperlaneLine` component (plain two-point `<line>`, same `bufferGeometry` pattern `OrbitRing` already uses) between the two stars' real scene positions — replacing a stale comment that used to say "no connecting lines yet" (which was about a *different*, still-unbuilt future feature, player-drawn plotted routes; this is unrelated and now actually exists). Verified thoroughly via direct repeated `planMove` calls (2000 trials each, since this is genuinely random): a Star Jumper without an established lane lost ~50.3% of jumps; a Turing Scout (see below) lost 0/2000; with a lane pre-established via `addHyperlane`, the same Star Jumper's loss rate dropped to ~10.2% — all within expected sampling noise of the target rates. Verified live too: a Turing Scout jumped Sol→Ross 154 (0% loss, deterministic for clean testing), a visible purple line appeared connecting the two stars immediately, and a same-ship return trip Ross 154→Sol landed it correctly orbiting Sol per the feature above.
  - **Two new ship classes**: **Hyperspace Scout** — mechanically identical to the existing Star Jumper today (plain hyperdrive, normal loss chance); the distinct class exists now specifically so it's already in place for its intended future role (a cheap, resource-light hull whose only job is mapping hyperlanes) once resource costs exist to make "cheap" mean anything. **Turing Scout** — identical to Hyperspace Scout except its hyperdrive carries `lossChanceOverride: 0` (a new optional `HyperDrive` field checked first in `hyperdriveLossChance`, before the lane-based calculation, so it wins regardless of lane state) — a jump-safe scout, per the ask, useful in practice as a deterministic ship for testing the risk framework itself (see above). Both show up automatically in `DebugConsole`'s ship-class selector (which just iterates `SHIP_CLASSES`) with no changes needed there.
  - **Testing note**: this session's live verification needed a fresh browser tab partway through — the documented "2nd+ Canvas mount in one tab loses its WebGL context" environment artifact (see the earlier entry on this) reproduced again exactly as described the moment Interstellar view's Canvas was followed by System view's Canvas in the same tab (system view rendered as a fully empty scene, confirmed via `get_page_text` showing zero marker text, i.e. not just a visual glitch — genuinely no Html-portaled content mounted). Not a code regression; switched to a fresh tab (which starts on System view as its first-ever Canvas mount) to actually see the orbit animate, per this project's own established workaround. **Superseded a few hours later** (see below) — the hyperlane's color changed from purple to cyan on request, so "a visible purple line" in the note above no longer matches what actually renders.
- **A follow-up batch of five requests plus one bugfix, all again on the fleet/ship layer**, prompted by direct visual feedback on the previous batch (ships orbiting too fast, no visible orbit path, can't tell stacked ships apart, hyperlane hard to see) plus two new asks (enter a moon's exact orbit on the opposite side, and hyperdrive jumps shouldn't work while paused).
  - **Ship markers now stack when more than one shares a body, instead of hiding all their names.** The "a single resting orbiting ship shows no label" rule (Stellaris-style declutter, see earlier entry) was too aggressive once ships can end up co-located via `followingShipId` — hiding *every* name defeats the purpose of being able to tell a stack of ships apart at all. `ShipMarker`/`SatelliteShipMarker` both gained `stackIndex`/`stackCount` props (computed by each scene: `SolarSystemScene` groups `systemShips` by `location.bodyName`; `SatelliteViewScene`'s `orbitingShips` is already all-one-body, so it's just each ship's array index) — the label now shows whenever `stackCount > 1`, and each marker past the first gets an additional vertical CSS offset (`STACK_STEP_PX = 14`, stacked *below* the existing base `translate(4px, -18px)` — see that offset's own earlier entry) so the labels themselves don't overlap either. Verified live: two ships spawned at Earth showed two distinct, vertically staggered name labels immediately, where before only the triangle icons would have shown with no way to tell them apart.
  - **Hyperlane lines now use the exact same color/opacity as `OrbitRing`** (`#2fd8ff`, 0.55 opacity) instead of a dimmer, unrelated purple (`#8a5cff`, 0.45) — the explicit ask was "same color as the orbit lines to ensure visibility," i.e. the previous color was genuinely hard to see against the interstellar starfield. A one-line constant swap on an already-verified-working component (see the hyperlane entry above); not re-verified live this round (the WebGL context-loss artifact made a fresh Interstellar-view screenshot impractical in the time available — see the testing note below), but the change carries essentially no risk since it reuses values already proven legible everywhere `OrbitRing` renders.
  - **Ships no longer orbit dramatically faster than moons.** `DEFAULT_SHIP_ORBIT_PERIOD_DAYS` was 4 — over 4x faster than even the fastest *apparent* moon period in this project's data (Phobos, `0.319 * MOON_TIME_DILATION` ≈ 19.1 days — see orbitMath.ts). Raised to 20, landing right at that pace instead of well past it. Verified live: watched a ship's position on its ring barely advance (~36° of arc) over 2 in-game days, consistent with a 20-day period (180° would have indicated the old 4-day one).
  - **Every orbiting ship now has a visible orbit ring**, the same way planets and moons already do — a new `ShipOrbitRing` component (system view only; satellite view reuses the existing static `OrbitRing` directly, since the viewed body already sits at the scene's own origin there, no moving-parent problem to solve). System view's version is genuinely new territory for this codebase: every existing `OrbitRing` usage (planets around Sol, moons around their planet in satellite view) has a *static* center, so `ShipOrbitRing` wraps `OrbitRing` in a `<group>` whose position gets updated every frame via `bodyLivePosition(bodyName, simDays)` — the same "moving parent" problem `ShipMarker`'s own groupRef already solves for the ship itself, just applied to the ring instead. Rings are deduped per (body, inclination) pair, not one per ship — every ship sharing both traces the identical circle (radius is a shared per-view constant), so `SolarSystemScene`/`SatelliteViewScene` each compute a small grouped list rather than rendering one ring per ship instance. `bodyLivePosition` had to be exported from `shipPhysics.ts` (previously private) for the system-view case to reach it. **Verification note, a real one this time**: the ring appeared to be completely missing through a long, misleading debugging chase — see below.
  - **A ship can now enter a moon's exact orbit on the opposite side of the parent planet, staying there permanently.** Right-clicking a moon (previously moons had no `onOrderTo` at all — "a moon isn't a valid move-order destination yet," per an earlier entry) while a ship is selected now orders that ship to orbit *the moon's parent planet*, synced to the moon's own period/phase/inclination via a new `oppositeMoonSyncOrbit(moon)` helper, with the phase offset by exactly 180°. Matching **period** (not just phase) is what makes "opposite" hold forever instead of just at the instant of arrival — a ship on its own default, unrelated orbit rate would drift in and out of alignment continuously. `oppositeMoonSyncOrbit` reuses the moon's real `periodDays * MOON_TIME_DILATION` (the same *apparent* rate the moon's own on-screen motion already uses, not its real-world one) — negated for a retrograde moon (Triton is the only one), which works with zero extra plumbing: `angleForYear`'s formula is `phase + (t/period)*2π`, so flipping the sign of `period` flips the sign of angle's rate of change over time exactly the way `getMoonPosition`'s own `simYears * direction` trick does. Implemented by extending `MoveDestination`'s existing `'body'` kind with an optional `syncOrbit: {periodDays, phaseDeg, inclinationDeg}` (rather than adding a whole new destination kind) — `orbitingLocation` (shared by `resolveArrivalLocation`'s `'body'` and system-having-`'star'` cases) uses it when present, defaulting to the existing hash-based fresh-arrival phase/period/inclination-0 otherwise. `ShipLocation`'s `'orbiting'` variant gained a matching required `inclinationDeg` field (0 for every ordinary arrival; every existing construction site — `orbitingLocation`'s default, `DebugConsole`'s spawn literal — was updated to set it explicitly) so the ship's own rendered path can actually match the moon's tilt, not just its angular position — "always between" only holds exactly in 3D if both objects share a plane, not just an angle. Matched with real physics, verified via a dedicated pure-function test: computed both the ship's and the moon's angle via the exact same `angleForYear` formula at five different `simDays` (0 through 3000) for Luna (prograde) and separately for Triton (retrograde, confirmed moving via its own changing `moonPos`) — angle difference was exactly 180.00° at every single sample for both, including across a moon actually in motion, not just at t=0.
  - **A hyperdrive jump can no longer fire while the game clock is paused.** Every other order already respects pause naturally (a continuous `MoveOrder` just never reaches its `arrivalSimDays` while `simDays` isn't advancing), but a hyperdrive jump is `'instant'` — it mutates the ship's location the moment it's ordered, entirely independent of `simDays` ever ticking, which meant it was the one action that could still happen while everything else in the world was frozen. `planMove`'s hyperdrive branch now checks `useGameTimeStore.getState().paused` first, before even the cooldown check, returning a new `'paused'` result kind. This is `planMove`'s second store dependency now (after `useHyperlaneStore`), read the same way — a `getState()` snapshot, no subscription. Verified via a pure-function test: toggled `useGameTimeStore`'s `paused` on, confirmed a Turing Scout's jump attempt returned `{kind:'paused'}`; toggled it back off, confirmed the identical call now returned `{kind:'instant'}`. **Superseded the same session** (see below) — `'paused'` was initially refused outright and silently ignored by callers; changed to queue-and-fire-on-resume instead, once the explicit ask came in.
  - **Testing note, the expensive one**: `ShipOrbitRing`'s ring appeared completely absent in every screenshot for a long stretch of this round, despite `useMemo`-computed ring groups showing the correct data and no console errors — a real debugging chase followed (isolating `useFrame` firing via a temporary `console.log`, bisecting `OrbitRing` out for a bare `<mesh>`, checking `requestAnimationFrame` call counts via a patched global, reading raw WebGL pixels via `gl.readPixels`). The `useFrame`/`requestAnimationFrame`-counting approach turned out to be fundamentally unreliable in this sandboxed browser tool specifically — `document.hidden`/`visibilityState` reports `"hidden"` *unconditionally* in this environment (confirmed on a freshly-loaded, newly-selected-to-foreground tab, immediately after navigation, every time), yet animation demonstrably continues working regardless (dates advance, camera eases, ships move) — so that property, and anything gated on believing it (including a naive assumption that a hidden tab throttles/stops rAF), is not a trustworthy signal here and shouldn't be used to diagnose animation issues in this harness going forward. Likewise, `gl.readPixels` against the live WebGL context read back all-zero even in regions known to contain rendered content (the animated starfield background), almost certainly because the canvas's drawing buffer isn't preserved (`preserveDrawingBuffer` unset, the performance-conscious default) and had already been cleared by the time the async devtools call executed — also not a trustworthy diagnostic here. What actually resolved it: a plain, visible `<mesh><sphereGeometry/></mesh>` swapped in for `<OrbitRing/>` rendered correctly on screen (proving the position-tracking wrapper was never the problem), and swapping `OrbitRing` back in *also* then rendered correctly, fully visible, tracing a small ellipse around Jupiter with the ship sitting right on it — the "bug" was never real; every prior negative check in this chase was a false signal from the tooling, not the app. Recorded here in detail because the false leads were specific and easy to fall for again: prefer a plain, unmissable visual swap-test over rAF-counting or pixel-reading when a WebGL feature seems to not be rendering in this environment.
- **A same-session follow-up: a "Current Action" line, a "Jump Risk" figure, brighter orbit/hyperlane lines, and hyperdrive jumps becoming queueable-while-paused (superseding "refused outright" from the entry just above).**
  - **`ShipPanel` now labels its status line "Current Action"** (was "Location" — the text there was always describing what the ship is *doing*, not just where it is, so the label was arguably wrong all along) **and that line now says so explicitly when following another ship.** `getShipStatusText` gained an optional third `ships?: ShipInstance[]` param used only to look up a followed ship's name; when `ship.followingShipId` is set, every branch of the function (travelling, jump-queued, resting) gets a `"Following <Name> — "` prefix, computed once and applied uniformly rather than threading it through each branch individually. The pre-existing separate "Following: `<name>` [Stop]" row stays — it's still the only place the Stop button lives — so a following ship now shows the fact twice (row + action-line prefix), which reads as reinforcing rather than redundant given one is glanceable and the other is the actionable control.
  - **`ShipPanel` gained a "Jump Risk" row** for any ship with a hyperdrive, showing `hyperdriveLossChance` for both lane states at once — e.g. `"50% new · 10% charted"` — rather than one live number, since this panel has no "selected destination" context to know whether a specific target lane is actually charted (same reasoning `Cooldowns` already established: ship-level info, not order-preview info). Collapses to a single percentage when both rates are equal (a Turing Scout's `lossChanceOverride` makes them both 0). Verified live: a Star Jumper showed `"50% new · 10% charted"` exactly.
  - **Orbit rings and hyperlanes are brighter** — both `OrbitRing` and `HyperlaneLine` moved from `#2fd8ff`/0.55 opacity to `#7ce8ff`/0.9 opacity (a lighter cyan, much less transparent). Kept identical between the two components on purpose, same reasoning as when hyperlanes were first matched to orbit rings' color: one shared "how bright a line reads against the dark scene" answer, not two independently-tuned ones.
  - **Hyperdrive jumps ordered while paused are now queued, not silently dropped — firing the instant time resumes** (superseding this same session's earlier "refused outright" behavior, changed within the hour once the actual ask — queueable, not just blocked — came in). Every call site that already queues on `'on-cooldown'` (`InterstellarScene.handleOrderToStar`, `useShipOrderSettler`'s follow-recompute branch) now also queues on `'paused'`, via `setPendingHyperdriveJump` exactly as before — from the caller's point of view the two `MoveResult` kinds are handled identically. `useShipOrderSettler`'s firing condition (`ship.pendingHyperdriveJump && simDays >= ship.hyperdriveReadySimDays`) gained an explicit `&& !paused` — provably redundant for a *real* tick (ticking itself requires `!paused`, so `settle()` can never observe `paused === true` from a genuine simDays change) but not for this hook's own mount-time `settle()` call, which runs once regardless of pause state; made explicit rather than relying on that reasoning holding forever. `getShipStatusText`'s queued-jump branch now also distinguishes *why* it's queued: `"queued — resume time to jump"` while paused (no numeric cooldown shown, since it isn't counting down and a stray `"0.0d"` would misleadingly suggest firing was imminent) vs. the existing `"queued (Xd)"` while genuinely waiting out the drive's cooldown. Verified via a pure-function test end-to-end: paused → ordered a jump → got `{kind:'paused'}` → queued it manually the way the real call sites do → confirmed the status text read `"Jump to Sirius queued — resume time to jump"` → unpaused → re-ran `planMove` for the same queued star and got `{kind:'instant'}` (a Turing Scout, 0% loss, for a clean deterministic check) — matches "queue it, then travel there instantaneously once time resumes" exactly as asked. Live UI verification of the right-click-while-paused path itself was blocked by the WebGL context-loss artifact (star markers only exist in Interstellar view, which was the 2nd Canvas mounted in the tab available at the time) — the underlying mechanism is proven by the pure-function test above, and every calling pattern involved (queue-on-`'on-cooldown'`, fire-from-`useShipOrderSettler`) was already separately verified live for the hyperdrive-cooldown case earlier this session.

## Combat system (Phase 1 — mechanics)

Built to an explicit four-part brief: a tactical time mode, per-component healthbars, a combat "detailed view" with a 3D movement grid, and a visible combat state. Scoped into three phases at the user's direction (mechanics → view → fleet UI) because the whole thing is several sessions of work; this section covers Phase 1, which is where the design risk actually sits.

- **Tactical time is a second *rate* on the existing clock, not a second clock.** Every position in this project is already a pure function of `simDays`, so `'tactical'` mode simply swaps `NORMAL_DAYS_PER_SECOND` (6) for `TACTICAL_DAYS_PER_SECOND` (1/86400 — one simulated *second* per real second). Nothing in the existing physics needed to change, and there is no parallel timeline that could desync from the strategic one. Speed tiers differ per mode (`[1,2,3]` tactical vs `[1,2,3,4,5]` normal — past 3x, individual shots land faster than they can be read); `setMode` clamps `speedIndex` into the new mode's range. The two paces differ by ~518,400x, which is exactly why the mode button (`.time-mode-btn`, STRAT/TAC) is styled far louder than the speed buttons beside it — it is not a neighbor of "one step faster." Tactical mode also shows a wall clock next to the date (`formatClockTime`), because at that pace the date is effectively frozen and the HUD would otherwise give no sign that time is moving at all.
- **The clock follows combat automatically, in both directions.** At normal 1x a real second is ~518,400 sim-seconds, so an entire battle resolves between two frames — combat is *literally* unobservable unless the game drops into tactical. So `useCombatResolver` pulls the clock down to tactical the moment an engagement forms, and hands it back to strategic when the last one ends. Both are gated on one preference (`combatStore.autoTacticalOnEngage`, default on) so it only ever undoes something it itself did, and neither touches `paused` — if you were paused when a fight broke out, you stay paused and get to look around first. Leaving out the return leg was a real bug caught in live testing: it strands the player at ~500,000x slower than strategic pace after every battle, which reads as the game having frozen.
- **Damage resolves through three layers with a per-weapon multiplier matrix** (`combatData.DAMAGE_PROFILES`), following the Stellaris convention the brief referenced. Energy is soaked by shields (0.5x) and melts armor (1.5x); kinetic is the exact mirror (1.5x / 0.5x). *The brief as written described both energy and kinetic as strong-vs-armor, which is the same thing twice — flagged and corrected to the mirror before implementing.* Missiles and torpedoes bypass shields outright and are instead answerable to point defense, a per-hull intercept chance rolled before any layer is touched — without it, torpedo boats are strictly dominant.
  - **Layer overflow is carried in *raw* (pre-multiplier) damage, not post-multiplier damage.** This is the subtle part: if a kinetic round overkills a shield and the surplus carried through at its 1.5x shield bonus, it would hit armor at 1.5x too, where it is supposed to be at 0.5x. Converting back to raw at each boundary (`applied / profile.shields`) is what makes the counter-matrix behave correctly on *partially* stripped layers rather than only on full ones. Directly regression-tested.
- **Three components, degrade-don't-instakill; only the core is lethal.** `weapons` scales firepower linearly (0 = can't shoot), `utility` scales thrust *and* FTL charge rate (0 = stranded and unable to flee), `core` at 0 destroys the ship. Disabling weapons or utility is never lethal by itself but can completely decide a fight, which is the whole reason for splitting them out. Damage that reaches this layer lands on the *targeted* component if one is chosen, otherwise on a random surviving component weighted by size, so a big core soaks proportionally more stray fire without any component being immune.
- **The overall bar blends all five pools, not just the three components.** First cut weighted only the components, reasoning that shields and armor are consumable buffers rather than the ship itself. Verification immediately showed why that reads wrong: a Battleship that won a fight having lost *every* point of shielding and half its armor still displayed 100% integrity. The brief said "a mixture of everything," and it was right to. Core still dominates (0.35), and weights are renormalized over whichever pools a hull actually has, so a shieldless design isn't permanently capped below 100%.
- **Persistent damage lives on `ShipInstance.combat`; everything transient lives on the engagement.** Component/shield/armor HP outlive a battle (armor never regenerates in the field — no repair system exists yet), while arena position, weapon cooldown timers, and chosen targets exist only for the duration of a fight and are stored on `combatStore.CombatParticipant`. A ship that disengages simply stops appearing in any engagement rather than needing a pile of arena fields reset on it.
- **Engagements form automatically on hostile co-location**, keyed by resting location (`combatLocationKey`). Only *resting* ships can meet: a ship mid-order is crossing real interplanetary distance and isn't anywhere another fleet could be sitting. Bare system/interstellar points are excluded too — matching two arbitrary float coordinates says "these ships share a float," not "these fleets met." Neutrals are genuinely inert (a real allegiance a fleet can hold without being dragged into every passing firefight); friendlies count as player-side.
- **Fleet-vs-fleet from day one, not 1v1.** The brief initially framed combat as duels, then correctly noted that pure 1v1 between empires isn't realistic. Sides are arrays of participants with free targeting, so N-vs-N works now rather than being a later rewrite of a duel abstraction.
- **The arena is a fixed-size cube subdivided into a lattice; ships travel its edges, including diagonals.** Arena span is constant (`ARENA_SPAN_UNITS = 12`) and only the subdivision count varies with density (`coarse`/`standard`/`fine` = 4/8/12), so a weapon's range in arena units means the same thing at every setting — density is a movement-resolution choice, not a rescaling. Diagonal hops need no separate cost rule: a body diagonal is physically √3 longer than a face hop, so deriving traversal time from distance charges for it automatically. Note weapons fire across *euclidean* distance while ships must travel the *lattice* — that asymmetry is deliberate and is what makes cutting a diagonal worth doing.
- **FTL charge is the only way out of a fight, and it costs you your guns.** A ship in an engagement is pinned — it can't leave at reaction drive. Ordering it somewhere instead begins a drive spool (hyperdrive 5s, warp 10s per the brief) during which it *cannot fire*, handing the enemy a real window to stop it. Charge time is divided by utility effectiveness, so a half-wrecked utility array doubles the spool and a dead one makes escape impossible — which is what makes utility a genuinely decisive target rather than a consolation prize. Implemented as a new `'engaged'` `MoveResult` returned from `planMove` *before* any drive logic, at the same single chokepoint that already enforces ownership, so no call site can bypass it and teleport a ship out of a battle.
- **A baseline approach behavior was necessary for Phase 1 to work at all.** Both fleets start on opposite faces 12 units apart — outside every weapon's reach — so with player movement deferred to Phase 2 a battle could never actually join; live testing showed exactly this (two ships staring at each other at 100% health indefinitely). Ships with nothing queued now close to a standoff just *inside* their own longest range (`APPROACH_RANGE_FRACTION = 0.7`) rather than to contact, so a long-ranged hull keeps its reach advantage instead of throwing it away. It only runs when a ship has no path of its own queued, so player orders (Phase 2) will always win.
- **Combat steps at a fixed timestep, and the accumulator must be strict.** `stepEngagements` advances in whole `COMBAT_STEP_SECONDS` (0.1) increments so outcomes never depend on framerate. A first cut advanced the cursor to "now" whenever less than a full step remained, which billed a partial frame as a *complete* step — at 60fps in tactical mode only ~0.017 sim-seconds elapse per frame, so every per-step effect (shield regen most visibly, measured running ~4x fast) ran too quickly *and* varied with framerate, defeating the entire point. Leftover time below one step is now carried into the next frame. Caught by measuring live regen against the in-game clock; regression-tested.
- **The resolver is pure and store-free**, exactly like `shipPhysics.planMove` — it takes `(engagements, ships, simDays, rng)` and returns what changed, with the RNG injected so tests can seed it. This is the primary verification path for combat: the browser sandbox can't reliably render a second WebGL context (see below), so a seeded scratch suite covering the damage matrix, layer overflow, point-defense rates, lattice geometry, engagement formation, a full battle from opening positions, FTL charge/escape, and regen rate is what actually establishes correctness. The live browser confirmed the integration end-to-end (auto mode switch both ways, health bars, a Corvette and a Swift Courier each destroyed, correct kinetic-vs-shields behavior).
- **Warship presets are differentiated by matchup, not stat inflation.** Corvette (shield-heavy, short kinetic), Frigate (long missiles, no point defense), Destroyer (highest PD, mixed guns), Cruiser (all three direct-fire types, no hard counter), Battleship (torpedoes, slow). Every warship keeps a real FTL drive per the brief, which is also what makes charge-and-escape available to all of them. The four pre-combat civilian hulls share one unarmed `CIVILIAN_COMBAT_PROFILE` — `combat` is *required* on `ShipClass` rather than optional, so every damage path is total and nothing needs a null check; an unarmed ship is an empty `weapons` array, not a missing profile.

## Combat system (Phase 2 — the combat view)

- **Combat is a real `ViewLevel`, not a modal overlay.** It gets the breadcrumb, the scroll-out-to-leave gesture, and the scene-remount transition every other level already has — the brief called it "the *detailed view* version of two fighting ships," and satellite view is the existing precedent for what a detailed view is in this project. It sits *outside* the galactic→satellite zoom ladder though: you don't reach it by zooming in on anything, you enter it from a ship that's in a fight, and leaving it lands you in system view (where the battle is physically happening) since there's no "one level up." The breadcrumb hangs COMBAT off SYSTEM for the same reason, styled hot to match the tactical clock.
- **`combatEngagementId` lives in `viewStore`, not `combatStore`.** It's navigation state that has to move in lockstep with `level` — splitting "which view am I in" from "what is that view showing" across two stores is exactly how you end up with a combat level mounted with nothing to render. (`combatStore.viewedEngagementId` remains for non-navigational uses.)
- **Entering combat is offered, never forced.** A fight already yanks the clock into tactical on its own; also teleporting the player's camera into an arena would be one automatic disruption too many, and would make it impossible to keep watching something else while a fight resolves. So `ShipPanel` grows an "Enter Combat" button when the selected ship is engaged, and that's the only way in. The view *does* exit itself automatically when the engagement ends, since there's genuinely nothing left to render.
- **The lattice is drawn in three layers of decreasing subtlety**: the full interior grid (faint), the node points ships can occupy, and the arena's outer cage (brightest — it's the hard boundary of the fight). Interior opacity *scales down* as density rises (0.5/0.3/0.16): at `fine` there are 507 lines crossing the view and at a fixed opacity they read as fog rather than as a grid. All lines go into one `BufferGeometry` per layer, so the whole lattice is three draw calls regardless of density and there's no need to cull interior lines.
- **Node picking scores every node against the click ray rather than raycasting geometry.** A click ray passes *through* many nodes, so "what did they click" can't be answered by a hit test. Instead an invisible box catches the click and every node is scored by perpendicular distance to the ray, closest wins — which is what players actually expect from a 3D grid, and at 2,197 nodes worst case it's a trivial per-click loop instead of 2,197 raycast targets in the scene. The catcher is a real mesh with a zero-opacity material rather than `visible={false}`, because an invisible object is skipped by the raycaster entirely and would never receive the click at all.
- **Player movement orders latch (`CombatParticipant.holdPosition`).** Without the latch, the resolver's default approach behavior resumes the instant a player-ordered path finishes and marches the ship straight back at the enemy — which makes deliberate kiting (the entire reason to give a long-ranged hull a speed advantage) impossible to actually perform. Ordering a move sets the latch; a "Resume Auto" control clears it *and* drops the queued path, so resuming takes effect immediately rather than after the ship finishes walking somewhere it's no longer meant to go.
- **Every committed route is drawn, not just the selected ship's.** Seeing where the enemy is heading is precisely the information positioning is meant to be played on. It also makes the lattice constraint *visible*: a ship crossing diagonally visibly staircases rather than gliding, and without the path lines the grid may as well be decoration.
- **Arena markers carry their own integrity bar and an FTL badge.** In a fleet fight the whole point of the arena view is seeing at a glance which ships are nearly dead and which are about to escape, without clicking through each one. A ship spooling a drive is defenceless and leaving — the single most decision-relevant fact on screen — so it gets a pulsing badge rather than living only in a panel.
- **`CombatShipMarker` reads position from the store every frame instead of via props.** The resolver rewrites participant state constantly during a fight; threading that through React props would re-render every marker on every step. `useFrame` + `getState()` keeps markers mounted and just moves their group, and the only React subscription is on health rounded to whole percent.
- **`CombatPanel` is separate from `ShipPanel` by design.** `ShipPanel` is a per-ship *inspector* available at every view level; `CombatPanel` is a per-engagement *command* surface that only means anything inside the arena. It's also the one window with no close button (`DraggableWindow.onClose` became optional for it) — dismissing it would leave the player in an arena with no way to give orders.
- **Verification note:** the interaction tests here have to be run with the clock *paused*. An early pass appeared to show the density and focus-fire buttons reverting their state; they were working correctly — the engagement was simply ending mid-test and taking the panel with it. Freezing the clock first made every control verify cleanly, and click-to-move + auto-approach were then confirmed running simultaneously (a manually-ordered player ship walking its path while a hostile closed on its own).

## Combat system (Phase 3 — environment, the movable frame, and fleet UI)

- **The arena is a *window* onto an unbounded lattice, not a box the fight is sealed in.** Node coordinates are now absolute and unbounded; `Engagement.center` says which node the visible cube is centred on, and a Recenter control slides it. That directly answers "movement shouldn't be constrained to the cube": a ship can be walked across the system a window at a time, and the cube only ever bounds *what can be ordered in one go*, never where a ship may exist. The scene, not the arena module, applies the centring offset when rendering — which is what lets the window move without rewriting a single stored position.
- **Density changes now remap every coordinate instead of accepting a snap.** Changing subdivision count changes the physical spacing between nodes, so the same integer would mean somewhere else and the whole battle would visibly jump. Remapping preserves each node's *world position*, making a density switch purely a change of movement resolution. (Phase 2 deliberately accepted the jump as harmless; once a body occupied a fixed world position, it stopped being harmless.) **Correction (Phase 4): this claim was wrong in practice.** "Remapping" round-tripped a position through real coordinates and snapped to the *nearest node of the new spacing* — a real, visible jump whenever the original position wasn't already an exact multiple of that new spacing, which is the ordinary case, not an edge case. This read exactly like "ships teleport to the nearest intersection on every density change," because that's what it was doing. See Phase 4 below for the actual fix (storing real coordinates as ground truth, so there's nothing left to remap).
- **Celestial bodies are real terrain.** Whatever the fleets are orbiting is placed at the window centre as a `CombatObstacle` with a real radius, and it blocks both line of fire and movement. Two fleets on opposite sides of a star genuinely cannot engage until one comes around — verified live: two Cruisers at Sol traded *no damage at all* for ~8 seconds before one manoeuvred into a firing position. Body radii come from real `radiusKm` through a fourth-root compression against Earth (moon ≈ 0.9 units, Earth 1.2, Jupiter ≈ 2.2, Sol ≈ 3.9 against a 12-unit arena) — real radii span four orders of magnitude and are unusable directly. Only the relative sizes are physical; the absolute scale is picked, same honesty as `moonData.ts`'s orbit compression.
  - Line of fire is a **segment**-sphere test, not an infinite-line one, so a body *behind* the shooter never blocks. Checked once per firing ship rather than per mount, since it's a property of the geometry, not the weapon.
  - Moons of the primary are deliberately excluded — at real scale they're far outside a 12-unit arena, so including them would be inventing geometry rather than modelling it.
- **Pathing became real A\*, but only when it has to be.** The greedy straight-line walk is still used in open space (cheap, and exactly optimal there); A* runs only when a body actually sits on that straight route, so the common case pays nothing for the obstacle system existing. The approach AI also had to learn to seek *firing positions* rather than just close distance — it samples directions around the target and picks the nearest one with clear line of sight. Without that, two fleets either side of a star sit forever, each unable to shoot and each convinced it was already in position.
- **Two real bugs this pass, both found live rather than by the tests:**
  - **The window/pathing deadlock.** A* was bounded to the *display window*, so any ship outside it (via recentring or a density remap) had every neighbour out of bounds, found no route, and froze permanently — observed as two ships sitting at 100% health doing nothing. The search box is now derived from the path's own endpoints plus a margin sized to the widest body. The principle: **pathing is simulation, the window is a camera, and they must not be coupled.** The approach AI's window check was removed for the same reason.
  - **Truncated long routes.** `greedyPath`'s loop guard was sized to the old bounded cube (`divisions * 3`), which silently cut any route longer than one window once the lattice became unbounded, leaving ships short of where they were sent. The guard is now derived exactly: each hop closes every differing axis by one, so a trip takes precisely `max(|dx|,|dy|,|dz|)` hops.
- **Panel placement.** Every `DraggableWindow` opened at the same spot, so the combat view's order panel and the selected ship's inspector landed exactly on top of each other. Fixed with an `initialOffset` — computed from the viewport rather than hardcoded, because a fixed 300px shove pushed the panel entirely off-screen at ~520px wide, which is a worse bug than the overlap it was fixing.
- **Fleet Management is the first NavBar category with real content behind it**, replacing its placeholder. Three subtabs per the brief: **Fleet Manager** (every ship in service with live integrity and current action — the counterpart to the Outliner's flat list: this is where you read the *state* of a navy rather than navigate to one ship), **Ship Designer** (a read-only catalog of the preset hulls with full component/defense/mount breakdowns and, more usefully than raw DPS, the damage-type *matchups* each loadout brings — since the counter-matrix is what actually decides fights), and **Strategizer** (deliberately empty, reserved not invented, same spirit as the other placeholders). Editing isn't wired up because no resource or refit system exists to make a custom design mean anything yet — but the underlying data is already a per-hull list of independent mounts and defense values, so a designer that *builds* loadouts drops onto this same model rather than needing a rewrite.
- **Turing Scout hyperdrive cooldown 27 → 7 days** (`TURING_HYPERDRIVE_COOLDOWN_DAYS`). Named separately rather than inlined so its two advantages — jump-safety and fast recycling — read as one deliberate design idea about its navigational AI, in one place.

## Combat system (Phase 4 — position-model rewrite, right-click movement, engagement counting, FTL risk)

- **Ship/body/window positions are now real, continuous coordinates (`ArenaPoint`) — never lattice indices.** This is the core fix for a real bug: the previous model stored a ship's position AS a `GridNode` (an index into that density's lattice), and a density change "remapped" every stored index by round-tripping it through real coordinates and snapping to the *nearest node of the new spacing*. That snap is a real, visible jump whenever the original position wasn't already an exact multiple of the new spacing — which is the normal case, not an edge case, so switching Coarse→Fine (or back) read as ships "teleporting to the nearest intersection," exactly as reported. The fix removes the bug at the root rather than patching the remap: `CombatParticipant.position`/`hopFrom`/`path`, `CombatObstacle.position`, and `Engagement.center` are now all `ArenaPoint` (plain `{x,y,z}` in real arena units, density-independent). `combatStore.setDensity` is now a one-line field flip — nothing needs remapping, because nothing is stored in density-relative terms to begin with. The lattice (`GridNode`, `GRID_DIVISIONS`) still exists, but purely as an ephemeral planning aid: a real position is snapped to the nearest lattice node only at the moment a route needs to be computed, and the result is converted straight back to real waypoints that never move again.
- **"Nodes are simply a pathfinding tool"**, per the explicit ask — implemented as: `orderParticipantTo` first checks whether the *straight real-space segment* from current position to the requested destination is clear (`segmentClearsObstacles`); if so, the ship walks that exact line to that exact point, which can be anywhere — not snapped to any intersection, in Coarse or Standard or Fine. Only when that direct line is obstructed does it snap both ends to the lattice, route around the body with A*, and convert the result back to real waypoints — and even then, the *final* waypoint is overwritten with the exact requested destination rather than left at its lattice-snapped position, so only the detour itself is grid-quantized, not the resting place. (Documented as an approximation: the very last leg, from the final detour waypoint to the exact destination, isn't re-checked for clearance — acceptable since that waypoint sits within about half a grid cell of the destination, but not a rigorous guarantee against every obstacle geometry.)
- **Movement is right-click; left-click no longer does anything on the grid.** Left-click-drag is OrbitControls' own camera-rotate gesture — sharing the button with "issue a move order" made ordinary camera orbiting and commanding a ship fight over the same input, which is what the report of "recentering causes problems" traced back to. `CombatGrid`'s invisible click-catcher mesh moved from `onClick` to `onContextMenu` (with `e.nativeEvent.preventDefault()` to suppress the real browser context menu), matching targeting (right-click a hostile marker), which was already right-click and needed no change — the two never collide since they're different objects (grid mesh vs. marker). Also **dropped `nearestNodeToRay`'s node-snapping search entirely** in favor of reading the click event's own `e.point` directly (the exact 3D intersection with the click-catcher), which is both simpler and is itself half of what makes off-grid placement possible.
  - Verification note: confirming this live took real debugging. Synthetic `contextmenu` events dispatched as a plain DOM `MouseEvent` (the browser's native type for that event) were never picked up by react-three-fiber's raycaster, regardless of coordinates or preceding pointerdown/up — only dispatching `contextmenu` as a **`PointerEvent`** (matching r3f's internal pointer-event taxonomy, not the DOM spec's native event type) was recognized. Worth remembering for any future synthetic right-click testing in this sandbox.
- **`activeEnemyContacts`/`isActivelyEngaged`** (`combatResolution.ts`) answer "how many enemies is this ship *actually* fighting right now," distinct from "is this ship part of a battle" — a fleet fight can easily include ships sitting outside anyone's range or blocked by a body, present in the Engagement but not exchanging fire with anyone. Range check uses **either** ship's reach, not just the querying ship's — a longer-ranged ship kiting a short-ranged one counts as actively engaging it even though the target can't shoot back, which is exactly the situation that mechanic is supposed to create. Surfaced in `ShipPanel` as a new "Engaged Against" row (below "Engagement," which keeps meaning the broader "in combat at X"), working for a selected enemy ship exactly the same as an owned one, per the ask.
- **FTL risk now has two independent, documented-scope modifiers** (`combatData.ts`): a small **core-damage** bonus (`coreDamageRiskBonus`, capped at 0.15) applied **universally** — any hyperdrive jump a damaged ship ever attempts, in or out of combat — and a larger **active-engagement** bonus (`ACTIVE_ENGAGEMENT_RISK_BONUS = 0.2`) that only ever applies at one specific moment: an FTL escape charge completing while the ship has a live enemy contact. That second constraint isn't a policy choice bolted on top, it's structural: `planMove` already refuses to plan an *ordinary* jump for any ship currently in an Engagement (its `'engaged'` short-circuit forces the charge-and-escape path instead), so "actively engaged" and "issuing a normal jump" are mutually exclusive by construction — the engagement bonus can only ever reach the two new roll sites in `planMove`'s warp branches (see below), never the pre-existing ordinary hyperdrive path.
  - **Warp escape risk is a genuinely new mechanic, deliberately scoped to combat only.** Ordinary warp travel has never had any risk in this project, and that's preserved exactly: `warpEscapeLossChance` is only ever consulted when `planMove`'s new optional `riskContext` parameter is present, and the *only* caller that ever passes one is `useCombatResolver`'s FTL-charge-completion path. A plain player-issued warp order (every existing call site) passes nothing, so the new roll code doesn't execute at all — not "rolls and gets 0%," genuinely unreached. Regression-tested directly: a ship with its core almost fully destroyed, given 200 ordinary warp orders with no risk context, never once lost the roll.
  - `activelyEngaged` is computed once per `useCombatResolver` tick, **before** stepping — by the time a completed charge reaches `planMove`, `stepEngagements` has already dropped that ship from its engagement (it's leaving), so `planMove` itself can no longer see what it was fighting. A tick-start snapshot is a deliberate, documented approximation (steps are 0.1s, a tick catches up at most 4s), fine for a coarse modifier, not intended as a precise mechanic.
  - `ShipPanel`'s Jump Risk row now folds in both modifiers live and appends "(elevated)" when either is active; a new Warp Risk row appears only for warp-capable ships when the figure is actually nonzero (a permanent "Warp Risk: 0%" on every peaceful ship would be noise, not information).
- **Turing Scout hyperdrive cooldown**: 27 → 7 days (`TURING_HYPERDRIVE_COOLDOWN_DAYS`, `shipData.ts`) — named separately from the shared baseline so its two advantages (jump-safety via `lossChanceOverride`, and this) read as one deliberate idea about its navigational AI, not two unrelated tweaks.

## Combat system (Phase 5 — real motion physics, stances, and two position bugs)

- **`CombatParticipant` now stores position + velocity, not a hop.** This is the root-cause fix for the reported "*giving a ship a new command teleports it to the destination I previously commanded*". The old model set `position` to the current leg's ENDPOINT the instant that leg began — so `position` meant "where this hop ends", not "where the ship is". `orderParticipantTo` then read it as the ship's start point and marked the hop complete, snapping the ship visually onto the previous order's destination. Position and destination are now different things (`position`/`velocity`/`positionSimDays`), which makes that entire class of bug unrepresentable rather than merely fixed — `orderParticipantTo` no longer writes a position at all, it only sets `path`.
- **The same conflation caused the second reported glitch — "*the line showing where the ship is going disappears the moment I give a command*".** For the common unobstructed order the route is a single waypoint, and the first movement step consumed it immediately, emptying `path` while the ship was still flying. `CombatPathLine` now reads live participant state per frame and draws from where the ship actually *is* through the remaining waypoints, so the line shrinks as the ship closes and vanishes only on arrival. It also stays mounted and hides itself via `setDrawRange(0,0)`, so routes appearing/completing never remount anything.
- **Ships are now provably sub-light, and the code makes it hard to regress.** The reported problem was real and quantifiable: light crosses the Sun's diameter in ~4.6s, and the old fastest hull (1.6 units/s) crossed Sol's *rendered* 7.76-unit diameter in ~4.8s — 96% of light speed, on reaction drives. `ARENA_LIGHT_SPEED_UNITS_PER_SECOND` (combatArena.ts) now derives that anchor from real constants (Sol's true radius and *c*, against Sol's rendered radius), and every hull's speed is declared as a fraction of it via `hullMotion(lightDivisor, secondsToCruise)` rather than as a bare number. Nothing caught the old value because nothing expressed the relationship; now a light-speed hull would require literally writing `hullMotion(1, …)`. Crossing times run 18.6s (Corvette) to 60.4s (Battleship) against light's 4.6s.
  - Honest caveat on the scale: because body radii are fourth-root compressed, the arena has **no single consistent km-per-unit scale** — one unit is ~179,000 km measured against Sol but ~5,300 km measured against Earth. The light anchor is therefore defined against one specific body rather than pretending a global conversion exists, and is documented that way in `arenaBodyRadius`'s comment. Fully realistic reaction-drive speeds (0.01c) would put a single arena crossing at ~12 minutes, which isn't playable; "clearly sub-light and visibly slower than light across the star you're fighting beside" is the honest bar actually being met.
- **Real acceleration, from one rule.** `integrateMotion` steers the velocity *vector* toward a desired velocity under a single `accel * dt` budget. Starting, stopping, and turning all fall out of that one constraint — turning spends acceleration exactly like speeding up does, so a Battleship sweeps a wide arc while a Corvette pivots, with no special-case turn logic. Braking asks each step "how fast could I be going and still stop exactly on the final waypoint" (`v = √(2ad)`); intermediate waypoints deliberately skip it so a ship flies *through* the corners of a detour instead of halting at each one. Rendering extrapolates along the velocity vector between the resolver's 0.1s steps (clamped to two steps) so motion stays smooth rather than visibly updating 10×/second.
  - Consequence worth stating: fights are now substantially longer, since closing 12 units at sub-light speed takes tens of seconds rather than a handful. That's the intended trade, and it's why tactical time gained its upper speed tiers back (below).
- **Four auto-combat stances** (`CombatStance`, resolved by `stanceDestination`), set per ship in **Fleet Management → Strategizer** and stored on `ShipInstance` so a doctrine can be chosen *before* a fight and persists across engagements. `balanced` (the previous behavior, still default) holds just inside longest range; `swarm` drives all the way in to the *shortest* mount's range, the only distance a mixed loadout fires everything; `kite` holds the outer edge of longest range and actively backs off when crowded, with a tolerance band so it isn't re-planning every step; `stall` refuses the fight by putting the nearest body between itself and the enemy, breaking line of fire outright (and simply opening distance when there's no body to hide behind). A manual move order still overrides the stance for that fight via the existing `holdPosition` latch.
- **Yellow dashed engagement lines** (`CombatEngagementLine`) between every pair actively trading fire — drawn as straight segments *ignoring the movement lattice*, because this is a line of *fire* and shots already travel straight; drawing it on the grid would imply a constraint gunnery doesn't have. Pairs come from the same `activeEnemyContacts` used for the "Engaged Against" readout and for FTL risk, so what's drawn is exactly what the simulation counts as engaged — there's no second cosmetic definition that could drift.
- **Fleet Management window sizing.** The content declared `min-width: 460px` inside a `DraggableWindow` hardcoded to 240px, so the text spilled straight out of its own frame. `DraggableWindow` gained an opt-in `wide` variant (560px, capped against the viewport) and `max-width`/`box-sizing` guards; the content now fills its window rather than dictating a width the window doesn't have.
- **Time controls: five tactical tiers, and speed decoupled from pause.** Tactical went 3 → 5 tiers with the slowest deliberately unchanged at 1x (the pace combat is authored at) — the upper tiers earn their place back now that closing takes real time, for skipping the approach rather than watching the shooting. Separately, `speedUp` no longer force-unpauses as a side effect and `slowDown` is no longer disabled while paused: both directions work in either state and neither touches `paused`. Pips now show the *selected* speed even while paused (dimmed), since blanking them would hide the very thing that's now adjustable.

**Verification note (browser sandbox):** catching a mid-fight moment by polling proved unreliable here, because real wall-clock time keeps advancing between agent tool calls — fights repeatedly resolved before a poll landed. What worked was doing the whole play→wait→pause sequence *inside a single* `javascript_tool` call with explicit `setTimeout` timing. And confirming the engagement line actually rendered was done by briefly instrumenting its live vertex count onto `window` under `import.meta.env.DEV`, reading it (2 vertices = one segment for one engaged pair, matching the panel), then removing the probe — the same "use an unmissable direct signal, not an inferred one" lesson as the earlier orbit-ring false alarm.

## Combat system (Phase 6 — click picking, placement resolution, route-line legibility)

- **A click is a RAY, not a point — and the old code hid that instead of solving it.** Reported as: "clicking a node creates a pathfinder to the node, but the path only goes to that node from a certain perspective... it just looks like it until I change views." Exactly right, and worse than described. `CombatGrid`'s click-catcher was a solid box spanning the window and the handler used `e.point` — the raycast hit — as the destination. **A raycast against a box can only ever return a point on the box's surface**, so every order landed on the arena's outer shell at whatever spot happened to lie along the view ray. Shell point and intended target are collinear from the camera that issued the order, which is precisely why it looked right until the camera moved. Measured against the real geometry before touching anything: **all 729 standard-density nodes mis-picked, 729/729 landing on the shell, the worst by 19.81 units across a 12-unit arena** — the arena's entire interior was unreachable by right-click. Combat only kept working because the stances auto-approach; manual movement had been ordering ships to the wall since Phase 4.
  - Phase 4 introduced this. Its recorded reasoning — that dropping `nearestNodeToRay` in favour of `e.point` was "simpler, and half of what makes off-grid placement possible" — was wrong: it removed the only thing resolving depth, and the box silently supplied a garbage depth in its place. Noted here rather than left standing in the Phase 4 entry.
- **The lattice is what resolves the ambiguity, because its nodes are discrete points in 3D.** "Which node did the cursor land on" has a single answer where "how deep along this ray" has none. `pickLatticeNode` (combatArena.ts) projects candidate nodes to screen and takes the one nearest the cursor, with depth used *only* to break ties between nodes drawn on top of each other. Taken as a `project` callback rather than a camera, so it stays pure and unit-testable without mounting a scene — the same discipline as the rest of that module, and directly motivated by the fact that the bug it replaces lived in exactly the kind of untested scene code this project otherwise avoids.
  - **The ordering of those two criteria is load-bearing and was got wrong once mid-implementation.** The first version treated any node within a fixed 14px radius as "clicked" and ranked that whole band by depth — which let a node 13px away outrank the one the cursor was sitting exactly on, purely for being nearer the camera. Live measurement caught it: four of five clicks landed on a neighbour. Screen proximity now decides, and `tieRadius` (4px) only separates genuinely overlapping dots. That is a smaller version of the same failure as the shell bug — something other than where the player pointed deciding the answer — and it is why the live click test exists rather than trusting the unit tests alone.
  - **What remains ambiguous is genuinely irreducible**, and is documented as such rather than papered over: nodes that really do share a view ray cannot be told apart by one click. Front-most is the honest answer — it is the dot the player can see — and orbiting slightly breaks the alignment and exposes the rest. Measured live at 8/8 exact from the default camera and 5/6 from a second angle, the single miss being a provably collinear pair.
  - Nodes buried inside a body are excluded from picking. They are never legal destinations (`orderParticipantTo` refuses them), so including them would turn "click across the star" into an order that silently does nothing.
- **Placement resolution is the fine lattice, and the densities now nest to make that true.** Per the explicit ask: "the resolution is essentially the fine version of the grid — ships will always be on the intersection for fine mode, but not always for coarse or standard." Implemented as: a click resolves to a node actually *drawn* at the current density (the player can only aim at dots they can see), and `GRID_DIVISIONS` changed **fine 12 → 16** so the three densities are 4 | 8 | 16 — each exactly double the last. That nesting is a requirement, not a tidy coincidence: at the old 4/8/12, standard's 1.5-unit spacing has no counterpart on a 1.0-unit lattice, so ordering at standard density would have landed ships **up to 0.87 units off the dot that was clicked** — a milder replay of the very bug being fixed. Coarse and standard are untouched, so only the finest tier gained resolution (2197 → 4913 nodes, still one draw call).
  - Consequences, both intended: in fine view a ship always sits exactly on a drawn intersection; in coarse or standard view it frequently sits between them, because those densities don't draw every position a ship can occupy. It also gives the density control real weight for movement instead of leaving it cosmetic — coarse for quick chunky repositioning, fine for placing a hull exactly.
  - **Scope boundary worth stating:** this constrains what the *player can ask for*, not where a ship can be. `orderParticipantTo` is unchanged and still accepts any real point, and stance/auto-approach destinations are still continuous — so AI-moved ships are not lattice-aligned. The Phase 4/5 real-coordinate position model is fully intact; only the picker sits on top of it.
- **Route lines drawn with `Line2` instead of `lineSegments`.** WebGL ignores `linewidth` on `LineBasicMaterial`, so a route was stuck at one hardware pixel and read as just another strand of a lattice that already has hundreds of them. drei's `<Line>` builds segments as camera-facing quads, giving a real 2.6px width. The buffer is still written **in place** (Line2 interleaves both endpoints of each segment at stride 6, and `instanceCount` selects how much is live) rather than via `geometry.setPositions`, which reallocates on every call and would do so 60×/second per ship — preserving the no-reallocation property the previous implementation had.
- **Route lines now render only for player-owned and allied ships.** Hostiles and neutrals still manoeuvre exactly as before — this hides the *information*, not the behaviour. Knowing precisely where an enemy is heading several seconds early trivialises the positioning the fight is played on; reading their heading off the hulls is the intended skill. (Reverses Phase 2's "every committed route, not just the selected ship's", which had the opposite reasoning; the ask was explicit.)

**Verification note (browser sandbox) — the earlier synthetic-right-click note was incomplete and cost real time here.** Phase 4 recorded that r3f only recognises a synthetic `contextmenu` dispatched as a `PointerEvent`. True, but two further conditions matter and neither was written down:
  1. **Dispatch on `state.events.connected`, not the canvas.** r3f connects its listeners to a wrapper `<div>`, so events fired at the `<canvas>` are never seen. There is no `__r3f` on the canvas to discover this from — read `useThree().events.connected`.
  2. **`offsetX`/`offsetY` must be set explicitly, and a `pointerdown` must precede the `contextmenu`.** r3f's default `compute` reads `event.offsetX/offsetY`, which a synthetic event leaves at 0 (every click lands in the top-left corner); and click-type events only fire on objects that were in the hit list of a preceding pointerdown. The working sequence is `pointermove → pointerdown → pointerup → contextmenu`, all as `PointerEvent`s with `offsetX`/`offsetY` defined via `Object.defineProperty`.
Also confirmed again that batching many synthetic clicks into one `javascript_tool` call hits the 30s tool timeout well before it hits any app limit — ~8 clicks with 60ms settling is a comfortable ceiling per call.

## Combat system (Phase 7 — real-time AI tracking, real gravity, Flee stance, targeting UX, player-only fleet views)

- **Auto-controlled ships now re-track a moving stance destination every step, not just when idle.** Reported as: a kiting/swarming ship would commit to wherever its target was the INSTANT an order was queued and never reconsider — because the approach logic's guard was `if (p.path.length > 0) return p`, which conflated "has a route queued" with "is under manual control." Those are different things: a manual order and a stance-issued one both populate `path` identically, so the guard silently froze auto-piloted ships the same way it correctly protected a player's own order. Fixed by keying off `holdPosition` (the actual manual-control flag) instead, and recomputing `stanceDestination` every step regardless of whether a route already exists — replanning only when the fresh destination has moved more than `STANCE_REPLAN_TOLERANCE` (0.25 units) from where the existing route already ends, so a target holding still doesn't cost a fresh lattice plan every 0.1s step. Verified live in the resolver: a target relocated mid-flight pulls the route's endpoint with it; sub-tolerance jitter leaves the participant object completely untouched (same reference, not just "close enough").
- **Overshot sub-waypoints are now dropped instead of treated as sub-destinations.** Reported as: a ship would visibly reach its destination, then peel back AWAY from it to go tag a stale waypoint behind it. Root cause: every node in `path` was being treated as something the ship was *required* to touch, when only the LAST one ever was (nodes are a pathfinding aid — see the Phase 4 header). `pruneOvershotWaypoints` drops the next sub-node the moment the ship's current position is both closer to the final destination than that sub-node is AND the straight line onward is actually clear — applied to every route regardless of who queued it (manual or stance), every step, before approach logic runs.
- **A ship with dead utility now drifts on real, unmodified momentum — a real physics bug was found and fixed while building the collision consequence below.** `integrateMotion`'s final `if (velocity.length() > maxSpeed) velocity.setLength(maxSpeed)` clamp ran unconditionally, and at zero utility `maxSpeed` is *also* zero — so a "drifting" ship's velocity was being snapped to exactly zero on the very next 0.1s step, not preserved. The clamp now only runs inside the `budget > 0` (real thrust) branch; with no thrust at all, velocity is left completely untouched by steering. Caught by writing the collision test below and watching a ship that was supposed to be coasting toward a body stop dead at step 1.
- **Flying into a body is now fatal.** There was no collision consequence at all before this — `isPointBlocked` was only ever consulted during route *planning* (refusing a destination inside a body, routing around one in the way), never checked against where a ship actually ends up after a step. That's exactly the case a dead-utility ship exposes: nothing is steering it around anything any more. A post-movement check now destroys any ship whose new position falls inside an obstacle, applied via the same "zero the core" path a normal shot-kill uses (so it also correctly stops that ship firing the same step it collides).
- **Bodies now have real gravity, felt only by a ship with no thrust of its own** — the explicit design brief being "thrusters let ships treat it as flat space; with utility dead, gravity is what's left." A working ship (any `accel > 0`) is completely unaffected — gravity is applied only inside `integrateMotion`'s zero-thrust branch, right alongside the velocity-preservation fix above, so a dead-utility ship's drift now curves and accelerates instead of continuing in a straight line forever. `CombatObstacle` gained `surfaceGravityUnitsPerSecondSq`, derived per body from real mass/radius via the standard `g = GM/R²` surface-gravity formula, then compressed onto arena scale by the same "one honest anchor, real ratios preserved" method `arenaBodyRadius`/`ARENA_LIGHT_SPEED_UNITS_PER_SECOND` already use: Earth is anchored so a dead-thrust ship starting a couple of units out falls in within tens of real seconds (legible within one fight, not a 19-real-minute literal free-fall or an instant unavoidable trap), and every other body's arena gravity is that anchor scaled by its REAL surface-gravity ratio to Earth's — so Sol pulls ~28x harder and Luna ~1/6th as hard, matching reality exactly in relative terms. `gravitationalAcceleration` sums every obstacle's pull as a real vector (an Earth fight now has two gravity sources, Earth and Luna, both counted). Live-verified end to end through the real resolver: a ship starting 2 units past Earth's arena radius with a small sideways velocity curved — not fell straight in — and crashed 32.2 real seconds later.
- **New `flee` stance, and it's now the automatic fallback for any ship that can't fight back.** Runs from the CENTROID of every hostile in the fight, not just the nearest one (`fleeDestination`) — genuinely different from Kite, which only ever backs off the one ship it's tracking. `stanceDestination`'s old "unarmed hull behaves as if stalling" fallback now points at flee instead, and is also newly gated on a `weaponsOnline` flag computed from live weapons-component HP each step — so a fully-armed ship whose weapons get shot offline mid-fight *also* falls back to flee automatically, not just a hull with zero mounts (a spawn-time default alone could never satisfy that half of the ask, since weapons go offline mid-fight, not at spawn). Stall remains a deliberate, explicit CHOICE even for an unarmed ship (still useful for pure survival) — only the *other* stances get silently rerouted to flee when there's no working weapon to hold a firing line for.
- **Combat targeting UX**: right-clicking a Hostiles row in the Engagement panel now assigns that ship as the selected ship's target — the same gesture the arena's own markers already used, just reachable from the roster too (useful once a fleet fight has more ships than fit on screen at once). Right-clicking an ALREADY-targeted ship (marker or roster row) now clears it back to auto-targeting — a second way to reach what the panel's "Auto" button already did, addressing the report that there was "no way to remove a target" (there was, via that button, but the right-click toggle is the more discoverable path since it's the same gesture that set it).
- **Fleet Manager and the Outliner's Fleets list are now player-owned ships only.** Both previously listed every ship regardless of allegiance — the Strategizer tab already scoped itself to `allegiance === 'player'` (this is "my orders," not a sensor sweep), and the other two now match it for the same reason.
- **Earth's own combat arena now includes Luna as real terrain.** Moons are still deliberately excluded from combat obstacles in general (a real orbit radius — 60 Earth radii for Luna — is six times the 12-unit window's own span, so including one honestly would mean inventing geometry, not modelling it — see the Phase 3 note this doesn't change). Luna is the one explicit exception: close enough, relative to its primary, to be worth it anyway. Its position (`EARTH_MOON_OFFSET`) is a picked, documented compromise exactly like every body's *size* already is (`arenaBodyRadius`) — there's no honest way to fit a 72-unit separation in a 12-unit window — just applied to a position instead of a radius. `MoonRawData` gained an optional `massKg` (only Luna's entry sets it — no other moon currently reaches a combat arena, so no other entry needs to lie about having a real value).
- **Satellite view now renders bodies at genuinely different sizes.** Every inspected body (star or planet) previously rendered at one flat `PRIMARY_VISUAL_RADIUS = 3` constant regardless of its real size — Sol and Earth read as visually identical, which is exactly the reported bug. `satelliteVisualRadius` compresses real radius with the same fourth-root curve `arenaBodyRadius` already uses, anchored so Earth keeps its long-tuned value of 3 (every camera-distance constant in this scene was tuned against that number) — Sol comes out at 9 (the clamp ceiling), Mercury at ~2.36, Jupiter at ~5.46. The first implementation attempt also scaled the initial camera distance proportionally to each body's own radius, which was wrong in a way only caught by actually comparing two screenshots: proportional framing makes every body fill the same fraction of the screen by construction, which defeats the entire point (you can't SEE Sol is bigger than Earth if both are auto-zoomed to look identically sized). Fixed to a single FIXED camera position (`[0, 6, 27]`, sized to comfortably frame the largest clamped body) shared by every satellite view — confirmed live, Sol now visibly dwarfs Earth, which visibly dwarfs Mercury, all from the same vantage.
- **Settings tab**: new NavBar category with the first real display preference — Navigation Line Thickness (Thin/Medium/Thick, defaulting to Thin). Wired into both `CombatPathLine` and the new out-of-combat `NavigationLine`.
- **Navigation lines now apply outside combat too, and point somewhere.** `NavigationLine` (system/interstellar views) mirrors `CombatPathLine`'s reasoning — a ship's current order drawn from its live position to `order.endPosition`, player/allied ships only (same information-hiding rule as combat's route lines). Both route-line components also gained a chevron arrowhead at each leg's endpoint (`routeArrow.ts`'s `arrowWings`, a pure, camera-billboarded function shared by both, tested independently of any scene) — showing the direction of travel, not just where the line goes.
- **`DraggableWindow` gained a collapse toggle and viewport drag-clamping.** Collapse hides the body, keeping the title bar; since the window is centred on its position via `translate(calc(-50% + ...))` on BOTH axes, naively toggling the body would shrink the box toward its own vertical middle and visibly drop the title bar. Fixed by compensating `pos.y` by half the body's measured height in the same click handler that toggles `collapsed`, so the top edge — and the title bar sitting on it — doesn't move (confirmed live: 0.5px residual, from the 1px border removed in the collapsed state, otherwise an exact round-trip). Dragging is now clamped so a window's rendered box can never leave the viewport, stopping exactly at the border rather than being draggable off-screen — computed by diffing the proposed position against the window's own live `getBoundingClientRect()`, so it needs no knowledge of the CSS `left`/`top` percentages driving its base position.
- **The dev-only debug console is no longer text-selectable** (`user-select: none`) — it's an overlay, not document content.
- Confirmed, not changed: tactical time's speed tiers already run at literally 1x/2x/3x/4x/5x real time (`TACTICAL_DAYS_PER_SECOND` is exactly 1 sim-second per real second, and the multiplier applies directly on top) — this was asked for as if it might not already hold, but the math was already exact.

**Verification**: 173 pure-function checks (up from 120), typecheck clean, build clean, plus live browser verification of the harder-to-unit-test pieces — real-time stance tracking, collision death, gravity's actual fall trajectory and timing, satellite-view relative sizing (screenshots), the Engagement-panel targeting toggle, and the collapse-anchor fix — all driven through the real production code paths (spawn → syncEngagements → stepEngagements), not synthetic fixtures alone.

## Combat system (Phase 8 — strategy-time backlog, panel layout, hull separation, out-of-combat drift, disengagement, chaff)

- **Combat no longer runs at ~240x after a trip through strategic time.** Reported as "go into strategy time, then back to tactical, and the fleets are still fighting in strategy time." Root cause was a compounding, permanent debt: the resolver catches up at most `MAX_STEPS_PER_TICK * COMBAT_STEP_SECONDS` (4 sim-seconds) per tick — about **240 sim-seconds per real second at 60fps** — while strategic pace advances the clock **518,400 sim-seconds per real second**. Every real second spent in strategic time therefore bought roughly **36 real minutes** of hyperspeed catch-up, which kept burning down long after the clock returned to tactical. The fix (`combatCatchUpCursor`) DISCARDS backlog beyond one tick's reach instead of queueing it, so combat always resolves against the current moment and returning to tactical is immediately 1:1 again. Extracted as a pure function specifically so the bound is unit-testable rather than buried in a hook — the numbers above are asserted directly, including the 36-minute figure, so the regression stays legible.
  - Deliberately keeps the documented "fights resolve offscreen at their own maximum pace" behaviour intact; what changed is only that unsimulated time is skipped rather than owed. Time a fight was never stepped through simply didn't happen to it, which is the honest reading of "combat is unobservable at strategic pace" the whole module is built on.
- **The combat view's two panels are pinned to opposite screen edges** (`DraggableWindow`'s new `anchor` prop) — the engagement roster left, the selected ship's inspector right — instead of both hanging near the middle and covering the arena. Replaces the old viewport-derived horizontal gap helper entirely: the edges do the separating at any width, which the gap maths could never guarantee (it collapsed to zero on a narrow viewport). Anchored windows skip the `-50%` horizontal pull-back but keep vertical centring, so the collapse-anchoring fix from Phase 7 still holds for both.
- **Two hulls can no longer occupy the same point.** A post-movement separation pass pushes overlapping ships apart symmetrically along the line between them (`SHIP_SEPARATION_UNITS`, 0.45), with a fixed-axis fallback for the exactly-coincident case that has no line to push along. Done as a correction rather than by teaching steering to avoid squadmates, deliberately: every stance resolves its destination from the ENEMY's position, so a whole fleet legitimately wants the same spot, and encoding "avoid your allies" into that choice would fight the stance logic for control of the same value. Two passes, not iterate-to-convergence — a residual overlap in a dense pile-up is acceptable for what is a readability rule, not a rigid constraint.
- **Ships can now leave a fight by simply outrunning it** (`DISENGAGE_DISTANCE_UNITS`, 30 — beyond both the longest weapon and the arena window's own span). Previously the ONLY exit was an FTL charge, which a ship with wrecked utility cannot even attempt, so a Flee-stance ship could run forever without ever escaping. A disengaged ship is relocated to a `system-point`, which is what makes it stick: `combatLocationKey` returns null for that kind, so `syncEngagements` can never pull it back into the fight it just left (every other location kind would re-form the engagement on the very next tick). Confirmed live end-to-end — two unarmed couriers auto-fled (no weapons → Flee), broke contact, and ended up reading "In Sol System, Deep Space" with the combat view exiting on its own.
- **Unpowered hulls keep drifting under real gravity OUT of combat too** (`useShipDriftIntegrator`), not just inside the arena. Unlike the arena — whose body radii are fourth-root compressed, leaving no consistent km-per-unit scale — system view IS true-to-scale, so this uses **real Newtonian gravity with no invented anchor constant**: `systemGravityAcceleration` sums the star and every planet at their live positions. Sub-stepped (`MAX_SUBSTEP_DAYS`) because gravity is inverse-square and strategic pace would otherwise integrate it in 6-day leaps straight past a body. Falling into a star or planet destroys the ship, matching the arena's own collision rule.
  - **Two real bugs were found by actually running this rather than only unit-testing it**, both about seeding the drift, and both worth recording because the failure mode was silent and plausible-looking:
    1. **Seeding at heliocentric rest made wrecks spiral into the Sun.** A ship in Earth orbit is also travelling with Earth around the Sun at ~0.34 units/day; dropping that velocity means it no longer orbits anything. Measured: a hull stranded near Earth struck **Sol after 64 sim-days**. Fixed by inheriting the body's own orbital velocity (`bodyOrbitalVelocity`, central-differenced from its live position).
    2. **Placing a stranded hull where a fleeing one goes put it outside the body's gravity entirely.** A powered ship that runs is placed 1.5 units out, but Earth's Hill sphere is only ~0.2 units — seven times closer. A wreck left at 1.5 units wandered up to 34 units away because the Sun dominates completely out there. Powered and stranded disengagement now use different offsets (1.5 vs 0.08 units), and the stranded nudge (0.003 units/day) is sized to stay well under escape velocity at that offset (~0.013) — an earlier 0.05 value was nearly **twice** escape velocity, so the wreck simply left. With both fixed, a stranded hull stays inside 0.084 units of Earth and falls into it after ~13 sim-days.
  - Worth knowing for any future work here: what binds a ship to a planet is the **differential** field, not the net one. At 0.08 units the Sun pulls on the ship ~5x harder than Earth does, but pulls Earth almost identically, so the common part cancels — which is exactly what the Hill sphere measures. A test asserting on the raw net vector fails while the behaviour is correct; assert on `g(ship) - g(body)` instead.
- **Chaff** (`CHAFF_CHARGES` 2, `CHAFF_DURATION_SECONDS` 6, `CHAFF_MISS_CHANCE` 0.5). Implemented as a **miss chance on incoming fire**, not a damage reduction — the two play very differently: a damage tax is something an attacker out-scales, while a coin-flip per volley is what makes spending one of only two charges at the right moment a real decision. Rolled after point defense (so a missile must survive interception AND still find its target) and applied to every damage type, since it degrades aim rather than physically stopping a projectile. Charges do NOT regenerate, like armor. The AI spends its own charges on two health thresholds (0.7, then 0.4) rather than one, so it doesn't burn both at the same instant, and only while ACTIVELY engaged — chaff spent while nothing can shoot you is exactly the mistake a player wouldn't make. Deployment runs BEFORE the firing step so a burst answers the same volley it was deployed against, not the next one. Verified live: `2 left` → `Active 5.7s` → `Active 3.2s` → `1 left`, a real 6-second burst at tactical 1x, plus a 4,000-shot statistical check landing at exactly 50.0% misses.
- **Browser swipe-navigation can no longer eject the player from a running game.** `overscroll-behavior-x/y: none` on html/body/#root, plus `overscroll-behavior: contain` on every scrollable panel so a gesture that hits a panel's scroll end doesn't chain up to the page and become history back/forward. A fixed-viewport canvas game has no reason to ever hand a horizontal swipe to the browser.
- Confirmed, not changed: tactical time's tiers already run at literally 1x–5x real time — re-verified rather than adjusted.

**Verification note:** the app's stores are NOT reachable by `await import(...)` from the console — Vite hands the dynamic import a *separate module instance*, so spawns made that way never reach the running UI (it kept reporting "No fleets deployed"). Everything live here was driven through the real UI instead (the dev console's own spawn controls). Related: the game clock does not tick while the browser pane is backgrounded, so anything depending on `simDays` advancing needs the pane fronted.

## Combat system (Phase 9 — playing against the odds)

The ask was for more to do when a fight is going badly. An earlier cut of this pass misread it as counterplay *to* chaff and built a range falloff that switched chaff off as enemies closed — exactly backwards, since a losing ship is usually one with enemies closing on it. A comeback tool doesn't need its own counter. That falloff was reverted; what follows is the corrected direction.

- **Chaff's miss chance 0.5 → 0.75, flat at any range.** Three shots in four don't connect. At an even coin-flip, a two-charge six-second countermeasure was a rounding error on a multi-minute fight. Auto-deploys by default for every allegiance now too (`ShipInstance.chaffAutoDeploy`, default true — spent the instant it's worth it, same threshold logic the AI already had), with a checkbox in the panel for a player who wants to hold a charge for a specific moment instead.
- **Fleet-wide focus fire** (`setFleetTarget`, "Focus Fleet" in the engagement panel). One click points every ship the player commands at the same hostile; clicking the active one releases the whole fleet back to auto. This is the canonical answer to being outnumbered — each kill permanently removes that ship's damage from the fight, so concentrating fire is how a smaller force closes the gap — and doing it ship-by-ship put the most clicking on the player who could least afford the time.
- **Local-odds readout on every roster row** (`×2`, `×3`, red above 1) — how many enemies can actually shoot that hull *right now*, from the same `activeEnemyContacts` the engagement lines and FTL risk already use. Shown for both sides, since spotting an isolated *enemy* is half the same decision.
  - This exists to make **splitting the fleet** playable, which was the first thing asked for: if outnumbered overall, the winning line is to arrange local 1v1s rather than fight 2v4 at once. Verified that the underlying behaviour genuinely supports it — enemies pick targets by proximity (`nearestEnemy`), so with the player's two ships pulled far apart, four hostiles split cleanly 2/2 rather than blobbing onto one. The mechanic was already there; what was missing was any way to *see* the local balance, which is what this readout supplies.
- **Scuttle — the doomed-ship trade.** Detonates a hull that's going to die anyway, damaging hostiles within 3 units with linear falloff. Yield scales with **remaining core**, which is the whole tension: a healthy ship makes a far bigger bang but is also the one you'd rather keep, so the best payoff never coincides with the moment you most want to spend it, and a nearly-dead hull is a poor bomb (this stops it being a free finisher bolted onto every losing fight). Blast damage goes through shields then armor then core via `applyRawBlast` rather than `applyShot` — a reactor breach has no damage type for the matrix, nothing for point defense to intercept, and no firing solution for chaff to spoil. Hits only the opposing side, so it never punishes keeping a formation. Behind a two-step confirm in the UI, since it's the one irreversible action in that panel.
- **Point defense now scales with the target's own weapons-component health.** Kept from the earlier cut because it survives the reframing intact — it *is* an underdog tool. PD was previously a flat hull stat nothing could degrade, so a Destroyer's 0.55 interception held at full strength until the instant it died and missile/torpedo boats had no play against it at all. Point defense is gunnery, so it now dies with the gunnery array: Focus Fire → Weapons brings the screen down. Measured 55% → 28% at half weapons → **0%** with the array destroyed. That's a setup a torpedo boat can now create and previously could not.

**Verification note (browser sandbox):** the preview pane only advances animation frames when a screenshot forces a paint — between screenshots the clock doesn't tick and r3f `Html` markers never mount, so DOM queries read empty and it looks like nothing spawned. Interleave `computer{action:"screenshot"}` between steps when driving anything frame-dependent. (In addition to the Phase 8 note that `await import(...)` gets a *separate* module instance from the running app.)

## Combat system (Phase 10 — dev-console scenarios, proven by simulation not judgment)

- **"Add Scenario" in the Debug Console** — `src/data/scenarios.ts` defines 6 pre-built fights at three difficulty tiers (Easy/Medium/Hard), loaded via a dropdown + "Load Scenario" button that clears every ship currently on the board first (a scenario is meant to be a clean, reproducible test bed — a leftover ship from an earlier spawn would silently change the fight without it being obvious why the outcome doesn't match what was verified).
- **The tiers are defined by what the built-in automation can do against them, and every claim is PROVEN, not eyeballed** — the user's explicit ask before any code was written. `tests/scenarios.test.ts` re-runs each scenario through the real resolver (`stepEngagements`), 16 seeded trials per configuration, with genuinely zero player input (no manual moves, no manual targeting, no manual chaff — just whatever `stance`/`chaffAutoDeploy` the ships were built with), and checks the win rate lands where the label claims:
  - **Easy** — default automation (`stance: 'balanced'`) wins on its own, β‰₯85% of trials.
  - **Medium** — pure defaults reliably LOSE (≀15%), but the scenario's own baked-in stance reliably WINS (β‰₯85%), and the swing between the two is real (β‰₯50 points), not noise.
  - **Hard** — EVERY stance the game has (Balanced/Kite/Swarm/Stall) fails to reliably win (≀15% each). Nothing in the automation toolbox solves it; the description says what a human would actually need to do (focus-fire timing, active repositioning, hand-timed chaff) instead.
  - "Medium" was scoped from the user's own answer to a direct question: any ONE-TIME automation-level configuration (stance, chaff toggle, fleet-wide focus target) counts, as long as the player never gives a continuous stream of manual orders afterward — not "the player pilots it a bit better."
- **Design was empirical, not theorized** — iterated through a throwaway simulation harness against the real ship-class numbers (weapon ranges, DPS, speed) before writing any scenario data, and it caught real, non-obvious behavior:
  - Balanced already holds a long-ranged ship (frigate, 11-unit missiles) outside a short-ranged attacker's own reach (corvette, 3 units) — so **Kite made a frigate-vs-corvette fight WORSE**, not better, by overshooting into awkward positioning against a faster attacker. That combination was dropped.
  - The winning Medium pattern that DID work cleanly: a fast-enough, long-ranged ship (frigate) against a much slower, heavier one (battleship or cruiser pack) — Kite's 92%-of-longest-range hold sits just outside the slow hostile's own max reach, while Balanced's 70% sits just inside it. Both Medium scenarios use this, verified at a stark **0/16 (Balanced) β†’ 16/16 (Kite)**.
  - A cruiser's mixed loadout (one 9-range mount, four mounts at 5-6) means Balanced — holding at 70% of its LONGEST mount — leaves 4 of 5 weapons out of range; Swarm should fix that in theory, but against 2 destroyers it didn't move the needle at all (0/16 both). Kept as evidence that not every plausible tweak actually rescues a fight — some matchups are just lost, which is exactly what a Hard scenario needs.
- **Final roster** (all 6, exact numbers in `scenarios.ts`'s own comments): Easy — 1 Cruiser vs 1 Corvette @ Mars, 3 Destroyer vs 2 Corvette @ Earth. Medium — 1 Frigate(Kite) vs 1 Battleship @ Earth, 2 Frigate(Kite) vs 3 Cruiser @ Earth. Hard — 1 Destroyer vs 1 Battleship @ Earth, 2 Destroyer vs 1 Battleship+2 Corvette @ Earth.
- Verified live: loading "Gunboat's Gambit" (Medium) spawns exactly the 2 specified ships, and the Strategizer confirms the frigate starts on Kite (not the game's own default Balanced) — the scenario's winning configuration is what a player actually sees, not something they have to discover cold.

**Verification note:** `tests/scenarios.test.ts` is deliberately separate from `tests/combat.test.ts` — it runs far more and far longer simulations (16 seeds Γ— up to 4 configurations Γ— 6 scenarios) than the primary suite's style, so keeping it apart means the fast, frequently-run primary suite stays fast, while this one still exists as a real, re-runnable regression guard for whenever scenario content or combat balance changes.

## Combat system (Phase 11 — arena/UX bugfixes: Sol's arena, enter-arena vs enter-combat, the order-linger bug)

- **Sol's own combat arena no longer renders inside the star.** `CombatViewScene`'s camera framing was scaled off raw `maxObstacleRadius` with ad-hoc multipliers, which at Sol's true-to-scale size still let the star overwhelm the frame. Fixed by unifying the camera-distance formula to scale off `arenaWindowSpan` using the same ratio the old fixed constants implied, and by memoizing the `camera` prop via `useMemo` keyed on a stable primitive (`windowSpan`) — a fresh object literal every render was fighting `OrbitControls`, landing the camera at a technically-correct distance but pointed the wrong way, which looked identical to "nothing is rendering."
- **"Enter Arena" vs "Enter Combat"**: entering the tactical view now reads as "Enter Combat" specifically when a live hostile pair is present at that location (mirrors how the arena is actually used), "Enter Arena" otherwise (surveying/pre-positioning with no fight yet — see `createSoloEngagement`).
- **The "ordering a ship inside the arena immediately pushes you back out" bug**: `syncEngagements`' "is this ship leaving a still-live fight" check compared against `atRest` ships only (excluding the departing ship and any other departing ones), which made even a genuine 2-ship battle read as "not contested" the instant one ship got an order — trivially true once that ship is excluded from a 2-ship pair, regardless of whether the fight was actually over. Fixed by checking hostile-pair-presence across the FULL raw roster (including the departing ship itself), so a ship leaving a real fight is still correctly pulled immediately, while a solo/lingering engagement's sole remaining ship is not force-exited.

## Combat system (Phase 12 — fleet-scale UI: clustering, split/merge, panel scrolling, the engagement health bar)

Driven by hitting real UI limits once fleets could run into the dozens: "100 fleets with one ship per fleet is very messy," plus a request for scrolling panels and an easy way to split/merge fleets.

- **Every ship always belongs to a fleet** (`ShipInstance.fleetId: string`, solo = a fleet of one) via a separate `fleetStore.ts` (`Fleet {id, name, allegiance, strategy}`). Fleets auto-merge on arrival — `spawnShip`/`setShipLocation` match on `combatLocationKey` so ships landing at the same place as an existing same-allegiance fleet join it automatically, no manual step required.
- **One marker per fleet, not per ship**, via `clusterRestingShipsByFleet` — keyed by a compound `${fleetId}::${combatLocationKey}` (not fleetId alone) so members of the same fleet that have physically drifted apart never wrongly render as one cluster.
- **Split/merge UI** in `FleetManagement.tsx` / `ShipPanel.tsx`: a fleet roster with per-ship split-off checkboxes and a "Split Off (N)" button (`shipStore.splitFleet`), plus a "Merge Fleets" button (`shipStore.mergeFleets`) for combining co-located fleets directly.
- **Scrolling panels** — the relevant `DraggableWindow` bodies gained real scroll containers instead of growing unbounded off-screen.
- **Engagement total-health bar** (`.combat-totals-bar` in `CombatPanel.tsx`) — a bar across the top of the Engagement panel showing total current HP (not headcount) on each side, so five wounded corvettes vs. one fresh battleship doesn't misread as an even fight. Uses the same per-ship Integrity % the roster rows already show, scaled by each ship's actual `totalHitPoints`.

## Combat system (Phase 13 — fleet-wide strategy: the 'fleet' sentinel, Divide/Condense/Screen)

- **`CombatStance` gained a `'fleet'` sentinel value**: on a ship, it means "defer to my Fleet's own `strategy`," resolved via `effectiveStrategy(ship, fleets)` in `combatResolution.ts`. This is what lets one ship's individual stance choice override fleet-wide coordination for free — setting a ship to anything ELSE just stops it deferring, no separate "detached" flag needed. Only reachable by a fleet-wide strategy bulk-assigning it, or by the player re-selecting `'fleet'` for one ship whose fleet already has an active strategy (to rejoin it) — never a free player pick on its own.
- **The Strategizer groups ships by fleet** and exposes a fleet-wide strategy picker with a disclaimer that it's overridden by any ship's own individual, non-`'fleet'` stance.
- **`FleetStrategy` = the five normal stances (bulk-applied to every member) plus three genuinely coordinated, multi-ship behaviors**, all threaded through `stepEngagements`'s `fleets: Fleet[]` parameter (sourced from `useFleetStore` in `useCombatResolver.ts`):
  - **Divide** (`divideAssignment`) — spreads target ASSIGNMENT across the fleet (not just movement), so ships spread out to take different enemies instead of piling onto whoever's nearest.
  - **Condense** (`condenseDestination`) — regroups the fleet on its own centroid; the natural follow-up after Divide, though usable any time.
  - **Screen** (`screenDestination`) — the toughest half of the fleet by design toughness (`shieldHp + armorHp`, NOT current damage — reshuffling "the tank" mid-fight would make the wall visibly crumble and reform for no reason) holds a line between the fleet's centroid and the nearest enemy; the rest fall back to the centroid. Draws fire purely by being the closest thing to shoot at — no artificial taunt mechanic.
- Precedence: an individual ship's Chase still wins over all of this (matches Chase's pre-existing precedence over a plain stance) — both are explicit per-ship overrides, not fleet coordination.

## Combat system (Phase 14 — range-favor line colors, the ShipPanel favor tag, scuttle friendly fire, and Ramming)

- **Engagement lines are now 3-color, not flat yellow**: yellow (`MUTUAL_COLOR`) when both ends of a pair can reach each other, red (`HOSTILE_ONLY_COLOR`) when only the hostile end can hit the friendly/player end (genuinely dangerous — taking fire you can't return), green (`FRIENDLY_ONLY_COLOR`) when only the friendly/player end can hit without retaliation (a free shot). New shared pure functions in `combatResolution.ts` — `rangeContactStatus(a, aShip, b, bShip, simDays)` (low-level, generic a/b pair) and `rangeFavor(participant, engagement, ships, simDays): 'favored'|'unfavored'|'even'` (self-centric majority-vote over `activeEnemyContacts`) — are shared by `CombatEngagementLine.tsx` (the 3-color rendering) and `ShipPanel.tsx`, so the arena's line colors and the panel's text can never quietly drift apart from each other.
- **`ShipPanel`'s "Engaged Against" row now also shows a favor tag**: green "(favored)" or red "(unfavored)" next to the enemy-contact count, silent on a genuine tie rather than claiming an edge that isn't there. Works when inspecting any ship, including an enemy one.
- **Scuttle now hurts everything in the blast, not just hostiles** — a reactor breach doesn't check IFF. Detonating a ship in the middle of your own formation is exactly as dangerous as anywhere else now; this was a deliberate tightening (it used to explicitly spare same-side ships), making Scuttle purely a last resort rather than a tool that's ever safe to use near your own fleet.
- **New Ram order** (`CombatParticipant.ramming`, `combatStore.setRamming`) — any ship, armed or not, can be ordered to charge its current target (explicit `targetShipId`, or nearest enemy — same resolution firing already uses) and collide with it. Resolved in `stepEngagements` as a dedicated ramming-impact step: detects real contact by checking PRE-separation positions (checking post-separation would almost never see a "collision," since separation's whole job is pushing overlapping hulls apart), deals damage to BOTH hulls as raw component damage (shields/armor first, same treatment as Scuttle), scaled by the RAMMER's own closing speed as a fraction of its top cruise speed (`ramDamageAt` in `combatData.ts`) — a slow bump barely scratches, a full-speed charge is a real, costly commitment. The rammer takes a fixed `RAM_SELF_DAMAGE_FRACTION` (0.5) of whatever it dealt. One-shot: the order clears itself the instant it connects, mutated in place on the resolver's own `separated` array entry (same pattern the ship-separation pass itself already uses for position). Doesn't gate on having a weapon range to close to (unlike Chase/`approachNode`), so an unarmed hull can still ram. Mutually exclusive with Chase/holdPosition/inherit-velocity in the store, same as those are with each other.

## Combat system (Phase 15 — missile/torpedo range mechanics, and a real slot-based Ship Builder)

Two related but separately-scoped changes, done in that order: first a global weapon-mechanic change affecting every existing hull, then a wholly additive ship-customization system built on top of it.

**Part A — missile damage falloff and torpedo accuracy (every ship, old and new).** Every direct-fire weapon (energy/kinetic) is completely unaffected — full listed damage anywhere inside range, exactly as before. Missiles and torpedoes now behave differently from each other and from direct fire:
- **Missiles** keep 100% tracking — no accuracy roll at all, ever — but do less DAMAGE the further past their `optimalRangeUnits` they're fired from, down to a floor (`MISSILE_FALLOFF_FLOOR = 0.6`) at max range. `missileDamageMultiplier`/shared `rangeEffectiveness` in `combatData.ts`.
- **Torpedoes** keep fixed damage per hit, but the hit itself is now a roll: `torpedoAccuracy` combines the same range-taper (floor `0.5`) with a target-size multiplier (`TORPEDO_SIZE_ACCURACY`, keyed by the TARGET's `HullSizeClass` — small `0.55` up to X `1.15`, since a slow torpedo is simply easier to land on a bigger hull) and the target's `evasion` stat, clamped to `[0.05, 0.98]` so neither a certainty nor an impossibility. Combined multiplicatively with chaff's existing miss chance in the firing loop (`applyShot`'s `chaffMiss` param renamed `missChance` — it always meant "additional pre-rolled miss chance," torpedo inaccuracy is just another source of the same thing).
- **Flak** (`DefenseProfile.flakRating`) stacks onto the point-defense interception screen, but only against torpedoes specifically — a wide burst that does little against a nimble missile but disproportionately punishes something big and slow.
- `optimalRangeUnits` was deliberately tuned close to each weapon's own `rangeUnits` (missile ~86%, torpedo ~87%) rather than as a short-range peak — Kite already holds at 92% of longest range (`KITE_RANGE_FRACTION`), and that's exactly the position the proven Medium scenarios rely on, so optimal has to sit near where ships actually already fight or the falloff would have gutted existing proven balance. Verified: the full scenario suite (Easy/Medium/Hard thresholds) is unaffected — re-ran clean with zero threshold changes needed.
- `CombatProfile` gained a required `sizeClass: HullSizeClass` (small/medium/large/x — civilian+Corvette=small, Frigate+Destroyer=medium, Cruiser=large, Battleship=x) and `DefenseProfile` gained required `evasion`/`flakRating` fields (both `0` on every hand-authored hull) — additive, no behavior change to any existing hull beyond the missile/torpedo mechanic itself.

**Part B — the Ship Builder (wholly new, additive system; zero changes to any existing preset hull's stats).** The explicit design choice, confirmed with the user before writing any code: the five hand-authored presets (`CORVETTE_PROFILE` etc.) and the `ShipClass` entries that reference them are left **completely untouched** — the builder never edits a preset, it only ever builds something new from scratch.
- **Five slot categories** (`SlotCategory`: weapon/armor/shield/defense/upgrade — "defense" holds point-defense/flak modules, "upgrade" holds Afterburner and any future non-weapon system), each sized small/medium/large/x (`SlotSize`, literally the same type as `HullSizeClass` — a slot's size and a hull's size class are the same idea). A module fits any slot at least as big as itself — **down-fit only, never up-fit** (a Small autocannon drops into an X slot fine; a Titan Beam never fits a Small one).
- **`HullChassis`** (`hullChassis.ts`) is metadata only — size class, a fixed (non-slotted) component-HP/speed baseline via the same `hullMotion()` every preset uses, FTL drives, and a concrete slot budget — never a `CombatProfile` itself. `HULL_CHASSES` holds six chassis mirroring the five warship presets' identities by name/size (plus Civilian), each in a **separate array from `SHIP_CLASSES`** with no id collision.
- **`buildCombatProfile(chassis, design)`** is the load-bearing function: sums a `ShipDesign`'s equipped modules (by category) into a standard `CombatProfile` — weapons list assembled from equipped weapon modules, armor/shield HP and shield regen summed from equipped plates/generators, point-defense/flak summed and capped (`0.9`/`0.6`), evasion summed and capped (`0.6`) from Afterburner-family upgrades, speed/acceleration multiplied by `(1 + summed speedBonusFraction)`. Because every existing combat/UI function (`stepEngagements`, `overallHealthFraction`, Screen's toughness ranking, `ShipPanel`, ...) already operates on the generic `CombatProfile` shape, a profile built here works everywhere those already do — **verified live**: a custom design spawned through the store fought a real engagement end-to-end (formed, entered combat, rendered correctly on `ShipPanel` with exactly the equipped modules' stats) with zero changes needed to any combat-resolution code.
- **`designToShipClass`** wraps a built profile into the same `ShipClass` shape a preset already is (id namespaced `design:<id>`), and **`resolveShipClass`** (`shipClassResolver.ts`) is the one new indirection every `classId`-consuming call site now goes through instead of `SHIP_CLASSES.find` directly — presets first (cheap, common case), custom designs as fallback. This is what makes a custom design visible everywhere a preset already was (combat resolution, ship physics, `ShipPanel`, the Debug Console's spawn picker, the Fleet Manager roster, the escape-behavior hook) without any of those seven call sites needing to know a design store exists.
- **`useShipDesignStore`** (in-memory only, no persistence — same as every other store in this project right now) holds player-built `ShipDesign`s and their CRUD actions; the Ship Designer tab's new **Custom Designs** sub-tab is the actual builder UI (see the `FleetManagement.tsx` entry above).
- Module catalog content (`shipModules.ts`) is new but deliberately modest — the six existing weapon archetypes slotted by size (small: Autocannon/Missile Battery, medium: Mass Driver/Laser/Torpedo Tube, large: Heavy Beam) plus one new X-slot weapon (Titan Beam, since nothing previously filled that tier), four armor/shield/defense tiers each, and four Afterburner tiers (speed + evasion, per the user's explicit ask) — not an attempt to fully flesh out a tech tree, just enough to make every slot size and category meaningfully fillable.
- **Verification**: a new `tests/shipDesigns.test.ts` (its own file, same reasoning as `scenarios.test.ts` being split from `combat.test.ts` — a separate system, not a combat mechanic) covers slot-fit rules, the chassis catalog, `buildCombatProfile`'s summing and capping, `resolveShipClass` for both presets and designs (including create/equip/clear/delete), and a full `stepEngagements` fight involving a spawned custom design. Live-verified in the browser: built a design in the actual UI, and separately drove the store directly (`window.__designs`/`__ships`/`__combat`/`__resolve`, cleaned from `main.tsx` after — see this file's own verification note) to spawn one, watched it form a real engagement, and confirmed `ShipPanel` showed exactly its equipped modules' stats (40 shield HP from one Deflector, 25 armor from one Light Plating, 15% PD from one Point Defense Turret, 1× Autocannon).
- **Bugfix, same session**: the ship builder's Civilian Hull chassis had been given a weapon slot AND a point-defense (Defense-category) slot — directly contradicting `CIVILIAN_COMBAT_PROFILE`'s own explicit design ("unarmed, fragile... exist to be chased, not to fight"). The user caught this by asking "why do civilian ships have weapons and stuff?" Fixed by zeroing out `weapon`/`defense` in `hullChassis.ts`'s `civilian-hull` chassis (`slots: { weapon: [], defense: [], ... }`) — armor/shield/upgrade slots stay, so a custom civilian design can still be built tougher or more evasive, just never armed. `tests/shipDesigns.test.ts`'s "every chassis has a slot in every category" assertion had to be relaxed to warships-only for the same reason (civilian-hull is now the one legitimate exception).

## Technology system (Phase 1 — the Physics tree, and its first four mechanical hooks)

Three independent research trees (Physics/Society/Engineering, matching the Stellaris convention this is explicitly modeled on — see `techData.ts`), each with its own research-point pool, a "you can see two nodes past anything you've already researched" fog-of-war visibility rule, and a fully-populated Physics tree (30 nodes across 8 open real-world-physics branches — including a later-added Biology branch — plus a locked Anomalous) per the user's own detailed design brief — Society/Engineering are structurally real (every helper works for them) but content-empty, reserved for the collaborator's track and an unclaimed future one respectively.

- **The defining scope decision, corrected once mid-design and then held to for everything downstream**: an early draft of the plan left Warp Drive/Hyperdrive permanently ungated (reasoning: every hull already has one, and FTL drive isn't a builder-selectable module). The user explicitly rejected that — "current mechanics like warp and hyperdrive should be gated... you may make the technologies 'already researched' for now so that gameplay remains the same." The actual fix: Relativity's **Warp Theory** and Extradimensional's **Hyperspace Theory** nodes genuinely gate the two drives inside `shipPhysics.ts`'s `planMove` (the single, already-player-only entry point for real FTL travel — it refuses any non-player ship at its very first line, so no separate allegiance check was needed), but a fresh country's `TechState` (`techStore.ts`) seeds `researched` with both already in it. The gate is real and live-read every call, not a hardcoded bypass — it just starts unlocked, so nothing regresses today. This is the pattern every other wired hook in this phase follows: **default-unlock what's already working, gate what's new.**
- **Four mechanical hooks actually wired this pass** (everything else described — thermal cloaking, sensors/jammers, mirror coating, Dyson swarms, exotic-matter/antimatter research, hyperium synthesis — is a real, researchable, described node whose gameplay effect is explicitly marked not-yet-wired, the same honest-placeholder spirit as `bodyStats.ts`'s heuristics or Outliner's empty Colonies/Starbases, since the systems those would hook into — stealth/detection, a production chain for exotic matter/hyperium — don't exist yet):
  1. **Warp/Hyperdrive gating** (`planMove`, default-unlocked — see above).
  2. **Ship-builder module gating**: `shipModules.ts`'s `WeaponModule`/`ShieldModule`/`DefenseModule` gained an optional `requiresTechId`; Laser/Heavy Beam/Titan Beam need Electromagnetism's Directed Energy Weapons, every Shield tier needs Shielding, every Defense (PD/Flak) tier needs Point Defense Systems. `modulesForSlot()` gained an optional `researchedIds` filter param. **Deliberately NOT default-unlocked** — custom designs are new this same session, so there's no existing capability to preserve, unlike warp/hyperdrive. The five hand-authored presets are completely unaffected either way (they don't go through this catalog at all).
  3. **Mandatory orbit-in-combat** — the flagship new mechanic, and the one genuinely large piece of this phase. Ships currently free-float anywhere they stop in the arena; physically that's wrong, and it should default to the same real orbital motion resting ships already have in system/satellite view. Classical Mechanics' **Free-Flight Maneuvering** node is what restores today's free-hold behavior; without it, a thrust-alive player ship holding position (empty path) instead gets a target velocity of `orbitalHoldVelocity` — a new pure function in `combatArena.ts`, right next to `gravitationalAcceleration`, reusing its exact same per-body inverse-square G·M plumbing: circular-orbit speed `v = sqrt(g(d) * d)`, direction `up × radialDirection` (one consistent rotational sense shared by every ship near the same body, so two ships holding position circle the same way rather than crossing). Recomputed fresh from the ship's CURRENT position every step — not solved once as a fixed ellipse — so a ship nudged off its circle by combat maneuvering settles onto a new one rather than snapping back, matching the same approximate/self-correcting style `stanceDestination`/`approachNode` already use elsewhere. Wired into `integrateMotion` (`combatResolution.ts`) via a new `canFreeFloat` param (default `true` — every existing caller/test unaffected), threaded through `stepEngagements` via a `playerCanFreeFloat` boolean (kept a plain param, not a store read, so the function stays pure — `useCombatResolver.ts` is what resolves the player's actual researched set into it). **Only the player is ever gated** — hostile/friendly/neutral ships have no country-tech link modeled at all (`ShipInstance` only carries `allegiance`, not a country id) and keep today's unconditional free-floating, the same scope cut warp/hyperdrive gating made for the same underlying reason.
  4. (Research economy, not a combat mechanic but load-bearing for the other three) — `techStore.ts`'s per-country `researchPoints`/`researched`, `usePlayerTech.ts` joining it the same way `usePlayerEconomy.ts` already joins the real economy sim, and a dev-only "Grant Research" row in `DebugConsole.tsx` (category + amount) — the only way to gain points beyond the default seed, since research **income** isn't wired to the real economy sim (`economyStore.ts`/`economyTick.ts`) yet; that's the collaborator's territory, same honest gap `resourceData.ts`'s 8 HUD resources already sit in.
- **UI**: `NavBar.tsx`'s `Technology` category (previously reserved but unrouted) gained `subcategories: ['Physics', 'Society', 'Engineering']` and now renders `wide` (its multi-column branch grid needed the room — first attempt at the default narrow width had node labels overlapping their own Research buttons). `TechPanel.tsx`'s `NationTechPanel` mirrors `NationEconomyPanel`'s exact join pattern.
- **Verification**: new `tests/tech.test.ts` (prerequisite AND/OR logic including the real convergent Exotic Matter Theory / Hyperium Synthesis nodes, the 2-hop visibility rule, Anomalous's aggregate threshold, `techStore`'s default seeding and stable-reference behavior) plus new sections appended to `tests/combat.test.ts` (`planMove`'s warp/hyperdrive gating — a gated Corvette's interstellar trip measured **159,502.9 days** reaction-only vs **160.1 days** with warp, the kind of gap that leaves no doubt the gate is real; `orbitalHoldVelocity`'s speed/direction/rotational-sense math; `integrateMotion`'s canFreeFloat branch; a full `stepEngagements` scenario proving a gated player ship starts moving on its own while an identically-positioned hostile stays put, and that researching the tech restores stillness) and `tests/shipDesigns.test.ts` (module gating). Live-verified end to end via the store-probe method (`window.__tech`/`__player`/`__ships`/`__combat`/`__modulesForSlot`, cleaned from `main.tsx` after): granted research and unlocked nodes through the real `TechPanel` UI and confirmed the point totals/Researched labels updated; confirmed the Laser disappears/reappears from the ship builder's weapon picker exactly on Directed Energy Weapons; and — the most direct proof — reset a live player cruiser in an active Earth engagement to a dead stop, watched its velocity spontaneously become nonzero (genuinely drifting) with Free-Flight Maneuvering unresearched, then researched it and confirmed the same reset-to-rest this time held at a true `{0,0,0}` velocity.
- **A live-testing note worth keeping**: this sandbox's screenshot tool can go stale relative to actual app state after a direct store mutation (e.g. `usePlayerStore.getState().selectCountry(...)` from a script) — `get_page_text` (or another store read) confirmed the app had genuinely progressed past the onboarding gate well before a screenshot ever reflected it. When a screenshot looks like nothing happened right after a script-driven state change, check the DOM/store directly before concluding the action failed.

## Window manager fixes, a Tech Tree graph view, and Workspace Tabs

Four related UI requests in one session. The first two were small, unambiguous `DraggableWindow.tsx` bugs, fixed directly: **resize now anchors the top-left corner** (was centre-hung — the window's box is positioned via a `calc(-50% + Xpx)` transform, so growing width/height without correction expanded it symmetrically in both directions; the resize handler now shifts `pos` by half of whatever the size just grew by on the axis that re-centres, cancelling the drift on the edge that's supposed to stay fixed) and **clicking a window brings it to front** (a shared module-level `zIndex` counter every window instance draws from on mount and on any click, capture-phase so it works through a child button — previously every window shared one fixed CSS `z-index: 20`, so focus order was pure DOM mount order with no way back). The third — a real node-link **Tree View** for the tech tree — and the fourth — **browser-style tabs** — are covered in their own file-structure entries above (`TechTreeGraph.tsx`, `workspaceStore.ts`/`TabBar.tsx`). Worth recording here is how the tabs design actually landed, since it went through two real revisions from the user's own feedback:

- **First cut**: a permanent new row under the top header, always showing the tab strip. The user caught two real problems with this live: it needed the header+strip's COMBINED height threaded through to `useHudBarLayout` (so NavBar/Outliner would dock below both), which added real coupling for a feature that's supposed to reduce clutter — and mid-implementation the user said outright: **"make it a swap — we can either see the tabs or our current resources, but not both at the same time... this way we can reduce clutter."** Revised to occupy the SAME slot `ResourceBar` already had in the header (`HudCenterSwap`, a small toggle button flips between them) — the header's own height never changes, so the whole "combined height" problem disappears entirely rather than needing a fix. This is also just a better fit for a HUD that's already tight on vertical space.
- **Also requested**: tab renaming. `TabSnapshot.name: string | null` (a player override; `null` falls back to the auto-derived level+body label) plus `workspaceStore.renameTab`, triggered by double-clicking a tab (an inline `<input>`, Enter/blur commits, empty/whitespace normalizes back to `null` — done in the STORE action itself, not the UI handler, so every caller gets the same guarantee a tab can never render a literally blank title).
- **A real, previously-undiscovered bug found while verifying the user's explicit ask** ("make sure... the sidebars still correctly connect to the top/bottom bars"): `useHudBarLayout.ts`'s effect had a `[topRef, bottomRef]` dependency array — but a `useRef` object's identity never changes, and `App.tsx` calls this hook *before* its own onboarding-gate check (`if (!selectedCountryId) return <MainMenu />`), so on the very first render the refs are null, the effect no-ops, and — because the dependency array never actually changes value — it then never runs again once the header/footer actually mount after a nation is chosen. `--hud-top-height`/`--hud-bottom-height` had therefore never been set since the onboarding gate was added in an earlier merge; every docked panel had silently been running on hardcoded `52px`/`58px` CSS fallbacks the whole time — invisible when the header happened to be close to that height, but a real, measured ~24px overlap between NavBar and the header whenever the header wrapped to two lines (confirmed live, then fixed by re-running the effect on every render instead of once, and confirmed live again at `gapOrOverlap: 0` on both edges).
- **Architecture note for anyone extending tabs later**: the single-active-tab "snapshot out, restore in" design (see `workspaceStore.ts`'s own header comment) only works as cleanly as it does because a code search confirmed EVERY piece of "which window is open" state in the app already reduces to `viewStore` + `shipStore.selectedShipId` (NavBar's category state was the one exception, and got folded into `viewStore` for exactly this reason). If a future window ever needs its own open/closed state outside those two stores, it either needs its own snapshot slot on `TabSnapshot` or it will silently NOT be tab-scoped (staying visible/hidden the same way across every tab, which may or may not be what's wanted).
- **Gallery view (several tabs' scenes rendered live at once) was explicitly scoped OUT** after being raised as a real technical risk and confirmed with the user: the app currently mounts exactly one `<Canvas>` at a time; N simultaneous tiles means N real WebGL contexts (browsers cap concurrent contexts around 8-16) each duplicating a full per-frame orbital/physics render loop. Left as a clearly-flagged future phase rather than guessed at now.


- [x] Scaffold Vite + React + TS + three.js/r3f/drei project.
- [x] Build the Solar System (true-to-scale, orbit-controllable) as the starting point.
- [x] Stellaris-style time system (pause/speed/date) driving all orbital animation.
- [x] Realistic orbit tilts (inclination + ascending node) and orbit ring lines for all planets.
- [x] Full 4-level navigation (satellite/system/interstellar/galactic-stub) via click-to-fly and scroll-past-the-edge, both directions.
- [x] DEFCON/HUD-style procedural planet visual (wireframe grid + dot cloud + rim glow), no texture assets — generalized to stars too (`HologramBody`).
- [x] Moons: real data, realistic (time-dilated) orbits, inspectable, moon count in info panel.
- [x] Sol/stars inspectable via satellite view (renamed from "planet view" to accommodate this + future asteroids/black holes).
- [x] Draggable inspect-window UI with basic + Stellaris-style game info (habitability/districts placeholder heuristics).
- [x] Unify interstellar view onto the same select-first model as system view.
- [x] Moons: their own "Detailed View" + holographic representation (`MoonDetailScene`), reached from satellite view.
- [x] Moon habitability (reuses the planet heuristic via the parent planet's `orbitAU`).
- [x] Satellite view: prominent focused-object label above the hologram + click-the-hologram-to-select.
- [ ] Flesh out galactic view for real (currently intentionally empty).
- [x] Build out other stars' systems (every nearby star in `starData.ts` now has real planet data — see that file's entry above; done via the collaborator's track, not this session's work).
- [x] Ship data model (warp/hyperdrive/reaction drives, two example ship classes) + dev-only spawn console.
- [x] Ship selection (persists across view changes), right-click move orders, real-scale travel-time math, live location status text.
- [ ] Real gameplay/economy systems to back the "game info" habitability/district numbers (currently a placeholder heuristic).
- [x] Right-side collapsible outliner (in-view celestial bodies; real fleets + placeholder colonies/starbases sections; search + kind-filter; per-section collapse; click-to-select entries).
- [x] Left-side collapsible nation nav (placeholder Situations/Government/Technology/Society/Species/Contacts/Fleet Management panels).
- [ ] Broader DEFCON-style GUI/side panels beyond the current minimal HUD.
- [x] Fleet allegiance (player/friendly/neutral/hostile) + matching marker colors everywhere (viewport, Outliner, presence badges).
- [x] Interstellar-view no-text presence badges for fleets nested inside a system.
- [x] Ships render in a body's own satellite view once resting in orbit there, not just in system view.
- [x] Moons render an actual hologram (not just a marker dot) in satellite view.
- [x] Ownership enforced — any fleet is selectable/inspectable, but only player-owned ships can be commanded.
- [x] Move-order travel distance correctly accounts for a ship needing to cross interstellar space first, not just the in-system hop at the destination.
- [x] Ship ETA shown while travelling; resting-orbit ships drop their name label (Stellaris-style); interstellar presence badges are clickable.
- [x] A resting ship renders visibly beside the body/star it arrived at, not on top of it; satellite view supports right-click-to-order; warp drives have a (non-blocking) cooldown.
- [x] Ship markers guaranteed visible separation from their anchor body/star at any zoom (screen-space offset); orders settle globally regardless of which view is active; initial system-view camera framing is legible, not clustered.
- [x] "Go To" camera fly-in for a selected ship (ShipPanel); resting ships have real, continuous orbital motion in both system and satellite view, sharing the same underlying angular motion at each view's own scale.
- [x] Fixed stale-selection auto-navigation bug (all three enter-next-level watchers now also check !selectedShipId); hyperdrive "jump when ready" queueing; drive cooldown display in ShipPanel.
- [x] Ship camera-focusing (SelectionTracker) matching planets, gated behind a top-HUD lock-on toggle.
- [x] Warp drive: direction-change stop-and-start cooldown, opt-in "warp when ready" mid-flight auto-engage, player-toggleable "use warp at all," and mandatory gravity-well-escape phase before warp can fire. Hyperdrive intentionally untouched.
- [x] Ships ordered to a star with system data now arrive orbiting it (not resting beside it at interstellar scale); gravity-well escape time derived from real per-body mass/radius (escape velocity) instead of a flat number.
- [x] Ship-to-ship "follow" directive (right-click a ship while another is selected), re-targeting whenever the leader's destination changes.
- [x] Hyperdrive jumps carry a real, tunable loss-chance framework (base/established-lane rates), destroying the ship on failure and charting a rendered hyperlane on success; Hyperspace Scout and Turing Scout (0% loss) ship classes added.
- [x] Ship markers stack (with names) when sharing a body; visible orbit rings for orbiting ships in both system and satellite view; ship orbit speed toned down to moon-comparable pacing; hyperlane color matches orbit rings; a ship can enter a moon's exact orbit on the opposite side of its parent (synced period/phase/inclination); hyperdrive jumps queue while the clock is paused, firing the instant it resumes.
- [x] ShipPanel: "Current Action" line (was "Location," now also states when following another ship) and a "Jump Risk" figure for hyperdrive ships; orbit rings/hyperlanes brightened.
- [x] **Combat, Phase 1 (mechanics)**: tactical time mode; weapon/defense damage matrix (energy/kinetic/missile/torpedo vs shields/armor, point defense); three component healthbars + a blended overall; five warship preset classes; auto-forming engagements on hostile co-location; a 3D movement lattice; FTL charge-to-escape. See "Combat system" under Design Decisions.
- [x] **Combat, Phase 2 (view)**: `combat` as a real view level with breadcrumb + zoom-out-to-leave; the arena lattice rendered in three layers with a live density control; ray-scored node picking for click-to-move; latching manual movement vs. auto-approach; right-click target assignment and focus-fire component selection; per-ship path lines; arena markers with integrity bars and FTL-charge badges.
- [x] **Combat, Phase 3**: celestial bodies as real terrain (line-of-fire blocking + A* routing around them); the arena as a movable *window* on an unbounded lattice with a Recenter control; Fleet Management subtabs (Fleet Manager, Ship Designer preset viewer, Strategizer reserved). Phase 3's density-change remap turned out to still cause visible jumps — fixed properly in Phase 4 below.
- [x] **Combat, Phase 5**: ships store position+velocity instead of a hop (fixes the "new order teleports the ship to the last destination" bug at its root, and the vanishing route line with it); provably sub-light speeds anchored to a real light-speed constant, with acceleration/deceleration/turning from a single acceleration budget; four auto-combat stances (Balanced/Swarm/Kite/Stall) in the Strategizer; yellow dashed lines between actively-engaged pairs; Fleet Management window sized to its content; tactical time back to five tiers with speed adjustable while paused.
- [x] **Combat, Phase 4**: positions rewritten as real, density-independent coordinates (fixes the density-switch teleport bug at its root, not via a better remap); unobstructed moves go to the exact clicked point, not the nearest lattice intersection ("nodes are a pathfinding tool"); grid movement moved to right-click (left-click is now pure camera control); `activeEnemyContacts`/"Engaged Against" distinguishes being in a fight from actively trading fire; FTL risk gained a universal core-damage modifier and a combat-only active-engagement modifier (warp included, scoped so ordinary warp orders are provably unaffected); Turing Scout hyperdrive cooldown 27 → 7 days.
- [x] **Combat, Phase 6**: click picking rewritten — the click-catcher box was returning shell points, so every manual move order in the game had been landing on the arena wall (729/729 nodes mis-picked, worst by 19.81u); destinations now resolve to a drawn lattice node via screen-space picking with depth only breaking overlap ties; GRID_DIVISIONS fine 12 → 16 so the densities nest (4|8|16) and every placement is a fine-lattice point; route lines thickened to a real 2.6px via Line2 and restricted to player/allied ships.
- [x] **Combat, Phase 7**: auto-controlled ships re-track a moving stance destination in real time instead of freezing the instant a route existed; overshot sub-waypoints are dropped instead of forcing a ship to backtrack for them; a real physics bug fixed (dead-utility velocity was snapping to zero instead of coasting); flying into a body is now fatal; bodies have real gravity felt only by a thrustless ship (curves/falls, doesn't just drift straight); new Flee stance, and the automatic fallback for any disarmed ship (mount-less OR shot offline); right-click targeting reachable from the Engagement panel roster, toggle-off on a repeat click; Fleet Manager/Outliner Fleets scoped to player-owned ships; Luna added as real terrain to Earth's own arena; satellite view renders bodies at genuinely different, real-ratio sizes instead of one flat constant; a Settings tab (route-line thickness); out-of-combat navigation lines with arrowheads; DraggableWindow collapse (anchored so the title bar doesn't move) and drag-clamped to the viewport; the dev console is no longer text-selectable.
- [x] **Combat, Phase 8**: fixed combat running at ~240x after any trip through strategic time (a compounding catch-up debt — 1 real second bought ~36 real minutes of hyperspeed); combat panels pinned to opposite screen edges so they stop covering the arena; hulls can no longer overlap; ships can disengage by outrunning the fight (the only exit that works with wrecked utility); unpowered hulls drift under real system-scale gravity out of combat, staying with their body and falling into it (two seeding bugs found by running it — heliocentric rest sent wrecks into the Sun, and the fleeing-ship offset sat outside Earth's Hill sphere); chaff as a 2-charge, 6-second, 50% incoming-miss countermeasure with AI usage; browser swipe-navigation blocked.
- [x] **Combat, Phase 9**: tools for playing against the odds — chaff raised to a flat 75% miss (an earlier range-falloff experiment was reverted for switching the tool off exactly when it was needed); fleet-wide focus fire in one click; a per-hull local-odds readout that makes splitting the enemy into 1v1s legible (enemies already split by proximity — the gap was visibility); Scuttle, a doomed-ship blast whose yield scales with remaining core; and point defense made destructible by scaling it with the target's weapons component.
- [x] **Combat, Phase 10**: an "Add Scenario" option in the Debug Console — 6 pre-built fights at Easy/Medium/Hard, where the tier is a PROVEN claim (a separate 16-seed-per-configuration simulation suite, `tests/scenarios.test.ts`) rather than author judgment: Easy wins hands-off, Medium loses hands-off but wins with the one stance change baked into the scenario, Hard loses on every stance the game has.
- [x] **Combat, Phase 11**: Sol's arena no longer renders inside the star (camera framing scaled off `arenaWindowSpan`, memoized to stop fighting OrbitControls); "Enter Combat" now shown instead of "Enter Arena" when a live hostile pair is actually present; fixed the bug where giving an in-arena ship an order immediately force-exited the player back to system view (`syncEngagements`' live-fight check was reading a genuine 2-ship battle as "not contested" the instant one ship got an order).
- [x] **Combat, Phase 12**: fleet-scale UI — every ship always belongs to a fleet (`fleetId`, auto-merge on co-located arrival), one arena marker per fleet instead of per ship (`clusterRestingShipsByFleet`), split/merge UI in Fleet Management, scrollable panel bodies, and an engagement total-HP bar (not headcount) across the top of the Engagement panel.
- [x] **Combat, Phase 13**: fleet-wide strategy system — a `'fleet'` sentinel stance that defers to the ship's own Fleet's strategy (individual non-`'fleet'` stances still override it for free), the Strategizer grouping ships by fleet, and three genuinely coordinated multi-ship behaviors: Divide (spreads target assignment, not just movement), Condense (regroups on the fleet centroid), Screen (toughest half holds a wall between the fleet and the nearest enemy).
- [x] **Combat, Phase 14**: engagement lines are 3-color (yellow mutual / red hostile-can-hit-only / green friendly-can-hit-only), sharing new `rangeContactStatus`/`rangeFavor` functions with a matching green "(favored)"/red "(unfavored)" tag on ShipPanel's "Engaged Against" row; Scuttle now damages everything in the blast including your own side (was hostile-only); a new Ram order lets any ship — armed or not — charge its target and collide, hurting both hulls, damage scaled by the rammer's own closing speed, self-damage a fixed fraction of what it dealt, one-shot per collision.
- [x] **Combat, Phase 15**: missiles now do less damage past their optimal range (100% tracking, damage-only falloff); torpedoes now roll accuracy instead of guaranteed damage, driven by range-from-optimal and the TARGET's hull size (bigger = easier to hit) plus a new evasion stat; Flak modules add torpedo-only bonus point defense. A real, additive Ship Builder: five slot categories (weapon/armor/shield/defense/upgrade) sized small/medium/large/X, a six-chassis catalog separate from the five hand-authored presets (which are completely untouched), a module catalog, `buildCombatProfile` turning a chassis + equipped modules into a standard `CombatProfile` that works everywhere existing combat/UI code already does, and a `resolveShipClass` indirection making custom designs spawnable and usable everywhere a preset is. New Custom Designs sub-tab in the Ship Designer.
- [x] **Technology, Phase 1**: three independent research trees (Physics/Society/Engineering), Physics fully populated (28 nodes, 7 real-world-physics branches + locked Anomalous), a 2-nodes-into-the-future fog-of-war visibility rule, per-country research points, and four wired mechanical hooks — Warp/Hyperdrive genuinely gated behind Relativity/Extradimensional (default-unlocked so nothing regresses today), Laser/Shield/Point-Defense ship-builder modules gated behind Electromagnetism (NOT default-unlocked), and the big one: ships in combat now default to actually orbiting the body they're fighting near instead of free-floating, unless Classical Mechanics' Free-Flight Maneuvering is researched — player-only, real physics (`orbitalHoldVelocity`, reusing the existing gravity model). New `Technology` nav category (Physics/Society/Engineering sub-tabs), a dev-console "Grant Research" row (no real income system yet).
- [x] **Window manager + Tabs**: `DraggableWindow` resize now anchors the top-left corner (was centre-hung) and clicking a window brings it to front (shared z-index counter). A real node-link Tree View for the tech tree (`TechTreeGraph.tsx`, portalled above everything, showing genuine multi-branch convergence). Browser-style **workspace tabs** — each with its own independent view/selection/open-window state, switched via an instant snapshot/restore (`workspaceStore.ts`) rather than keeping multiple `<Canvas>`es alive — occupying the header's centre slot as a swap with `ResourceBar` (never both at once), with tab renaming. Gallery view (several tabs rendered live at once) explicitly scoped out as its own future phase — real WebGL-context-count risk, confirmed with the user rather than guessed at. Also fixed a real, previously-undiscovered bug found while verifying this: `useHudBarLayout`'s CSS vars had never actually been set since the onboarding gate was added (a `useRef`-identity dependency-array bug), so every docked panel had silently been running on hardcoded fallback offsets the whole time.

# Current Objective:
Core 4-level navigation (select-first and consistent at every level, now with Outliner click-to-select too), time system, satellite-view inspection (planets, stars, moons, moons' own detail view, now with real hologram moons), and a fleet system (allegiance-colored, selectable from the Outliner, visible at every zoom level via full markers, presence badges, or stacked-with-names markers, with real warp/hyperdrive cooldown, gravity-well mechanics, ship-follow, moon-relative orbit syncing, a hyperlane/jump-risk network, and a ShipPanel that surfaces current action + jump risk clearly) are all working end-to-end.

**All ten combat phases are complete and verified.** Tactical time with automatic entry/exit; the full weapon/defense damage matrix with point defense; component healthbars; five warship presets; auto-forming fleet-vs-fleet engagements; FTL charge-to-escape; the playable arena (3D lattice, live density control, right-click movement, latching manual control vs. auto-approach, right-click targeting, focus-fire); celestial bodies as real terrain that block line of fire and force ships to manoeuvre around them; the arena as a movable window on an unbounded lattice; the Fleet Management subtabs; and — Phase 4 — ship/body/window positions rewritten as real, density-independent coordinates (the actual fix for the "teleport to nearest intersection on density switch" bug, which Phase 3's remap-based approach only partially addressed), unobstructed moves landing on the exact clicked point rather than a lattice node, right-click-only grid movement (freeing left-click for pure camera control), an "actively engaged" distinction from mere co-location, and FTL risk modifiers for core damage and active combat (including a new, deliberately combat-only warp-escape risk that provably never touches ordinary warp orders). Phase 5 then replaced the hop-based motion model with real position+velocity physics (fixing the teleport-on-reorder and vanishing-route-line bugs at their root), made ships provably sub-light with acceleration and deceleration, added four auto-combat stances in the Strategizer, drew yellow dashed lines between actively-engaged pairs, fixed the Fleet Management window sizing, and gave tactical time five tiers with pause-independent speed control. Verified by a 90-check seeded pure-function suite plus live browser testing, including two real interaction-testing lessons: this sandbox's react-three-fiber canvas only recognizes a synthetic `contextmenu` when dispatched as a `PointerEvent` (not the DOM-native `MouseEvent` type), and real wall-clock time advances between agent tool calls, so timing-sensitive checks must run inside a single call rather than across several.

Phase 6 then fixed the click path itself, which turned out to have been broken since Phase 4: the invisible click-catcher was a solid box, and a box raycast can only return a point on its own surface, so **every manual move order in the game had been landing on the arena's outer wall** (measured: 729/729 standard-density nodes mis-picked, worst by 19.81 units in a 12-unit arena). Destinations now resolve to a lattice node actually drawn at the current density — screen proximity deciding, depth only separating dots drawn on top of each other — with the remaining ambiguity (nodes genuinely sharing a view ray) documented as irreducible rather than hidden. `GRID_DIVISIONS` fine went 12 → 16 so the three densities nest as 4 | 8 | 16, which is what makes "every placement is a fine-lattice point" true at any density and stops a standard-density click landing up to 0.87 units off the dot clicked. Route lines gained a real 2.6px width (Line2; WebGL ignores `linewidth` on `LineBasicMaterial`) and are now drawn only for player-owned and allied ships. Verified by 120 pure-function checks plus live browser testing of the real right-click handler — 8/8 exact picks from the default camera, 5/6 from a second angle with the one miss provably collinear.

Phase 7 (see its own section above) then fixed a class of bug the real-time-tracking rewrite exposed — auto-piloted ships freezing their destination the instant any route existed, sub-waypoints acting as hard checkpoints instead of a pathfinding aid, and a genuine velocity-preservation bug in the zero-thrust branch of `integrateMotion` — added a real gravity model (felt only without thrust, per the design brief's "flat space" simplification), a Flee stance with an automatic disarmed-ship fallback, targeting/UX polish (roster right-click, collapsible/clamped windows, out-of-combat navigation lines with arrowheads, a Settings tab), fixed satellite view rendering every body at one identical size, scoped the fleet-list UI to player-owned ships, and added Luna to Earth's own arena as real terrain.

One open item carried forward: **combat balance is still untuned by playtesting** — gravity and the AI fixes above change fight dynamics again and haven't been played against each other yet. The AI's stance/auto-approach destinations remain continuous rather than lattice-aligned (unchanged from Phase 6's note) — "ships sit on intersections" still only holds for player-issued orders.

**Phases 11-14 (see their own sections above) then took combat from "works for one fight" to "works at fleet scale, with real fleet-level tactics and two more decisive tools for a losing position."** Every ship now always belongs to a fleet (auto-merging, one marker per fleet, split/merge UI, an engagement total-HP bar) rather than the arena getting unreadable past a handful of ships. The Strategizer gained real fleet-wide coordination (`'fleet'` sentinel stance, Divide/Condense/Screen) on top of the five per-ship stances, so a fleet can be given one order rather than clicked through ship-by-ship. The engagement lines and ShipPanel now agree on, and surface, WHO actually has the range advantage in a given pair (`rangeContactStatus`/`rangeFavor`), not just that a fight is happening. And two hull-vs-hull tools were reworked/added together: Scuttle now genuinely punishes bad positioning (hits your own side too, no more free area-denial), and a new Ram order gives even an unarmed hull a way to hurt something, trading a chunk of its own hull for a bigger chunk of the target's, scaled by how much speed it actually built up first. Two investigations this session (raised by the user noticing combat "felt" different) each turned up NO regression, confirmed via a `git worktree` A/B test against the pre-session commit with an identical seeded script on both: the Raider King's Escort Hard scenario is unchanged (still 0/16, but both destroyers reliably kill both corvette escorts first — a genuine near-miss, kept as-is), and "two destroyers can't beat a battleship any more" traced to a lone-vs-escorted-battleship distinction in what was actually being tested, not a balance change.

Natural next steps, in rough order of how much they'd add:
- **Gallery view** — the deferred half of Workspace Tabs: several tabs' scenes rendered live at once in a grid (up to 4-9, player's choice). Needs a real performance budget designed against (N simultaneous WebGL contexts, throttling background tiles' frame rate) before starting, not guessed at — see that phase's own Design Decisions note.
- **Combat feedback**: no visual for a shot being fired, intercepted, or blocked — right now damage just appears on bars. A tracer/impact layer and a "no line of fire" indicator would make the terrain rules legible without reading the panel.
- **Ship Builder polish/tuning**: the six chassis' slot budgets (`hullChassis.ts`) and the module catalog's numbers (`shipModules.ts`) are a first pass, explicitly the part of Phase 15 most likely to want adjusting once real designs are actually built and played — no resource/cost system gates building one yet either (any design is free to build and spawn), which is fine for now but is the next thing that would make custom designs a real strategic choice rather than a free toy.
- **Technology: real research income.** Points only exist via the dev-console grant right now — the next real step is hooking Physics/Society/Engineering research into the actual economy sim (`economyStore.ts`/`economyTick.ts`), which is the collaborator's territory to design (labs? scientist pops? a flat per-world rate?). Also worth revisiting: Society/Engineering content (still empty), and wiring more of Physics's described-but-not-yet-wired nodes (thermal cloaking and sensors/jammers in particular now have an evasion-like precedent — see combat's `DefenseProfile.evasion` — that a stealth mechanic could plausibly extend rather than invent from scratch).
- **A real playtesting pass now that fleet-scale tools exist** — Divide/Condense/Screen, the favor indicator, Scuttle's friendly fire, Ram, the Ship Builder, and now tech-gating/orbit-hold are all individually verified (pure-function tests + live browser checks) but haven't been played against each other in a real multi-fleet fight yet.
- Everything still unstarted from before: the galactic view, star systems beyond Sol (**note: this is now done** — see `starData.ts`'s entry above, corrected this session — leaving this line only to flag that the roadmap below had gone stale on it), and the rest of the collaborator's economy/politics track (see the "Conquest Directive" workflow doc referenced below).

# Additional Context:
Update this file with relevant, useful information as you go.

- **Two-person team split**: the user owns combat/tech; a collaborator owns economy/politics (Victoria-3-style simulation). A scaling workflow for this split was written up as a published HTML artifact ("Conquest Directive") in an earlier session — not stored in-repo, so re-derive or ask the user for the link if it's needed again rather than assuming its contents.
- **This project's browser sandbox cannot reliably render a second WebGL context** (see the `CombatViewScene.tsx` code comment, and Phase 5's own note above) — screenshots of the live app can silently fail or show a stale/black frame. The established, reliable verification method is direct store/JS inspection: temporarily expose the zustand stores on `window` from `main.tsx` (`window.__ship = useShipStore`, etc.), drive the exact same store actions the UI buttons call, and inspect state via `javascript_tool` — then ALWAYS restore `main.tsx` to its clean original content before ending the turn. `tests/combat.test.ts` (pure functions, no store/browser needed) remains the primary, fastest verification path and should be extended first for any new combat mechanic.
- **The dev server serves from a `/TRCONQUEST/` base path**, not root — a fresh browser tab lands on `http://localhost:5173/TRCONQUEST/`, and any direct `fetch`/dynamic `import()` of a source file for ad-hoc verification needs that prefix (e.g. `/TRCONQUEST/src/state/shipStore.ts`), not a bare `/src/...` path.
- **A "Choose your nation" onboarding gate exists in `App.tsx`** (added by the collaborator's economy/politics work, encountered via a merge) and resets ship/combat store state — driving the live app from a fresh page load requires clicking through it (any of the four nation cards) before ships/combat exist.
- House rules, reconfirmed again this session: never commit unless asked; always clean up scratch files and temporary `window.__x` debug probes in `main.tsx` immediately after verification (confirm via `git status --short` showing no diff on that file); kill stale dev-server processes on port 5173 before starting a fresh one; full verification sweep = `npx tsc -b` + `tests/combat.test.ts` + `tests/scenarios.test.ts` + `tests/escape.test.ts` + `tests/economy.test.ts` + `tests/shipDesigns.test.ts` + `tests/tech.test.ts` + `tests/workspace.test.ts` + `npm run build`.
- **Bugfix: zooming out from a non-Sol star's system view back to interstellar re-focused the camera on Sol instead of the star just left, then visibly PANNED from Sol to the star.** Two layered bugs, found in two passes because the first fix only addressed the data half:
  1. `viewStore.ts`'s `inViewSelection` (what `InterstellarScene` reads to decide which star `SelectionTracker` locks onto) was unconditionally reset to `null` by the plain `enterInterstellar()` action, which was also what the zoom-out `DistanceThresholdWatcher` in `SolarSystemScene.tsx` called. Fixed with a new `exitSystemToInterstellar()` action (mirroring `exitSatelliteToSystem`'s existing carry-the-selection-forward pattern one level down) that seeds `inViewSelection` from the current `selectedStarId` before switching levels — only the zoom-out watcher uses it; the breadcrumb's INTERSTELLAR button still calls plain `enterInterstellar()` with no preselect, matching every other "plain jump vs. continuity" pair here (e.g. `enterSystem`'s optional `preselectBody`).
  2. Even with the correct star selected from frame one, the user immediately caught that it was still visibly SWEEPING from Sol to the star rather than starting there already — because `SelectionTracker` eases `controls.target` every frame from wherever it currently is (see its own header comment: this is deliberate for an in-scene click, smoothly re-panning to a new selection), and both the Canvas's `camera` position AND `OrbitControls`' initial `target` were still hardcoded to the plain "nothing selected" defaults (`[20, 30, 55]` / world origin — which IS Sol's own position, `starData.ts`'s `sol` entry sits at `[0,0,0]`), so the target had a genuinely huge distance to visibly ease across before settling. Fixed by mirroring `SolarSystemScene`'s `continuityBodyRef` pattern one level up: a `continuityStarIdRef` captures `inViewSelection` once at mount (not reactively — a later in-scene click shouldn't retroactively move where the camera STARTED), and both `initialCameraPosition` (the returning star's position + the same `[20,30,55]` offset the default view uses, just re-anchored) and `initialTarget` (the star's position outright) are memoized with an empty dep array and fed to the `Canvas`/`OrbitControls` props — so there's nothing left for `SelectionTracker` to visibly ease, it's already there. The empty-dep-array memoization matters for the same reason `CombatViewScene`'s camera-prop note already flags: a fresh array/object literal passed to a three.js prop every render actively fights `OrbitControls`, not just wastes a render.
  - Verified live for both: (1) via the store-probe method, entering Alpha Centauri's system then calling the new action left `inViewSelection: 'alpha-centauri'`; (2) two screenshots taken back-to-back (and again 1.5s later) after triggering the transition show Alpha Centauri already centered and selected in the very first rendered frame, pixel-identical to the later one — no sweep. A plain `enterInterstellar()` arrival (breadcrumb/galactic) was re-checked afterward and still uses the unchanged wide default framing.

## Open Questions (Economy/Politics track)
- Whether to commit the M2 work (offer stands).
- Alpha Centauri / Sirius planets Orion owns beyond Arcadia/Proxima b: treated as claimed-but-uninhabited for now.

## Next Steps (Economy/Politics track — per design doc §10 build order)
1. ~~M2: Production Methods + throughput + employment/qualifications~~ **DONE** (+ owner-autonomy / economic-system law).
2. ~~M3: Standard of Living loop~~ **DONE** (SoL → growth + education drift; consumption tiers). Also done in the same pass (ahead of order): ~~M4 energy + expanded goods/chains~~ (electricity + 23 goods + ~24 buildings), and the corporations/characters/shareholding/stock-exchange layer (design doc §3e/6, pulled forward at user request).
3. **Balance tuning** — the economy works but is uneven (Venus/Lalande healthcare low; some worlds richer than others). Retune seed building levels per world + the global levers (`JOB_SCALE`, `INVENTORY_DECAY`, `GROWTH_*`, species need amounts). Consider a headless balance harness that prints per-world SoL/needs.
4. **Milestone 5: Trade & logistics** (freighters/range/transport cost + market-access price blending) — this is what lets worlds SPECIALIZE instead of each being self-sufficient, and is the natural next system. THIS IS THE NEXT BIG MILESTONE.
5. National government depth (multiple tax forms, more laws as modifiers — economic-system law is the first; more central-bank policy). Corporate/dividend tax already flows to treasury.
6. Politics depth (interest groups by all axes + discrimination; then parties, legislature, Political map mode). Also: let players **build under a corporation's charter** (createCorporation makes empty corps today), assign buildings to corps, and deepen character interactions (marriage, succession, appoint-as-leader).

## User Preferences (Economy/Politics track)
- Wants realism first (Vic3-level), optimize after. Dislikes abstract/confusing numbers (fixed the "2.3 pops" issue).
- Eager for breadth/progress ("get to it", "more everything"); references Vic3 & TNO economy guides.
- Wants to interact with buildings/employment, not just read them (→ construction done, PMs next).
- Appreciates honest feasibility assessments over hype.
- Git commit conventions: branch off main if needed; end commit messages with `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`; only commit/push when asked.
